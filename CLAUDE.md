# StockPulse — Claude Code Context

## What this is
A stock momentum screener POC. State lives in **Turso** (hosted libSQL/SQLite). Price data comes from the Twelve Data API. The owner (admin) manages portfolios and refreshes data; the public sees a read-only cached snapshot.

The repo/folder is `StockPulse`; the app is branded **Tickr Lab** in the UI (`<title>` and the bar wordmark).

**93 symbols**, 14 of which are deliberate ballast — see **The universe, and the ballast that has been draining out of it**. **Read that section before trusting any number in this file**: the universe was 116 with 33 laggards until 2026-09-10, most measurements here were taken on that universe, and shrinking it moved them. Before designing any backtest, read **Does any of this predict anything? — the research log**: four framings have been tested and all four came back flat, and it records what is ruled out so the next attempt is a new one.

## Running the app
```
node --use-system-ca server.js
```
Runs on port 3000. Requires `.env` with `TWELVE_DATA_API_KEY`, `TURSO_DATABASE_URL`, `TURSO_AUTH_TOKEN` and `ADMIN_PASSWORD`.

**Careful when running it just to look at the UI:** `computeStocks()` calls `ensureProfiles()`, which re-pulls any profile older than a day and rewrites the `profiles` and `snapshot` tables, burning API credits. To check frontend changes only, serve `public/` with a throwaway static server instead — the page falls back to admin-open mode when `/api/me` fails, so the whole chrome renders without touching the API.

## Key files
| File | Purpose |
|---|---|
| `server.js` | Express backend — all API routes, auth, data computation |
| `db.js` | Turso persistence layer — every read/write goes through here |
| `migrate-to-turso.js` | One-off JSON → Turso seeder; `--commit` to write, idempotent |
| `momentum.js` | **Momentum scored from bars and nothing else** — no database, no API, no universe. Shared by the live refresh, the backfill and the chart |
| `analysis-db.js` | **The local SQLite copy of the archive, for analysis.** `--full` rebuilds, no flag syncs, `--stats` reports. Never query Turso for research when this exists |
| `private/action.js` | **The Action rules** — Company Type + Action from ordered rule lists, first match wins. Shared by server and browser |
| `action-test.js` | 64 hand-built cases, one per rule with its expected flag. Run after touching any rule |
| `action-backtest.js` | Replays the technical rules over 18 years of bars; reports per-Action downside (p10) |
| `purge-orphans.js` | Deletes data for symbols in no portfolio; `--commit`, `--only`. Dry run by default |
| `backfill-bars.js` | One-off deep pull of the daily bar archive; `--commit`, `--depth`, `--only` |
| `set-password.js` | Local account admin — list accounts, set a password, change a role |
| `momentum-model.js` | Writes an Excel model of the momentum score for one symbol — see **The Excel model** below |
| `public/app.css` | **Shared stylesheet** — tokens, atmosphere, bezel, buttons, table base, row card. Linked by all four pages |
| `private/index.html` | Single-page frontend (no build step, vanilla JS, page-specific CSS inline) |
| `private/strategy.js` | **The backtest engine** — signal to position to daily P&L. Pure functions; `require`d by the runner and loaded by the page |
| `strategy-runs.js` | Builds `private/strategy-index.json` + one `strategy-u-*.json` per universe: every rule variant over every portfolio. Runs against the local copy |
| `single-data.js` | Builds `private/single-closes.json`: every symbol's daily closes, delta-encoded, for `/single` to simulate in the browser |
| `private/single.html` | The single-stock strategy at `/single` — one stock in or out, plus the same rule swept across the whole universe |
| `private/strategy.html` | The strategy backtest at `/strategy` — equity curve, drawdown, costs |
| `private/indicators.js` | **Tunable short-horizon indicators, defined once** — loaded by `/lab`, `require`d by the server and the offline grid |
| `lab-grid.js` | Builds `private/lab-grid.json`: the cross-sectional result for every parameter setting the lab can reach. Runs against the local copy, never Turso |
| `private/lab.html` | The indicator lab at `/lab/<SYMBOL>` — sliders, a chart, and the universe-wide truth beside it |
| `private/stock.html` | One stock in full at `/stock/<SYMBOL>` — chart, range buttons, every field |
| `private/chat.html` | The assistant at `/chat` — any signed-in user, see **Chatbot** below |
| `private/visitors.html` | Admin-only visitor log page at `/visitors` |
| `private/users.html` | Admin-only account maintenance at `/users` — list and delete, no add |
| `private/contact.html` | Signed-in contact form at `/contact` — subject + message, mailed to the owner |
| `private/help.html` | User-facing help at `/help` — how scoring works, what each momentum factor means |
| `private/screens.js` | The momentum weight presets and `cleanWeights()` (its old `SCREENS` list is unused since /analysis was folded into Screens) |
| `private/filters.js` | **The column-filter grammar, defined once** — `filterValue`, `compileFilter`, `screenRows`. Loaded by the screener and `require`d by the server, which runs screens for the phone page |
| `private/mobile.html` | The phone page at `/m` — screens, rows, a stock sheet, and a view switcher. No settings of its own |
| `private/mobile-setup.html` | The admin editor at `/mobile-setup` — which views the phone offers and what each row shows |
| `private/cardshot.js` | **The rasteriser, defined once** — inlines computed styles, embeds the fonts, paints a card onto a canvas at its true 1080px. Used by the studio, `/cards` and the phone |
| `.github/workflows/nightly-refresh.yml` | The 4:15PM Eastern Refresh all — see **The nightly job** below |
| `public/favicon.svg` | Momentum-line mark, emerald on OLED black |
| `portfolios.json`, `snapshot.json`, `profiles.json`, `names.json`, `visitors.log` | **Legacy.** Pre-migration backups only — nothing reads or writes them any more. Safe to delete once you trust the database. |
| `.env` | `TWELVE_DATA_API_KEY`, `TURSO_*`, `ADMIN_PASSWORD`, optional feature flags |

## Persistence (Turso)
Everything goes through `db.js`. Tables: `portfolios`, `portfolio_tickers`, `names`, `profiles`, `snapshot`, `visitors`.

- **The accessor names are unchanged** from the flat-file era (`readPortfolios`, `writeProfiles`, …) and return the same shapes, so call sites only gained an `await`. `server.js` destructures them from `db.js`.
- **Writes are whole-collection replaces** — delete-then-insert inside one `db.batch`. That mirrors the old "rewrite the file" semantics and keeps `writeProfiles({})` working as the cache-clear `/api/refresh-all` depends on. Fine at tens of rows; revisit if the universe grows.
- **Portfolio order is an explicit `position` column.** The UI colours portfolios by index, so order has to survive the round trip — don't rely on insertion order.
- **`profiles.data` is a JSON blob** because it caches a third-party response whose shape we don't control. `fetched_at` is lifted into its own indexed column because the 24h TTL check runs against it every refresh.
- **`/api/visitors` aggregates in SQL** now (counts + `LIMIT 500`), instead of parsing the whole log into memory on every request.
- **`/api/refresh-all` expires profiles, it does not delete them** (`expireProfiles()` sets `fetched_at = 0`). Deleting them used to strip sector, market cap and fundamentals out of the shared snapshot for the ten-odd minutes the backfill ran, so every other viewer saw the holes. `readProfiles()` treats `0` as "keep the values, drop the timestamp": the row still renders, and `ensureProfiles()` still re-pulls it. **The blob also carries a `fetchedAt`, so the column has to override it** — otherwise the stale copy inside the JSON makes an expired profile look fresh.
- **Express 4 does not catch async handler rejections.** Every async route is wrapped in the `route()` helper near the top of `server.js`, which turns a database error into a 500 instead of a hung request. Any new async route must use it.

## Deploys — one hard rule
**Production deploys happen via `git push` only.** On 2026-09-12 the site went down for an hour with no commit involved: `vercel` CLI deploys made from another folder replaced production with foreign code (its logs said "Legacy server listening…", a string not in this repo), and every database route 500'd while `/api/health` stayed green. Diagnosis path when "site down, no commit": check `npx vercel ls stock-pulse` for deployments that do not match the git log, and `npx vercel logs <deployment>` for a boot line that is not ours. Fix: `npx vercel promote <last git-built deployment>`. The Turso token was never the problem — `Turso: schema ready` in the logs clears the credentials immediately.

## Serving and the gate
**Two directories, and which one a file lives in decides whether a stranger can read it.**

| | `public/` | `private/` |
|---|---|---|
| holds | `login.html`, `reset.html`, `app.css`, `favicon.svg`, `logo.png` | every other page, the shared JS modules, every research JSON |
| served by | Vercel's CDN, without invoking the function | this Express app, behind `gateAssets` |
| reachable signed out | yes, deliberately | no |

- **`public/` is a Vercel static directory, and the CDN answers it before the function ever runs.** That is why `GATED_PAGES` — the redirect bouncing `/chat.html` to `/chat` — **had never once executed in production**. Every gated page answered 200 to anyone with the URL, as did `single-closes.json`, `lab-grid.json` and all sixteen `strategy-*.json` files: the whole price archive, with the portfolio names inside it. Locally it worked perfectly, which is exactly why it went unnoticed for so long. Confirmed by `X-Vercel-Cache: HIT` on `/single.html` against `MISS` on `/single`.
- **The fix is the directory, not the code.** Everything requiring a session moved to `private/`, which the CDN knows nothing about, so the request falls through to the function and the guard actually decides. URLs did not change — `private/` is mounted at the root, after `public/`.
- **Never put anything in `public/` that is not meant for a stranger.** There is no code path that can protect it.
- **`gateAssets` guards only what `private/` actually holds**, matching a filename set read at boot, and calls `next()` for everything else. This is load-bearing: `app.use()` sees every request that reaches it and **every API route is registered below that line**, so the first version — which refused outright — swallowed `POST /api/login` and made signing in impossible. Not just the gated pages; the entire site. Caught by testing the signed-**in** path, which is the half that is easy to skip.
- **A page request redirects to `/login`; an asset request answers 401 JSON.** Content negotiation cannot tell them apart — a browser's `fetch()` sends `Accept: */*` exactly like a navigation, so `req.accepts()` answered "html" for both and every data file got a redirect, which `fetch` follows, handing the page a login form and HTTP 200 where it expected JSON. `Sec-Fetch-Dest` is what a browser sets to say what a request is *for*; the file extension is the fallback for clients that omit it.
- **`GET /api/health` is deliberately open and returns a count, nothing else.** `private/` is not a Vercel static directory, so if the platform ever stopped bundling it with the function every gated page would 404 and it would look like a routing bug. `{"ok":true,"assets":35}` says which it is.
- **The offline builders write into `private/`** (`single-data.js`, `strategy-runs.js`, `lab-grid.js`), and `server.js`, `momentum-model.js`, `lab-grid.js` and `strategy-runs.js` all `require` the shared modules from there.
- **The APIs were never affected** — `/api/stocks` 401s, `/api/visitors` 403s, and the pages were empty shells without a session. No user data or credentials were exposed. What was readable was the derived market data and the page source.

## Auth model
The app is a **door**: the screener is shared, and accounts only decide who gets in.

- **Anonymous** → `/` redirects to `/login`; `/api/stocks` and `/api/portfolios` return 401.
- **`guest`** (2026-09-13) → the "Try as guest" button on the login page: a deterministic HMAC cookie (`st_guest`, 24h, `guest-access-v1` salted with ADMIN_PASSWORD — rotating the password revokes every guest cookie), **no account, no sessions row**. Guests see **`GUEST_SYMBOLS`** (env, default NVDA/JPM/PTON/JOBY/DELL) and nothing else, **enforced server-side on every route** — the CDN incident's lesson. Filtered: `/api/stocks` (+ portfolio names), sparklines, news/latest, the stock-page universe list. Symbol-guarded 403: history, stock, news, model, and the `/stock/:symbol` page redirects. Member-only (`requireMember`): portfolios, chat, contact, signal, lab APIs; the research pages redirect `/`; gateAssets 403s the research JSONs (`single-closes`, `strategy-*`, `lab-grid`) for guests. Prefs: **a guest must never touch the `'admin'` prefs key** (`prefsKey()` falls through to it) — GET returns `{}`, PUT 403s. `/api/me` carries `guest: true`; the screener hides the research menu and Ask button and reword the read-only note. **Acceptance = the route walk as a guest AND as a member** — both were run on introduction: guest saw exactly 5 symbols everywhere and 403/redirects elsewhere; the member path was untouched (93 rows, all pages, research JSONs).
- **`member`** → sees the same shared screener, read-only (sort, collapse, personal portfolios). **Members and guests see an identity watermark** (2026-09-14): `private/wmark.js`, one script tag on each of the eight data pages, tiles the account email (guests: "Guest preview") across the viewport at 5% opacity — `.wmark` in app.css, fixed, `pointer-events: none`, z-index 70 so a capture of anything carries it. It prevents nothing (nothing in a browser stops a screenshot); it SIGNS captures, which is the honest version of "restrict screenshots". The owner is skipped, as is open mode. Copy-blocking was considered and rejected: `/api/stocks` hands any signed-in browser the whole table as JSON, so blocking Ctrl+C is friction dressed as security.
- **`owner`** → everything: add/remove tickers, Refresh, Refresh all, the forward-returns view, visitor log, user management.
- `ADMIN_PASSWORD` unset → fully open, everyone is admin (local dev). Set → sign-in required.

Accounts live in Turso (`users`, `sessions`). Passwords are hashed with **`crypto.scrypt`** and a per-user random salt — no dependency needed. Sessions are random 32-byte tokens in the `sessions` table with a 30-day expiry, so a single session can be revoked; they are *not* the old deterministic HMAC cookie, which cannot work for more than one user.

- **The first account created becomes `owner`**, so a fresh install bootstraps itself. Everyone after is a `member` — and since 2026-09-14 **every member registration starts `pending` and cannot sign in until the owner approves it** (`users.status`, backfilled `'active'` for everyone existing). Enforced three ways: registration creates **no session** for a pending account, login answers 403 "awaiting approval" (only AFTER the password verifies, so the endpoint still cannot enumerate accounts — a wrong password gets the generic 401), and `getSessionUser` refuses pending rows outright. The sign-up notice to the operator IS the approval request ("Approval needed: <email>", button to `/users`); the **welcome email moves to approval time**, when it is true. `/users` shows a pending badge, an Approve button (`POST /api/users/:id/approve`, idempotent) and an Awaiting-approval stat; Delete is how a request is declined. The login page tells a registrant they are waiting rather than bouncing them to a page that would refuse them; approval is logged to the activity log (`account` / `approved:<email>`). Guests are unaffected — the preview stays the try-before-you-register door.
- **`SIGNUP_CODE`** (optional env var) gates registration. Without it anyone who finds the URL can sign themselves up — set it before making the site public.
- **`POST /api/login` with a password but no email still checks `ADMIN_PASSWORD`.** That escape hatch is deliberate: a broken `users` table can't lock you out of your own instance. **Do not change the HMAC salt `'stock-tracker-admin-v1'`** in `adminToken()` or every legacy admin cookie dies.
- Login failures are counted on the user row (`failed_count`, `locked_until`) and lock the account for 15 minutes after 8 tries. Tracked per user, not per IP, because an in-memory counter is useless on serverless.
- Wrong password and unknown email return the **same** error, so the endpoint can't enumerate accounts.
- **Password reset is by email**, via `POST /api/forgot` → a link to `/reset?token=…` → `POST /api/reset`. `set-password.js` remains the local fallback for when mail is unavailable or an account is locked out of its own inbox; it prompts rather than taking the password as an argument, which would leak it into shell history. `--role owner|member` changes a role, refusing to demote the last owner.
  - **The token is never stored — only its SHA-256**, for the same reason sessions and passwords are hashed: a database read must not hand anyone a working reset link.
  - **Single use and 30 minutes.** Valid tokens are deleted as they are read, so a link cannot be replayed even while unexpired, and issuing a new one deletes any older link for that account.
  - **`/api/forgot` answers identically whether or not the address exists** — the login endpoint already refuses to confirm which emails have accounts, and this one would otherwise give it away. It also refuses to issue more than one token a minute per account, or it becomes a way to flood someone's inbox.
  - **The password length is checked before the token is consumed**, so a typo does not burn the link.
  - `setPassword()` already drops every session and clears the lockout counter, which is what a reset should do.
- A signed-in user can change their own password via `POST /api/password` (needs the current one). Both paths drop that user's sessions, so a change signs other devices out.
- **`/users` is list-and-delete only, never create.** Accounts come from sign-up (gated by `SIGNUP_CODE`); an admin-created account would need a password set for it, which means either mailing a secret or inventing one — `set-password.js` already covers the rare case properly.
- **`deleteUser()` purges `prefs` and `chat_usage` too.** Those are keyed on email, not user id, so without it a new account registered at the same address would inherit the deleted one's saved column layout and its chat quota for the day.
- **Two guards, both enforced server-side and mirrored in the UI as a disabled button:** the only owner cannot be removed, and nobody can delete the account they are signed in as — that would revoke their own session mid-request. `listUsers()` also returns active session count, last sign-in and any lockout, which is what makes the page worth opening.
- Password hashing (`hashPassword`, `newSalt`, `verifyPassword`) lives in **db.js**, beside the users table — `server.js` and `set-password.js` both import it so scrypt parameters can't drift apart.
- `isAdmin(req)` and `isSignedIn(req)` are **async** now — always `await` them. `requireAdmin` / `requireAuth` are middleware built on the `route()` wrapper.

## Email
**Every message goes through one shell.** `emailShell()` and `textShell()` in `server.js` wrap a dark header carrying the wordmark, a white card, and a footer with the tagline, the site link and a one-line note saying why this particular message arrived — the question a reader asks first about mail they did not expect. Four messages use it: the welcome, the password reset, the contact form and the refresh report.

- **Tables and inline styles, not the app's CSS.** Outlook renders through Word, most clients strip `<style>` blocks, and neither flexbox nor grid is available. The wordmark is text rather than an image, because images are blocked by default in most clients and a logo would leave a broken box exactly where the brand should be.
- **The palette is for a light background**, not lifted from the app. Mail clients invert or ignore dark themes unpredictably, and a screenshot-black email arrives unreadable somewhere.
- **`mailButton()` wraps the pill in a table cell** — Outlook ignores `border-radius` on an `<a>`, so the radius goes on the cell.
- **The From display name comes from `BRAND`, not from `MAIL_FROM`.** `fromHeader()` keeps only the address out of that variable, so renaming the product cannot leave a stale name sitting in everyone's inbox — which is exactly what happened when Ticker Lab became Tickr Lab.
- **A hidden preheader repeats the intro**, so the grey line clients print beside the subject is the real first sentence rather than whatever markup happens to come first.
- **Registration sends two notes to two people**, both after the response and both with their rejections swallowed — the account and session already exist by then, so a slow or failing mail provider can never hold up or fail a sign-up. `sendWelcome()` greets the new member; `sendSignupNotice()` tells the operator, with `Reply-To` set to the new member so a welcome reply reaches them directly.
- **The sign-up notice is skipped for the very first account**, which becomes the owner — otherwise it is an email telling you that you have joined your own site. It is skipped again when the destination *is* the address that just registered. Both guards are verified. **Since 2026-09-14 it is the approval request** — subject "Approval needed", button to `/users` — and the welcome is sent by the approve route instead of the register route (the owner's own welcome still goes at registration).
- **`operatorEmail()` (`REPORT_TO`, else the owner account) resolves the destination for both the refresh report and the sign-up notice**, so the two cannot drift apart. `contactDestination()` stays separate because `CONTACT_TO` is a deliberately different inbox.

Sent through **Resend** over plain `fetch` — a REST call does not justify a fourth dependency. Needs `RESEND_API_KEY`, `MAIL_FROM` and `APP_URL`; without all three `MAIL_READY` is false and `/api/forgot` still returns its normal reply while logging that mail is unconfigured, so the endpoint never reveals the difference.

- **Sending is scoped to `mail.tickrlab.com`**, a subdomain, so a reputation problem cannot reach the domain the site is served from. SPF, DKIM and an `MX` for bounce feedback live under it; **DMARC sits at the root** (`_dmarc`), where one policy covers every subdomain.
- **DMARC started at `p=none`** — monitoring only, no delivery effect. Tighten to `p=quarantine` after the aggregate reports come back clean.
- **`APP_URL` is `https://www.tickrlab.com`**, the canonical host — the bare domain 308s to it, and a link in an email should not depend on a redirect.
- **Nothing receives mail at the sending address.** Set `MAIL_REPLY_TO` to a real inbox, or add a forwarder (ImprovMX or Forward Email work with Vercel DNS; Cloudflare Email Routing does not, since it needs Cloudflare as the DNS host).
- Upstream errors are logged by type only, never echoed to the client — the response body can restate the request, and the key travels in the same headers.
- **`/contact` is signed-in only**, which is what makes it safe without a captcha or a honeypot: every sender is a known account and the address comes off the session rather than a form field. That is also why the address can be used as `Reply-To` — replying in a mail client answers the person, not the server.
- **The subject has CR and LF stripped, not escaped.** A newline in a header is how a subject line becomes extra headers; the body is the only place free text belongs. The subject is prefixed `[Tickr Lab]` and capped at 120 characters, the body at 4,000.
- **Destination is `CONTACT_TO`, falling back to the owner account's email**, so the form works even if the variable is never set.
- The daily cap reuses `chat_usage` under a `contact:` key prefix — that table is a generic per-key-per-day counter that happens to be named for its first caller.

## Feature flags in .env
- `ENABLE_FUNDAMENTALS=true` — requires Twelve Data Pro+ plan
- `ENABLE_ANALYST=true` — requires Twelve Data Ultra+ plan

## Visitor logging
- Every `GET /` inserts a row into the `visitors` table (fire-and-forget, so a logging failure can't block the page)
- Fields: `ts`, `ip`, `ua` (user-agent), `ref` (referrer)
- Admin-only endpoint: `GET /api/visitors` — returns summary + last 500 entries
- Admin-only page: `/visitors` — same visual system as the screener

## Activity logging
**Who did what, fact-only — the `activity` table** (`ts`, `user`, `kind`, `detail`, `ip`), separate from `visitors` on purpose: that one is the anonymous front-door knock, this one is a known account doing a thing. Facts, never content — a chat row says `asked`, not the question. Viewed at **`/activity`** (admin only, linked from the bar and from `/visitors`), which shows per-user and per-kind rollups plus the filterable 500-row tail, and carries a **Clear log** button (`DELETE /api/activity`, confirm-with-count like the visitor one). Pruned to `ACTIVITY_KEEP_DAYS` (60) during every refresh, fire-and-forget.

- **Two halves.** Server routes call `logAct(req, kind, detail)` (fire-and-forget, never blocks the action): `page` (every gated page, symbol included), `login` (password / account / signup / guest), `chat` `asked`, `refresh` (plain / all / as-of — a Refresh All logs ONCE at its POST; its `?refresh=1` rounds are skipped via `opts.running`), `portfolio` CRUD, `model`, `contact`, `account` (logout / password-changed / reset). The client half is **`private/track.js`** (`window.track(kind, detail)`), loaded by the screener and the stock page for what the server never sees: `sort`, `tab`, `picker` (columns / weights / past / rules), `export` (the CSV is fully client-side), `panel` (ladder opens), `chart` (ranges, MA / RSI / Mom / Trend toggles, why-card).
- **The beacon batches** — events queue and flush as one `sendBeacon('/api/activity')` every ~20s, on a full queue and on `pagehide` — so a sorting session is one Turso write, not thirty. `POST /api/activity` (any signed-in user, guests included) allowlists the six client kinds (`CLIENT_ACT_KINDS`), clamps details to 80 printable chars and caps a batch at 50, so it cannot become free-form storage — the prefs-PUT rule.
- **Guests are the point.** The shared `st_guest` token cannot tell two guests apart, so the guest door also sets **`st_gid`** — a random id used ONLY as the log key (`guest-<id>`), kept if already present so a returning guest stays one trail. Deliberately noise-free: no `/api/status` polls, no sparkline/news fetches, no hovers, no refresh-all rounds, no cron.
- Verified on introduction with an admin walk + guest walk against a local boot: junk kinds dropped, oversize details clamped, guest 403 on GET/DELETE, anonymous 401, clear removed all rows.

## Frontend layout
Everything above the table is **one bar**. There is no separate masthead, no tab rail, no chip row — they were merged to reclaim vertical space (the table used to start ~685px down; it now starts ~91px down).

```
.page (100dvh flex column, never scrolls)
├── header.bezel.bar          ← wordmark · portfolio picker · columns picker
│                               · add-ticker field · as-of field · status
│                               · Refresh · Export · ⋯ more menu
├── #readonlyNote / #fwdViewBar / .error    (flex: none, shown conditionally)
└── .table-bezel (flex: 1, min-height: 0)
    └── .table-wrap           ← the only scroll container on the page
```

- **The page itself does not scroll.** `body` is `height: 100dvh; overflow: hidden`; the table bezel is the flexible row and `.table-wrap` scrolls inside it. Never reintroduce a `max-height: calc(100dvh - Npx)` on the table — its height is derived from the flex column, so adding a notice steals from the table instead of pushing the page taller.
- **`.deck`** (add-ticker field, as-of field, status, Refresh) is `display: contents` on desktop, so its children join the bar's flex row directly. Below 768px it becomes a fixed full-screen glass sheet toggled by `#menuBtn` (a hamburger that morphs into an X, fixed to the top-right corner).
- **`.status`** uses `flex: 1 1 0` — a zero flex-basis makes it invisible to flex line-breaking, so it can never wrap a button onto a second line; it absorbs the slack instead. It's a **block** with `text-align: right`, not a flex row: a flex row justified to the end clips its *start* with no ellipsis.
- The bar stays on one line down to ~1280px; below that it wraps, which is intended.

### Dropdown menus
Four menus share one controller. Only one is open at a time.

| id | trigger | contents |
|---|---|---|
| `#picker` | portfolio name + count | All + every portfolio + "New portfolio" |
| `#colPicker` | `Columns n/10` | multi-select column groups + Show all / Hide all |
| `#researchPicker` | `Research` | Analysis, Strategy, Single + the three per-symbol pages |
| `#morePicker` | `⋯` (anyone signed in) | Delete portfolio (admin, contextual), Help, Contact, Change password, Sign out |

**One admin door, not a dozen (2026-09-14).** Everything that was not a description of the table in front of you moved to **`/admin`** (see **The admin console**), and the bar now carries a single `Admin` link for the owner. Gone from it: the Weights picker, the Research picker (for the owner), the forward-returns calendar, the add-ticker panel, Refresh, Refresh all, the Visitor and Activity icons, and the Users / Promo rows in `⋯`. What is left describes the view, in this order since 2026-09-15 (owner's call): **Columns, Portfolio, Sector, Industry, Advice, Changed chip, Filter, Promo Studio**, then status (the Rules picker was removed the same day), Ask, Admin and the account menu. Nothing depends on the order — no sibling selectors — so reordering is moving markup.

- `openPicker(id)` opens one and closes the rest; `closePickers()` closes all. They close on outside click, Escape and resize.
- `placeMenu()` clamps an opened menu inside the viewport — it shifts left of its trigger rather than overflowing, and never crosses the left gutter. Don't replace it with a static `left`/`right` anchor; no single anchor suits both triggers at every width.
- `#morePicker` holds the *real* buttons (`#renameBtn`, `#deleteBtn`, `#changePwBtn`, `#logoutBtn`) restyled as `.pick` rows, so their existing listeners and disabled-state logic still apply. `#portfolioActions` and `#sessionActions` wrap the contextual rows with their separators so both hide together.
- **`#researchPicker` is `member-only` now** — it is a member's only way to the research pages, and members cannot open `/admin`, which carries the same list for the owner. `applyAdminUI()` hides it for the owner and the guest branch still hides it for guests, in that order.
- **`#researchPicker` ("Research") holds every page that studies the data** — Analysis, Strategy and Single, then a separator and the three per-symbol pages (Stock, Signal study, Indicator lab). It replaced four separate labelled buttons in Sep 2026: the bar already carried six pickers and eighteen buttons and wrapped below ~1280px, and four nav buttons was the largest block that could become one.
  - **The three per-symbol rows open whatever is on top of the current view.** `researchSymbol` is set from `sorted[0]` in `render()`, not from the ranking, so it follows the sort and the portfolio filter rather than quietly always meaning rank 1. All three destinations carry their own symbol picker, so which one it lands on matters little — the point is not having to hunt for a name to click first. The menu names the stock it will open, because a link whose target moves should say where it is going.
  - **With no rows at all the per-symbol half is replaced by a line explaining why**, rather than three links to `/stock/undefined`.
  - **It uses `we()`, the page's top-level escaper.** The only `esc` here is scoped inside the CSV export — see the warning below; this is the third function that would have tripped over it.
- **Ask and Admin are the only top-level link buttons left.** Refresh, Refresh all, the logs and the studio are rows on `/admin` now.
- The column menu is rebuilt by `renderColumnMenu()` from inside `applyGroups()`, so the trigger count can't drift from the table state. It deliberately stays open while you toggle.
- **Each menu row also carries a jump arrow** that scrolls that group into view — 47 columns across ~3,300px is more than a scrollbar should be asked to do, and the groups are how anyone thinks about the table anyway. The toggle and the jump are siblings in a `.pick-row`, because a button cannot nest inside a button. The arrow is disabled while its group is collapsed.
- **`scrollToGroup()` works from `getBoundingClientRect()` plus the current `scrollLeft`, never `offsetLeft`.** A table cell's `offsetParent` here is `main.page`, not the table, so `offsetLeft` overstates by the table's own inset and lands the group ~29px *underneath* the frozen columns. It also subtracts the frozen block's measured width, or the target hides behind Symbol and Name.
- **Arrow keys scroll the table horizontally**, shift for a page, home/end for the ends — the hint at the foot of the columns menu is where they are advertised, since Shift+wheel already works natively but nobody knows. The handler ignores keystrokes aimed at an input and does nothing when there is no horizontal overflow.

## Design system
Dark-only. "Ethereal glass": OLED black with a fixed radial mesh aura and a film-grain overlay, glass chrome, hairline borders.

### Where CSS lives
`public/app.css` holds everything common to all four pages — tokens, `.aura`/`.grain`, `#sprite`/`.ic`, `.bezel`/`.core`, `.btn` and variants, the signal colours, table base, `#rowcard` and `.reveal`. Each page links it and then keeps **only its own layout** in an inline `<style>`.

- **The page's block loads second, so it wins ties.** That is how `login.html` keeps its full-width white submit button and its tinted card interior while still inheriting the rest.
- **Match the shared selector exactly when you mean to override it.** `login.html` styles `.bezel > .core`, not `.core`, because the shared rule is more specific and would otherwise win regardless of order.
- **Watch grouped selectors.** A page rule like `th, td { font-size: … }` sits after the shared `th { font-size: … }` at equal specificity and silently takes it over. `index.html` and `analysis.html` set their table font sizes on `td` alone for this reason.
- **`[hidden] { display: none !important }` sits at the top of `app.css`, and is load-bearing.** The attribute is only a plain UA declaration, so *any* rule of ours that sets `display` beats it on specificity and the element keeps painting — `#chart .readout { display: flex }` left an empty 22×12 glass pill parked over the stock page's change caption. Don't remove it, and don't write a `[hidden]` variant beside each new `display` rule instead. Audited when it was added: the readout was the only element on any of the seven pages that it changed.
- Before changing a shared rule, check the other three pages — the four style blocks used to be copies of each other and drifted (`20px` where the token said `var(--r-core)`, buttons that had lost `font-family`).

- **Type** — `Geist` / `Geist Mono` from Google Fonts (`--sans` / `--mono`). Numeric table cells use the mono face with `font-variant-numeric: tabular-nums`. This is the app's only external dependency; it degrades to `system-ui` offline.
- **Double bezel** — every card is `.bezel` (translucent shell, hairline, `--r-shell` radius, 6px padding) wrapping `.core` (opaque `--surface`, `--r-core` radius, inset top highlight). `--r-core` = `--r-shell` − padding, for concentric curves.
- **Icons** — inline SVG sprite in `#sprite`, used as `<svg class="ic"><use href="#i-name" /></svg>`. No emoji, no icon font. Add new glyphs as `<symbol id="i-...">` with 1.35px strokes on a 24×24 viewBox.
- **Motion** — only `--ease` / `--ease-soft` cubic-beziers, never `linear` or `ease-in-out`. `.reveal` + `IntersectionObserver` gives blocks a fade-up-and-deblur entry; table rows stagger via `--i` on each `<tr>`. All of it is disabled under `prefers-reduced-motion`.
- **Performance rules** — `backdrop-filter` only on fixed/sticky elements (island menus, mobile sheet, tooltip), never on a scrolling container. The grain and aura are fixed `pointer-events: none` layers. Animate `transform`/`opacity` only.
- **Layers** — `--z-sheet: 45`, `--z-nav: 46`, `--z-tip: 55`, `--z-grain: 60`. Menus sit at `z-index: 30` inside `.page`'s stacking context.

### CSS tokens (`:root` in `public/app.css`)
```
--void #050505      page          --text  #e9ecf2   17.2:1
--surface #0a0c11   card interior --muted #9aa3b2    7.7:1
--surface-2 #0d1017 group headers --faint #7d8797    5.4:1
--shell   rgba(255,255,255,.028)  --green #34d399   --red   #fb7185
--hair    rgba(255,255,255,.07)   --amber #fbbf24
--hair-2  rgba(255,255,255,.13)   --accent #7c9cff  --accent-2 #a78bfa
```
`--bg`, `--panel` and `--border` are kept as aliases so older rules and inline styles keep resolving.

**The two greys are contrast-floored.** Ratios above are against `--surface`; both clear WCAG AA (4.5:1). `--faint` used to be `#59616f` at 3.1:1 and it painted the column headers, the hover card's field names and every em-dash placeholder — the least legible text in the app. Don't darken either one back below 4.5:1.

**`--surface` must stay opaque.** The frozen table columns and the sticky header cells paint on it to mask the rows scrolling underneath; a translucent value makes them see-through.

## Column groups (frontend)
Each group has a fixed colour used for the group header `th` and its row in the columns menu:
- Scores `#a3e635` (group id `rank`) · Info `#7c9cff` · Chart `#a8a29e` · Short-term `#34d399` · Long-term `#a78bfa` · Forward `#fb923c`
- Relative `#22d3ee` · Trend `#fbbf24` · Volume `#f472b6` · Size `#94a3b8` · Fundamentals `#fb7185`

**To add a group:** append the id to `GROUPS`, give it a colour in `GROUP_COLORS` and a label in `GROUP_LABELS`, add the `th.group` banner with the right `colspan`, and tag every header/body cell with `class="grp-<id>"`. **The header cells and the body cells must be in the same order, and a group's columns must be contiguous** — a banner spans by `colspan`, so interleaving two groups puts every banner over the wrong columns and silently mismatches headers to values. Then mirror it in **three more places**: `GROUP_ORDER` and `FIELD_SPEC` in `private/rowcard.js`, and the palette copies at the top of `private/analysis.html`. `applyGroups()` and the columns menu pick it up with no further changes.

**The Price Momentum group is gone (2026-09-15, owner's call): its one column, Mom., moved into Scores after Overall** — Scores now reads Overall · Mom. · Qual. (banner colspan 3, Mom. centred like its neighbours), `pmom` is removed from `GROUPS`, `GROUP_COLORS`/`GROUP_LABELS` in index.html, analysis.html and rowcard.js, and the hover card's Mom. rows (rating and score) sit in the Scores section. A saved `collapsed.pmom` is simply ignored; column views are unaffected because they key on column ids (`momentumScore`), not groups. Verified headlessly: layout and alignment, the tooltip, the hover card, collapse, and the views / filter-row / advice suites unchanged. The **Scores** group (id `rank`, kept so saved prefs and `grp-rank` classes still resolve) carries the three score columns (Overall, Mom., Qual.) and **Info** the five metadata ones (Portfolios, Price, Sector, Market Cap, Next Earn) — they were one group until they were split, so scores and metadata collapse independently.

**There is no Rank column.** It was removed in Sep 2026: it only ever restated the row's position under the default sort, and read as a contradiction when the table was sorted by anything else. Sorting by Overall gives the same ordering. It went from the table, the CSV, the hover card, the stock page chip and `/api/stock` together.

The **Size** group carries the absolute-size columns — Revenue TTM, Gross Profit TTM, Gross Margin, Net Income TTM, FCF TTM, FCF Margin, Net Cash. Every one comes out of the `/statistics` call `fetchProfile()` already makes, so the group costs **no extra API credits**.

**A Short name column sits beside Name** (2026-09-14). The feed has ONE name field and it is the legal one ("Space Exploration Technologies Corp. Class A", "SK hynix Inc. American Depositary Receipt") — no short, display or brand name exists anywhere in the API, checked across `/profile`, `/quote`, `/stocks` and `/symbol_search`. So `deriveShortName()` in server.js strips the furniture by rule — share class and instrument suffixes, then the incorporation word, then a trailing Holding(s), then a leading "The" — each step keeping the previous value if it would empty the name. **ETF is deliberately never stripped**: for a fund the instrument IS the product. 16 cases are unit-checked in the scratchpad pattern; "Datadog, Inc. Class A Common Stock" → Datadog, "Alibaba Group Holding Limited SP ADR" → Alibaba Group, "VanEck Semiconductor ETF" unchanged.

- **Only OVERRIDES are stored** (`names.short_name`, null until the owner types one). The rule runs at serve time, so improving it improves every name nobody edited, and an edited one is never quietly overwritten. An empty edit clears the override and hands the symbol back to the rule — the only way to undo a bad one.
- **Stamped on the way out** by `stampShortNames()` beside `scoreActionInto()`, and for the same reason: it derives from data already held, so a snapshot written before the field existed carries it and an override shows without waiting for a refresh. One query reads both name columns.
- The owner edits from a **pencil in the cell** (visible on row hover), `PUT /api/shortname`. **The link and the pencil live in a flex wrapper (`.snw`), and they have to**: `td.frz1 .namelink` is `display: block` so a long name can ellipsize inside the fixed 203px frozen column, which made the pencil a second block and doubled the height of every row in the table. Anything else added beside a frozen-column link needs the same treatment. Brand names are the case no rule can reach — the API will never say Alphabet trades as Google — so that is exactly what the override is for.

**Industry sits beside Sector** (2026-09-14): Twelve Data's `/profile` returns both and only the sector was being kept, though the call is charged the same either way — so the finer cut cost nothing to add. It is **Twelve Data's own (Morningstar-style) taxonomy, not GICS**: values read "Banks - Diversified", "Semiconductors", "REIT - Industrial". Checked against the GICS sheet on 2026-09-14 — the API carries no GICS field, code or endpoint (`/sectors`, `/industries`, `/classification`, `/gics` all 404). At the SECTOR level the two are a rename apart and map one-to-one (Basic Materials→Materials, Consumer Cyclical→Consumer Discretionary, Consumer Defensive→Consumer Staples, Healthcare→Health Care, Financial Services→Financials, Technology→Information Technology; the other five already match). Below that they do not map, and GICS company assignments are licensed S&P/MSCI data — approximating them from another vendor's judgement would be a wrong answer wearing the standard's labels. **The column fills as each profile is next re-pulled** — and since fundamentals rotate (see **The refresh budget**), that is over about `FUND_ROTATION_DAYS` (7) nightly runs, not one: on 2026-09-15, 152 of 271 rows had it. The console's full **Refresh all** fills it in one go.

**There is an Industry picker in the bar beside Sector** (2026-09-14). It nests: the menu offers only the industries present in the current tab AND the current sector, because an industry belongs to exactly one sector and offering "Banks - Diversified" under Technology is offering a guaranteed-empty table. Changing the sector drops an industry that is no longer in scope — checked against the rows rather than assumed, so *clearing* the sector keeps an industry that still applies. Both menus start from `tabStocks()`, so a count in one can never describe a different population from a count in the other. **The menu says how many rows in scope have no industry yet**, since the field arrives one profile at a time and counts that do not add up to the total otherwise look like lost rows. The empty-state line names the narrowest filter in force, which is the one that emptied the table. Verified with a headless harness that runs the page's own `scopedStocks()` over a synthetic universe: 9 checks, including the personal-tab path and the mismatched sector/industry pair the reset exists to prevent.

**An Advice quick filter sits beside Industry** (2026-09-15, `#advPicker`, `adviceFilter`): one Balanced verdict at a time, applied in `scopedStocks()` beside sector and industry, so the Changed chip and the empty state follow it like they follow those. **Balanced only** — it is the always-shown column and the one every other surface reads; the other profiles filter from the filter row. The menu lists verdicts Strong Buy down with ladder-coloured dots and counts over tab + sector + industry, and says how many rows have no verdict. The filter row's Balanced cell is a `BOUND_KEYS` entry writing the same `adviceFilter`, so the two cannot disagree and it is not counted as a column filter. Not saved, like the other quick filters. Verified headlessly: 11 checks (ladder order, counts, filter vs cells, two-way sync with the filter row, AND with sector, clear).

The **Info** banner spans eight columns — Overall, Mom., Qual., Portfolios, Price, Sector, Market Cap, Next Earn — and all eight collapse together.

### Site-wide hidden columns (the owner's floor)
**`/columns` (admin, its own page, linked from the console's Screener section) hides columns for EVERYONE (2026-09-15).** It was a panel on `/admin` for an hour; the owner moved it out rather than grow that page — the console stays a door. The page carries search, a per-group tick with an n/total count, Show all, **Undo changes** (back to what the server last confirmed) and a Save that is disabled until something differs. 85 columns is more than most accounts want as a default, and a view is a per-person choice; this is the floor under all of them.

- The console's **Scoring** section became **Screener** (what the table shows, and how it scores) and carries the row that opens it.
- **The catalogue is parsed out of `private/index.html`'s own header row** by `columnCatalogue()` in server.js (cached per instance): id from `data-col` / `data-fkey` / `data-key`, group and group label from the banner, label from the cell's text with `<br>` and entities resolved. **78 columns** — Symbol, Name and the four forward-returns columns are excluded (the anchors, and the as-of view's own). Restating the list anywhere else would drift the first time a column was added; adding a column needs no edit here.
- **Stored in `app_meta` under `hidden_columns`** as a JSON id array (`readHiddenColumns` / `writeHiddenColumns`) — a site setting, so deliberately not in `prefs`. `GET`/`PUT /api/columns` are admin; the PUT keeps only ids the catalogue knows, so it cannot rot or become free storage.
- **Delivered to every page in `GET /api/prefs`** as `siteHidden`, which the screener already awaits before its first render — a second request would paint the full table and then visibly drop columns. Guests get it too.
- **Enforced in `applyView()` for Standard, views AND screens**: the `#viewStyle` nth-child rules are now built from `siteShows(c.id) && (!view || view.columns.includes(c.id))`, so a view or a screen cannot bring back what the site hid. Banners span what is actually shown; `hideCollapsedCells()` hides a group with nothing left. The view EDITOR does not offer a hidden column — listing one would look like the tick did nothing.
- Nothing is deleted: the data keeps being recorded, and unticking restores the column. An open screener tab keeps the old set until it reloads, which the admin note says.
- Verified end to end against the real server and pages in headless Chrome (20 checks — catalogue size and labels, unknown ids dropped, malformed PUT refused, `siteHidden` on prefs, the admin list/count/search/group tick, the column gone from header and body, a fully hidden group losing its banner, the neighbouring banner's span, a view unable to show a hidden column, the editor not offering one), plus the 42-check column suite re-run to prove the nothing-hidden case is unchanged.

### Tiles — the same rows, drawn as cards
**A Table / Tiles toggle beside View (2026-09-16).** A short selection — a portfolio, a screen, a filter — leaves two-thirds of the screen empty as a table; the owner asked for a nicer shape for exactly that case (the Mag 7 view, 7 rows). **One selection, two shapes**: `render()` builds the same `sorted` rows and then either `paintWindow()` or `paintTiles()`, so the sort, the portfolio, the sector/industry/advice pickers, the Changed chip, the screens and every column filter apply identically. `body.tiling` hides the table bezel and shows `#tileWrap`.

- **A tile shows**: symbol, display name, price and today's move, the 90-day sparkline (the SAME `sparkCache` markup the table's column uses — not a second drawing), the Balanced verdict with its fired rule and the trend word, four returns (1W / 1M / 1Y / 5Y) and chips for Overall / Mom / Qual and the sector. Nothing is re-derived: every value comes off the row the table would have drawn.
- **The whole tile is one link** to `/stock/<SYMBOL>` in a new tab, so tabbing moves tile to tile and Enter opens — no mouse-only targets.
- **A return of 100% or more drops its decimal** (`+850%`, not `+849.7%`), and **the name line is omitted when the display name has collapsed to the ticker**, which would otherwise print the symbol twice.
- **Tile setup is one SITE-WIDE setting, admin-only** (2026-09-16, owner's call — the tiles are what gets shared on social media, so "a member choosing their own would make the same screenshot mean different things"). `Tile setup` appears beside the toggle for the owner in tiles mode and opens a dialog: the **chart period** (none / 1M / 3M / 90d / 6M / 1Y), five toggles (verdict, the rule that fired, trend state, score chips, sector chip) and **up to 6 detail fields** (8 at first; six reads as a tile, eight reads as a table cell). Stored in `app_meta` under `tile_config` (`readTileConfig` / `writeTileConfig`), delivered to every page in `GET /api/prefs` beside `siteHidden` so the first render has it, cached a minute server-side.
- **The dialog IS the view editor's** — same grouped, searchable, three-column field list with the group colours and the count beside the search box, at the same 640px. The `.vd-*` styles were scoped to `#viewDialog`; they are shared now, **with a `dialog.modal` prefix on the label rules** because `dialog.modal label` (small caps, block) is more specific than a bare class and was winning — the first attempt rendered every field name as an uppercase caption above a floating checkbox. Both dialogs were screenshotted to check.
- **The fields are rowcard's own** — `RowCard.fieldCatalogue()` and `fieldValues()`, added for this, list FIELD_SPEC as `group|label` keys and return one row's values under the same keys. So a tile field is formatted exactly as the hover card formats it, and adding a field to FIELD_SPEC offers it here with no further edit. The server shape-checks the key (`TILE_FIELD_RE`) against the known groups and caps the list; the browser ignores a key it no longer knows.
- **Two escaping traps, both caught by tests.** `TILE_FIELD_RE` was built with `new RegExp(\`...\${groups}\|...\`)`, and inside a template literal `\|` collapses to a bare pipe — the pattern became "one of the groups, OR anything at all" and stored a field key of `evil|drop table`. It is a literal now. And the dialog **repainted its whole list on every tick**, which dropped the checkbox under the cursor and most of a bulk change; it updates the count in place instead, disabling the unticked boxes at the cap.
- **The tile's chart is drawn for the tile, at a chosen height** — Short 44px / Medium 72 / Tall 110, beside the period in the setup. `sparkSVG(closes, {w, h, pad, area})` takes a size now: **the viewBox height IS the pixel height**, so a tall chart is a taller drawing rather than the table's 32-unit box stretched, which thickens the stroke unevenly under `preserveAspectRatio="none"`. `opts.area` closes the path to the floor for a soft fill, and the tile's line is **coloured by its own period's move** — green up, red down — where the table's column stays deliberately neutral: a tile is a picture of one stock and the line is the biggest thing on it.
- **The tile's chart is fetched separately whenever it is not 90 days** (`tileSparkCache`), since the table's 90d column owns that cache. **`/api/sparklines` is now cached server-side per (days, guest) for 10 minutes** — a year of closes is ~68,000 bar rows and every signed-in page load was asking for its own copy.
- **A `Per row` control sits beside the toggle** (2026-09-16, shown only in tiles): Auto, or a fixed 2-6. Auto is `auto-fill minmax(290px, 1fr)`; a fixed count is `repeat(N, minmax(0, 1fr))` — a width floor there would wrap the row at the first long name instead of honouring the count. **Below 1100px the window decides regardless**, so six across on a monitor is not six across on a laptop. Saved as `prefs.tileCols`.
- **Capped at 60 with a More button**: a tile is markup and 271 of them is not free. The windowed table's own budget is untouched.
- **Saved per account** (`prefs.layout`, `'tiles'` only — table is the default and stores nothing), so it opens the way it was left; the PUT whitelist takes the one value.
- Verified in a browser over a 7-stock portfolio: 16 checks — the toggle, table hidden / tiles shown and back, one tile per row, the fields on a tile, the new-tab link, the 100% rounding, the name-line rule, tiles following the table's sort, the preference saved and re-read on the next load, the empty state, no console errors. The column-structure and changed-chip suites were re-run unchanged.

### The stock card — one renderer, two surfaces
**`RowCard.stockCard()` draws the card the screener's Tiles view shows and the card the phone page shows (2026-09-16).** The owner asked for "each stock tile to look like these", holding up the Apple Stocks and Google Finance cards, and chose one renderer for both rather than a second implementation: the quote on top, a chart with real gridlines and price labels, then a small grid of whichever fields the owner picked.

- **The contract is already-formatted data**, not a snapshot row: `{symbol, name, price, change, up, fields:[{k,t,c}], closes, from, to, rangeLabel, verdict, chips}`. The desktop hands it `RowCard.fieldValues()` output, the server hands it what `mobileRow()` already built — so the function knows how to lay a card out and nothing else, and it renders identically in Node and the browser. Styles are `.sc-*` in **app.css**, for the same reason the markup is in one function.
- **Gridlines come from `chartSVG(closes, {ticks, area})`**, not a third chart implementation. Two things were added to it: `area`, a tint drawn as its OWN path (never a `fill` on `.ln`, which closes the line back to its start and paints a wedge), and **`tick.drawn`** — it already skipped a gridline outside the price band but still returned the tick, so a caller labelling every tick hung a price beside nothing. On a card that label landed on top of the header; `stock.html` had the same latent bug and now filters the same way.
- **Price labels are HTML in a 36px gutter**, positioned by each tick's viewBox fraction. HTML because `preserveAspectRatio="none"` would distort SVG text; a gutter because a label over the plot has the line running under it.
- **The x axis is the window the chart actually drew** — that symbol's own first and last session, not a shared calendar. Symbols do not share one (Samsung misses US sessions, a recent listing starts late), so one date axis for everybody would mislabel some of them. `readCloseSeries()` returns the dates the query was already selecting and discarding; `sparkSeries()` trims to the range and takes the two ends **after** the trim, or a 3-month card would be labelled with the date its wider query happened to reach back to. The rest of the date list is dropped server-side — only the two ends are ever shown.
- **The middle of that axis carries the move over the window**, named ("3 months +22.5%"), because the header's change is today's and two unlabelled percentages on one card is a trap.
- **`CARD_RANGES` is 1W/1M/3M/6M/1Y and lives in server.js**, delivered to every page on `/api/prefs` and to the phone on `/api/m/config`, so neither surface restates it. **No 1D**: the archive is daily bars, so a single session is a single point. **No 2Y**: a year is 253 sessions ≈ 68k rows a fetch, and Turso meters rows read.
- **One range control for the whole view, not one per card** (owner's call): every chart on screen then covers the same window, which is what makes a grid of them comparable and what makes it one fetch instead of one per card. On the screener it is a `Range` select beside `Per row`; on the phone a strip under the title, shown only where there are charts to change. Both remember the choice in `localStorage`. **The admin's Tile setup period is still the default**, snapped to the nearest offered window, and `spark: 0` there still means no chart at all — so "no chart" stays a setting rather than a sixth range.
- **The tiles cache the DATA now, not the markup** (`{closes, from, to}` per symbol): the card draws its own chart, so one series serves it whatever it does with it.
- Verified: 30 checks over the card and the ranges inside the mobile suite — the quote, the gridlines, labels clear of the plot and none reading "88.0", the axis naming its own dates and its own move, the fields grid, the thinning, the compressed cost, a shorter range redrawing every card with a first date inside the window, an unknown range falling back; on the screener, a tile being the same card, still one link to the stock, chips intact, the control appearing only in Tiles and moving every tile together, the table still aligned after; plus the stock page's axis (one label per gridline, all inside the chart).

### Column views
**Named column sets per account, switched from the `View` menu (the former Columns button), 2026-09-15.** The column half of what personal portfolios are for rows. **Standard** is the full grouped table and keeps the group toggles; a view shows Symbol, Name and exactly its own columns, **in the table's existing order (v1 — the owner chose this over drag-to-reorder)**.

- **Storage: `column_views`** (`id`, `scope`, `name`, `position`, `columns` JSON). `scope` is `'shared'` for **starter views** — seen by every account, edited only by the owner, shown with a lock to everyone else, with a `+` to copy one into your own — or the account key (email, else `'admin'`) for personal ones, up to `VIEWS_MAX` 10. Own table, not prefs, for the user_portfolios reason; `deleteUser()` purges them. The active view is `prefs.activeView` (`standard` or an 8-char id), validated against the loaded views on boot.
- **Starter views are seeded once**, on the first `GET /api/views`, guarded by an `app_meta` marker (`views_seeded`) so one the owner deletes does not come back. Three: Trend & momentum, Fundamentals, Advice — each leads with Overall, so the default sort is never hidden in them.
- **Routes**: `GET /api/views` (any signed-in; guests get shared only), `PUT /api/views/mine` (member), `PUT /api/views/shared` (admin). Whole-collection replace, rebuilt by `cleanViews()`: names trimmed, unique case-insensitively, columns deduped and **shape-checked only** (`VIEW_COL_RE`) — the server does not know the screener's column list, and the page ignores ids it does not recognise, so adding a column needs no server edit. An id that would shadow a shared view's is reissued.
- **Column ids** come from the row-2 headers: `data-col` (the 90d chart: `spark90`), else `data-fkey` (Portfolios), else `data-key`. `buildViewCols()` reads them once; forward returns are excluded (they follow the as-of mode, not a view).
- **Hiding is CSS, not per-cell work**: `applyView()` writes `nth-child` rules into `#viewStyle` for the header (index + 1), the body (index + 3 — Symbol and Name come first; `.padrow` and `.errrow` excluded, since those rows span) and the filter row (index + 3, built in body order). So the windowed repaint and the filter row need nothing new. Group banners get `colSpan` = the view's columns in that group; a group with none is hidden through `hideCollapsedCells()`, which in a view ignores the saved collapse state (except forward returns).
- **`filterHidden()` treats a column outside the view as hidden** (the dashed chip); **`#sortNote`** says "Sorted by X — not in this view" when the sort column is hidden.
- **The editor** is `#viewDialog`: name, search, every column under its group with a group tick, a count; it starts from what is on screen. Saves adopt the server's cleaned answer and switch to the saved view.
- Verified: headlessly as member (48 checks), owner (48) and guest (32) against a stub — exact columns shown, header-over-cell alignment for every column, banners spanning exactly the visible columns, filter row aligned, deep scroll, sort note, hidden-filter chip, create / validate / edit / copy / delete, owner editing a starter view, Standard's group collapse unchanged; the real server locally — seeding, guest 403s on both PUTs, anon 401, `cleanViews` on junk (bad id, duplicate and unsafe columns, duplicate/empty names, empty column list, shadowing id), and the owner's list restored after.

### The filter row
**A filter box under every column, behind the `Filter` button in the bar (2026-09-15)** — built for a 1,000-symbol universe, and only possible because rendering is windowed (see `paintWindow`): a keystroke re-sorts and repaints ~40 rows, not 1,000.

- **Built once from the header, not hand-written.** `buildFilterRow()` walks row 1: a `rowspan` cell is one column, a group banner stands for the row-2 cells carrying its `grp-<id>` class (by class, not `colspan`, because `applyAdviceCols()` rewrites the Advice banner's span). **Every filter cell copies its header's classes**, which is the whole trick: `hideCollapsedCells()`, the `body.hide-av-*` rules and the frozen `.frz` offsets treat it like any other cell in that column with no new code. A header cell with no `data-key` gets an empty filter cell; Portfolios opts in with `data-fkey`. Adding a column therefore needs nothing here unless it wants a non-numeric filter.
- **A third sticky header row**, `top: calc(--h1 + --h2)`; `updateStickyOffsets()` measures both. `size=1` and `min-width: 0` on the inputs are load-bearing — measured, the table width does not move by a pixel when the row opens.
- **Filters read what the cell SHOWS** (`filterValue()`): the 1-10 rating rather than the score behind it, days to earnings rather than a date, the MA cross as its word, name as short + legal, portfolios as shared + personal. Kind comes from the key: `TEXT_KEYS`, `CAT_KEYS` + `av:*` (dropdowns ordered by the ActionRules ladders, with counts over the tab), `BOUND_KEYS` (sector/industry — **they write `sectorFilter`/`industryFilter`, the bar pickers' own state**, so the two can never disagree and they are not counted as column filters), everything else numeric.
- **Grammar**: bare number = at least; `> >= < <= = !=`; `a..b`, `..b`, `a..`; K/M/B/T; `%`, `$`, commas ignored; `=` matches at the precision typed (`=61` finds 61.2). **Blanks never match a number filter in either direction.** Text: contains · `=exact` · `!not` · `a|b`. Input that does not parse is marked red and **ignored rather than emptying the table**.
- **AND across columns. Filters are not saved** — a filter surviving a reload is a table missing rows for no visible reason — but **whether the row is open is** (`prefs.filterRow`, boolean-only in the PUT whitelist). `/analysis` now hands back the whole prefs row it read; it used to drop `advices`, a pre-existing instance of the hand-back rule.
- **A filter on a column that is then hidden keeps applying**, and `#fltBar` (chips with ×, `N of M rows`, Clear filters, and Show filter row when it is closed) marks it dashed "hidden column". The bar is redrawn from `applyAdviceCols()`, which every render and every column toggle reaches — the first cut drew it from `render()` only, and collapsing a group left it stale.
- **A filter change scrolls to the top**, and `paintWindow()` clamps its first row so a list that got shorter can never open on the blank spacer.
- Verified by a headless harness on the real page (400 synthetic rows): 58 filter cells aligned to 58 body cells by group, grammar units, counts against independent filters over the payload, AND, hidden-column persistence, bound sector, Escape, empty state, deep-scroll-then-narrow, clear, sort still working, prefs PUT carrying `filterRow`.

### Turso meters ROWS READ — the anchor query was the lesson
**Turso sent a 75%-of-quota warning hours after the archive was deepened to 1.08M rows (2026-09-15).** The cause was `closesBefore()`, which the new 5Y column calls **on every refresh round**: it joined against `select symbol, max(d) from bars where d < ? group by symbol`, and at a five-year boundary that reads **748,859 rows** — about 35 times a day across the nightly rounds and the intraday runs, so tens of millions of rows a day for 254 numbers. Rewritten to seek per symbol on the `(symbol, d)` primary key, batched and chunked at 250: **254 rows and 80ms, against 748,859 rows and 231ms**. Same answers, verified against the old query.

**Two more of the same shape were found and fixed in the same hour**, both on the refresh path, both reading the whole bars table every round: `barsMaxDates()` (`select symbol, max(d), count(*) ... group by symbol` — 1.08M rows, 133ms; now one `order by d desc limit 1` seek per symbol, 271 rows, 57ms, and the `count` it also returned was never read by anything) and `barsOn()` for the split probe (`where d in (...)`, which the `(symbol, d)` primary key cannot seek on a date alone — 1.08M rows, 261ms; now one seek per (symbol, date) pair, 271 rows, 62ms). **A refresh round went from ~2.2M rows read to ~800.**

**Being over quota SLOWS the database, it does not only bill for it.** The same evening a plain refresh went 15s → 143s → 187s with identical API usage (272 credits, no profiles), and came back to **25-39s** once the plan was upgraded and the scans were gone. So an unexplained, uniform slowdown on every database call is worth checking against the Turso usage page before it is debugged as a code problem.

**`phaseTimer()` exists because that was invisible.** Every round now logs and returns `phases` on the payload — `profiles 0.1s · prices 3.0s · score 0.5s · trend-bars 5.4s · persist-bars 2.4s` — since Vercel's log view does not show a function's stdout. **`trend-bars` is the one to watch**: it is `readBarsFor(universe, 650 days)`, 118,671 rows at 271 symbols and ~440k at 1,000.

**The usage itself is on `/database` now** — a tile above the tables showing rows read, rows written and storage for the billing period, from **Turso's own platform API** (`GET /api/turso-usage`, admin, cached an hour, `?fresh=1` to force). It has to come from Turso: the client reports rows RETURNED, and for a scan that is a tiny fraction of what is billed. Needs `TURSO_API_TOKEN` (a platform token from Settings → API Tokens, **not** the database auth token) and `TURSO_ORG`; the optional `TURSO_ROWS_READ_LIMIT`, `TURSO_ROWS_WRITTEN_LIMIT` and `TURSO_STORAGE_LIMIT_GB` draw the percentage bars, since the API does not report the plan's allowance. Unconfigured, the tile explains what to set. **The daily watchdog cron checks it too** and mails once the worst of the three passes `TURSO_ALERT_AT` (0.7) — the same pass that watches for a missed nightly run, because both are "only worth hearing about when wrong". A Turso outage never breaks the page or the watchdog. Verified with the platform call stubbed: 11 checks (numbers, limits, the hour cache, `fresh=1`, a 500 handled, the watchdog still answering, the tile's text, GB formatting, the amber bar past the threshold, the bar's width, no page errors).

**`query-plan-test.js` now enforces the rule** (2026-09-15): it lifts `SCHEMA` + `ADDED_COLUMNS` out of db.js into an in-memory `node:sqlite`, pulls every SQL literal out of the same file, runs `explain query plan` on each, and fails if anything plans as `SCAN` over a big table (`bars`, `fundamentals_history`, `earnings_history`, `news`, `activity`, `visitors`, `snapshot`). **No network, no data, under a second** — run it after touching db.js. Deliberate scans live in `ALLOWED` with the reason written beside them (the `/database` counts, whole-collection reads of small tables, the two log tails — which say SCAN but walk the primary key backwards to a LIMIT — and the pruned `activity` / `news` rollups). Proved to work by re-introducing the offending `group by` and watching it fail.

**It found three things on its first run.** Two queries got an index — `news (published_at)` for the ticker's 12-hour read and `fundamentals_history (d)` for the report's day-pair lookup — and, more seriously, **the `news` and `news_state` tables had no `create table` in the schema at all**: the momentum cleanup (`ca2efed`) deleted them, production already had them so nothing broke, and a fresh database would have failed every news write. Restored verbatim from the live `sqlite_master`. **The test also enforces statement ORDER**, since an index that precedes its table throws at boot.

**The rule this leaves: on this database, a `group by` over a whole table is a quota event, not a slow query.** Prefer N indexed seeks — they are cheaper in rows AND in time. Measured read sizes to budget against (271 symbols, 1.08M bars):

| path | rows read | when |
|---|---|---|
| a refresh round's 650-day window (`readBarsFullFor`) | **118,671** | every round — the dominant cost |
| sparklines (`readCloses`, ~90 sessions) | 25,914 | every screener load, after the table paints |
| the 5Y anchor, before / after | 748,859 → **254** | every round |
| `/database`'s counts | 1.08M+ | that page only, cached 5 min; `?fresh=1` always counts |

At 1,000 symbols the 650-day window becomes ~440k rows a round, which is what to design against next (a shorter window for intraday rounds, or fewer intraday runs).

### The 5Y column
**Long-term gained a 5Y column after 1Y (2026-09-15, shown to everyone).** It is the one return column that does NOT come from the refresh window: a refresh fetches ~300 bars and an archive round reads 650 days, so `pctChange(values, 1260)` would be null for every stock. Instead `computeStocks` takes **one `closesBefore()` query for the whole universe** — the last close before today minus five years, the same anchor mechanism the promo studio's week/month windows use — and divides today's price by it. An anchor more than **30 days** earlier than the boundary is refused, so a hole in the bars cannot quietly stretch the window; a stock with no bar that far back stays blank. A failed query logs and leaves the column empty, the bars rule.

**It is blank for most of the universe until the archive is deepened**: measured 2026-09-15, **82 of 271 symbols have five years of bars** (the 2021 deep backfill covered the universe as it was then), 182 have one to two years — those arrived through refreshes, which keep ~300 bars. `backfill-bars.js --commit --depth 1300` fills the rest at **1 credit a symbol**; the cost is rows written to Turso, not credits.

### Sparklines
The **Chart** group is one column (`90d`) between Scores and Short-term, holding a 90-session price line per row. A group of its own rather than a column inside Info, because the columns menu toggles *groups* — inside Info it could only be hidden by hiding Price, Sector and Market Cap too. `data-group="vol" colspan="1"` was already the precedent for a one-column group.

- **`RowCard.sparkSVG()` is separate from `chartSVG()`**, not a flag on it. At 63×20px there is no room for a baseline, an end dot or padding, and a function that draws "everything except" is harder to follow than two small ones.
- **The line is neutral (`--muted`), not green/red.** The five percentage columns immediately to its right are already coloured; a sixth coloured element there is noise. The shape carries the information, colour is left to the numbers. It brightens to `--text` on row hover.
- **`/api/sparklines` is one query for the whole universe** (`readCloses`), ~41 KB, fetched *after* the table paints — a decoration must not make the data wait. Each line is scaled to its own high and low, so the shape is readable but levels are not comparable between rows; the column header says so.
- **The generated markup is cached per symbol.** The table rebuilds every row on each sort and filter, and regenerating 69 paths each time would make sorting feel heavy. Measured at 81ms per sort with all 69 intact afterwards.
- Row height is unchanged at 37px — a 20px line fits inside the existing `padding: 7px` cells.

## Table specifics
- **Frozen columns are Symbol and Name only** (`.frz0`, `.frz1`). The classes are *positional*: `updateStickyOffsets()` measures header cell widths left-to-right to compute the cumulative `left` offsets, so reordering columns means re-dealing the `frz` numbers in DOM order, not just moving the markup. `.frz1` carries the boundary shadow.
- Two sticky header rows; `--h1` is measured at runtime because the second row's top offset depends on the first row's wrapped height.
- **Overall is centred (`ta-c`) and Mom. is left-aligned (`ta-l`);** everything else stays right-aligned. Those are alignment-only classes on purpose — `.left` also switches the cell to the sans face, and a rating is a number that belongs in the mono one. The sorted-column marker is `left: 50%`, so it is unaffected by either.
- The sorted column is marked by a small accent arrow absolutely positioned in the header's bottom padding — it's positioned, not inline, so it can't reflow a wrapped label.
- Score cells (`td.rating`) open the factor-breakdown tooltip (`#tip`, fixed-position glass card) on hover.

### The score tooltip
**`RowCard.scoreTip(stock, kind, opts)` builds it and `RowCard.placeTip()` positions it**; `#tip`'s styles live in `app.css`. Both were inline in `index.html` until the stock page needed to explain the same three numbers — a second copy of "why is this an 8" is exactly what `rowcard.js` exists to prevent. Only the markup is shared: each page keeps its own hover wiring, because one hovers table cells and the other a chip and a card row.

- `kind` is `overall` | `momentum` | `quality` | `rank`, and it returns **`null` when there is nothing to explain**, so a caller can skip showing rather than flash an empty card.
- **`rank` is not a factor breakdown** — a rank is a position in a sorted list, so the card explains the number it sorts on: the Overall score, its 65/35 split, and that the ordering runs across every stock in the screener.
- **`buildSections(s, { tips: true })`** marks the Overall/Mom./Qual. rows with `data-tip`. Off by default: inside the hover card those rows are already in a tooltip, and a tooltip on a tooltip helps nobody. Only `/stock/<SYMBOL>` passes it, where there is no table to hover and the scores would otherwise be four bare numbers.

## Fundamentals data — things that will bite you
- **A refused profile pull must never overwrite a good one.** `fetchProfile()` returns every field as `null` when a call fails, and `ensureProfiles()` used to store that verbatim with a fresh `fetchedAt` — so a rate-limited round blanked market cap, every fundamental and every size column, and the row then looked fresh enough to keep it blanked for a day. Observed live: twelve symbols, CRWD and Samsung among them, lost their fundamentals during a burst of refreshes. `fetchProfile()` now reports `fetchOk`, and a failed pull keeps the cached values and leaves `fetched_at` at `0` so the next round retries.
  - **The failure test is exact, not a guess.** Twelve Data answers a refused call with `{ status: 'error' }`; an instrument that genuinely has no fundamentals — an ETF — answers with a real `statistics` object whose `market_capitalization` is `0`. Only the first is a failure, which is why ETFs do not retry forever.
  - **`/statistics` costs 50 credits, not 1.** Measured off `Api-Credits-Used` in Sep 2026. Six cold profiles is ~300 credits against a 610/minute ceiling, which is why two refresh rounds inside a minute are refused.
  - Seven symbols legitimately have no market cap — all ETFs, DRAM (the Roundhill Memory ETF) included. Funds report AUM, not capitalisation.

- **All three margins are derived, not taken from the API.** `financials.gross_margin` does *not* equal `gross_profit_ttm / revenue_ttm` — it's computed on a different basis and differs by up to ~4 points (Samsung: 61.2% vs 57.5%). `profit_margin` is worse: it agrees with `net_income / revenue` for 42 of 44 tickers and is flatly wrong for the other two (JOBY +45.1% vs −755.1%, MSTR +68.7% vs −6294.2% — both loss-makers with small revenue). `fetchProfile()` derives `grossMargin`, `fcfMargin` **and `profitMargin`** from the absolutes the table displays, so dividing the two columns on screen gives the number shown.
- **Absolute columns are in the company's reporting currency.** Samsung's revenue is ₩485T. Pass `s.currency` to `fmtMktCap()` for every money column, and treat sorting on them as within-currency only — a KRW reporter tops any cross-currency sort.
- **ADR fundamentals can be incoherent.** SKHY returns net income (116.8B) larger than gross profit (104.0B), which is impossible, and its EBITDA appears to be in a different unit from its revenue. Don't assume a populated field is a correct one.

Forward revenue / EPS estimates (`/revenue_estimate`, `/earnings_estimate`, `/growth_estimates`) are **Ultra-plan only** — they 403 on the current Pro key. AAPL returns data for them because it's Twelve Data's free sample symbol, which makes it a misleading symbol to test plan access with. `/income_statement?period=quarterly` works but costs 100 credits/symbol (vs ~45 for `/statistics`) and is capped at 6 quarters, so trailing-twelve-month growth — which needs 8 — can't be computed on this plan.

## Scores / ratings
Three composite scores per stock (1–10): **Momentum** (price strength), **Quality** (company fundamentals), **Overall** (65% momentum + 35% quality). Computed in `computeScores()` in server.js.

**Momentum over time was retired on 2026-09-15.** The score above is live and unchanged; what went is the stored history, the Past Mom. and Mom. Delta columns and their picker, the direction arrow, the two momentum analysis screens, the stock page's momentum pane, the `/signal` study and the lab's slope indicator. Five framings on the delta all came back flat — the strongest **r = −0.003** against the next fortnight. The model, the findings and the one-command rebuild are in **[docs/momentum-delta.md](docs/momentum-delta.md)**, and the tag `momentum-retired` marks the last commit where all of it worked. **Do not rebuild any of it from memory; read that file first.** Advice never depended on it (`use_momentum: false` in every default profile), which is what made the removal safe.

### Momentum is absolute
Momentum is measured against a **fixed scale**, not against the rest of the list. A score therefore means the same thing in a weak quarter as a strong one, does not move when a ticker is added or removed, and is comparable with the same stock's score a year ago. `applyScores()` is now a plain loop over `computeScores()`; there is no cross-sectional pass and nothing depends on the universe. Adding a factor means one entry in `momComps`.

**It was cross-sectional until Sep 2026**, percentile-ranking six of the eight factors. That was dropped because a percentile median is 0.5 *by construction*: measured at six dates between 2011 and 2026 the ranked model's median score sat at **54 in every single period**, so it could not express a weak market at all — the best of a bad lot always scored near the top. It also meant adding a ticker nudged everyone's score, and made a momentum *history* meaningless, since the series was contaminated by changes in universe composition.

| factor | weight | notes |
|---|---|---|
| 12-1 momentum | 20 | 12-month return **skipping the last month**, risk-adjusted |
| 6M return | 18 | risk-adjusted |
| 3M return | 17 | risk-adjusted |
| % from 52W high | 10 | |
| Trend regime | 10 | 200D side + cross freshness; categorical, not ranked |
| Consistency | 10 | share of the last 12 months that closed up |
| 1M reversal | 8 | **inverted** — the biggest recent movers score lowest |
| RSI timing | 7 | non-monotonic, peaks at 70–75 |

Everything comes from the daily bars already fetched, so the extra factors cost **no API credits**. 12-1 and consistency need ~260 of the 300 bars `outputsize=300` returns.

**Why a logistic curve and not `lin()`.** The first absolute model used clamped straight lines and pinned **33–51% of stocks** at exactly a floor or ceiling on every major factor — a factor constant across half the list cannot order anything, and that is what sent the model to percentiles. `curve(v, centre, scale)` is `0.5 + 0.5·tanh((v − centre) / scale)`: asymptotic, so it approaches the ends without reaching them and ordering survives at the extremes. Measured after the change: **3.4% of sub-scores** land within 0.005 of an end, and that is an upper bound because the stored breakdown rounds to two decimals.

**The centres are measured, not guessed.** Each is the median of that factor across the bar archive at six dates spanning 2011–2026, with the scale roughly the interquartile spread — `mom121` 0.70/1.30, `ret6m` 0.55/0.90, `ret3m` 0.25/0.45, `fromHigh` −12/14, `consistency` 58/15, `revers1m` 1.0/10 inverted. The risk-adjusted returns are dimensionless (a return over its own volatility), which is what makes a fixed scale meaningful for them at all. **They are constants on purpose** — deriving them from the current universe would be percentiles under another name.

**Switching cost, measured before the change:** median order change **2 places**, worst 11; **no stock's 1–10 momentum rating moved by more than one point**, 48 of 82 unchanged. The list barely moved; what changed is that the numbers now mean something fixed.

**Four factors were removed, each for a measured reason:**
- **RS vs S&P** was `threeMonthPct` minus a constant identical for every stock — correlation with 3M return was exactly **1.000**, so it could not reorder anything while consuming a quarter of the weight. Ranking within the universe is already relative.
- **MACD** was binary (0.8/0.2), discarding magnitude; correlation with the composite was **0.022**.
- **Vol trend** was unsigned, so a crash on heavy volume scored like a breakout, and **51%** of the universe sat at its floor while none reached the ceiling. If it returns, sign it: `volTrend × sign(1M)`.
- **Short squeeze** rewarded heavy short interest, which predicts *weaker* returns; it correlated **−0.223** with the composite, pulling against everything else.

## The Excel model
**`GET /api/model?symbol=X`** serves it from the stock page, and `node --use-system-ca momentum-model.js CRWD` writes it locally. Both call `buildModel()` in the same file — verified byte-identical by SHA-256, so the workbook a reader downloads cannot drift from the one generated on the command line. The link is hidden when a symbol has no momentum score, because a link that downloads an error is worse than no link; the route answers 422 with the session count for anything under 274 bars.

The workbook: three sheets — **Bars**, **Factors** and **Score** — in which every cell is a live formula over the daily bars. Change a close price and the score moves, because the workbook is the calculation rather than a picture of it.

- **Verified against the app, not asserted.** The Score sheet holds what the app reported when the file was written and subtracts. Checked on CRWD: **72.1 against 72.1**, every factor agreeing within the 2dp the stored breakdown rounds to.
- **`values[k]` lives on row `D + 1 + k`.** Writing `D + k` reads the wrong session, and every return is a difference between two of these — it cost four wrong formulas the first time. The `B(k)` helper exists so it cannot happen again.
- **320 rows, mirroring the ~300 bars a refresh fetches.** The deepest factor needs 253, the moving averages 200 plus somewhere to look back for the last cross.
- **RSI is a recursive column**, seeded with a simple 14-day average at the *old* end and smoothed upward, because Wilder runs oldest-first while the sheet is newest-first.
- **Written by hand — an xlsx is a zip of XML.** A spreadsheet library would be a fourth dependency for a one-off explainer; the same reasoning that sends mail over plain `fetch`. Excel is told to `fullCalcOnLoad`, since no cached values are stored.
- **Generated per request, not cached.** ~35 KB and well under a second, and a cached copy would go stale on the next refresh and quietly disagree with the page it was downloaded from.
- **The output is gitignored.** It goes stale as soon as prices move, and regenerating takes a second.

## Everything the profile call returns is kept now (2026-09-15)
**Measured, not guessed: `/statistics` returns 58 fields and we were keeping 16.** The call is charged the same either way, so the other 42 were free and were being discarded — and a day not recorded is gone for good, since the API only ever returns today's numbers. The owner's call: store them, show them, and stop discarding the earnings quarters.

- **Added to the profile, the snapshot row, the screener and `fundamentals_history`** (24 numeric columns + `ex_div_date`): share count and float; total cash and total debt **separately** (netCash hid which side a company was on); debt/equity and current ratio; enterprise value, trailing P/E, P/B, P/S, EV/EBITDA; EBITDA, operating cash flow, operating margin, ROA, diluted EPS, book value per share; the dividend (forward yield, rate, payout, ex-date — the app had **nothing** on dividends, so every return in it is price-only); short ratio, short % of shares outstanding, insider % and institution %.
- **Deliberately still dropped**: beta, the 52-week high/low/change, the 50- and 200-day averages, the average volumes. The bar archive reproduces all of them exactly, and back to 2006.
- **The table grew by 21 columns**: Size 7 → 11 (Cash, Debt, EBITDA, Op Cash), Fundamentals 7 → 18, and a new **Ownership** group (id `own`, `#f0abfc`, last) with Shares Out, Float, Short Ratio, Short % Out, Insiders, Instit. `PAD_SPAN` 85, error row 80, empty row 82. Stored but not columns: enterprise value, book value per share, dividend rate, ex-dividend date (the hover card shows the last two).
- **`earnings_history` (symbol, d, eps_estimate, eps_actual, surprise, surprise_prc, reported)**. `/earnings` already returned 8 quarters for its 20 credits and everything but the latest surprise was thrown away. `fetchProfile` hangs the quarters on `out.earningsRows`, `ensureProfiles` strips that key before caching the profile (it is not a `PROFILE_FIELDS` field — putting it there would make every stored profile look like it had a gap) and writes **one batch per round**, upserting on `(symbol, d)`: a reported quarter does not change, so this is an append in practice. In `SYMBOL_TABLES`, so a dropped ticker takes it along. Nothing reads it yet — it exists for the PEAD study.
- **The refresh email did not get noisier.** Its moved-fields list (`FUND_MOVES`) is opt-in and separate from what is stored; six new entries were added (share count, total debt, EBITDA, operating cash flow, operating margin, ROA) and everything price-divided was left out for the reason the original six exclusions record.
- **Adding a field to `emptyProfile()` makes every stored profile a "fields" gap**, which is the designed behaviour (see Fill missing): the next nightly run fills gaps first, or Fill missing does it in ~40 min / ~22k credits at 271 stocks. Until then the new columns are blank.
- Verified: the real server on an in-memory node:sqlite — history round-trip for all 24 columns + the date, same-day upsert, earnings write/read/revise/purge (12 checks); and the real screener in headless Chrome (42 checks — every banner's colspan against its own cells, 85 headers = 85 body cells, each new column's rendered value, the filter row covering all 85 with the six new Ownership boxes, a filter on a new column, the hover card's new group and rows, no page errors).

## Fundamentals history
`fundamentals_history` keeps one row per symbol per day (18 columns — valuation, size, margins, growth, balance sheet), so the movement of a P/E or a margin can eventually be charted. **Empty until the next Refresh all** — nothing is backfillable, because `profiles` only ever holds the current value and no API on this plan returns historical forward estimates.

- **Written during a Refresh all only**, gated on `readRefreshState()` being non-null. An ordinary price Refresh reuses day-old cached profiles, so recording then would store identical numbers under a new date and invent movement that never happened.
- **One set per day**, enforced by the `(symbol, d)` primary key. A Refresh all calls this on each of its dozen-odd rounds, so the write is an upsert: later rounds carry more populated profiles and replace what earlier ones wrote.
- **Rows without a profile yet are skipped, not stored empty** — a later round in the same run fills them in.
- **Explicit columns, not a JSON blob** (unlike `profiles`): the shape is ours and stable, and a chart wants `select d, forward_pe` over a year rather than 365 blobs to parse. Adding a field is an `ALTER` in `ADDED_COLUMNS`.
- **The scores are deliberately absent.** They are model output rather than measurement — momentum has already been rewritten once, and a series mixing two scoring regimes compares nothing to nothing.
- **A history write never fails a refresh**, the same rule the bar archive follows.
- The series will be **irregular**: a point exists only for days a Refresh all was run, not every calendar day. Fine for a chart, awkward for precise period comparisons.

## The universe, and the ballast that has been draining out of it
**Every row count and symbol count in this file is a snapshot, and the universe is edited by hand.** Check against the database before relying on one: `node --use-system-ca analysis-db.js --stats`. The three derived files (`private/single-closes.json`, `private/strategy-*.json`, `private/lab-grid.json`) are built from a **local** copy, and `analysis-db.js` without `--full` syncs forward only — **it does not drop symbols that were purged upstream**, so after any ticker removal the rebuild sequence is `analysis-db.js --full`, then `single-data.js`, `strategy-runs.js`, `lab-grid.js`. Skipping the `--full` silently keeps dead tickers in every one of them.

**93 symbols, of which 14 sit in a portfolio called `Faded`** — BA, DIS, EL, ERIC, GE, HPQ, IBM, KSS, LUMN, PFE, PTON, SBUX, VFC, ZM. They are **deliberate ballast, not picks.**

> **It was 33 until 2026-09-10, and losing 19 of them measurably re-introduced the bias they exist to counter.** Removed entirely from the universe: T, VZ, WBD, NOK, XRX, CVS, TGT, DG, GPS, M, BBY, NKE, HAS, MMM, F, BMY, VTRS, PYPL, KHC. Measured before and after, on the same code:
>
> | | 116 symbols, 33 faders | 93 symbols, 14 faders |
> |---|---|---|
> | buy-and-hold CAGR | +18.6% | **+21.8%** |
> | buy-and-hold Sharpe | 0.95 | **1.03** |
> | best velocity \|t\| on the lab grid | 1.73 | **2.76** |
> | best RSI \|t\| on the lab grid | 1.76 | **2.73** |
>
> Nothing was discovered between those two columns. The indicators did not get better; the pool got easier. A t of 2.8 across 144 tests still is not a finding, but it is close enough to the conventional bar to be tempting, and that is exactly the trap the ballast was added to prevent. **If a result improves after the universe shrinks, suspect the universe.** Re-adding laggards is the cheapest way to sanity-check any new result — bars cost one credit per symbol at any depth.

Every backtest before they existed ran on 83 stocks chosen *because they are worth following in 2026*, so the whole archive was conditioned on having survived to that date — visible as a **+1.02% per fortnight baseline, about 30% a year**, which no universe returns. A ranking test needs losers in the pool or it only measures the selection.

- **29 of the original 33 had pre-2010 history**, which was the selection criterion that mattered: the pre-2020 window is where every result vanished, and a 2021 IPO cannot de-bias it. Candidates were validated against the API *before* being added — a dead symbol would sit in a portfolio erroring forever. WBA was rejected that way (taken private).
- **This is survivorship-*lite*, and it is now thinner than it was.** Every fader still trades. Companies that went bankrupt or were acquired are still absent, and those are the tail that matters most. Treat it as a large reduction in the bias, not its removal.
- **The 119 ceiling fell on 2026-09-13** — the price fetch is chunked (≤120 symbols per `time_series` call, SPY in the first chunk), so the batch cap no longer binds. The practical ceiling is now **~300 symbols** (the owner's stated target: 200–300), set by the refresh economics below and, further out, the single-payload/single-prompt architecture (chat measured viable to ~700). The growth caveat stands: **add losers as the universe grows** or every backtest number quietly improves for the wrong reason.
- **They cost the screener something**, and that was accepted: the All tab grew and a Refresh all needs more rounds (`MAX_ROUNDS=25` covers up to the API ceiling). Filter to a portfolio to get a smaller view back.
- **What adding them proved.** `from_high` had looked like the one factor with a consistent sign across the whole archive (1m long-short t −2.2 pre-2020). On the broadened universe it **fell to t −1.4 and lost half its magnitude** — it was universe-dependent, exactly the kind of result the hold-out discipline exists to catch. Meanwhile the post-2020 momentum effect *survived* de-biasing almost intact (mom121 top-decile t 3.3 before and after), which makes it more likely a regime than pure selection. The pre-2020 window stayed flat for everything (best t 0.9). Nothing yet clears a bar worth trading.

## The universe table — portfolios are groupings
**Since 2026-09-15 the stocks the screener tracks live in their own `universe` table (`symbol`, `added_at`); portfolios only group them.** Before, the universe was the union of the admin portfolios, so deleting a portfolio silently removed every stock only it held and purged their data — measured before the change: deleting Fin, Chips, Software and the industry-style lists would have taken ~150 of 271 stocks out. The owner wanted to delete those portfolios now that Sector and Industry cover them.

- **`store.readUniverse()` is the only source** — server (`readUniverse`), `purge-orphans.js`, `backfill-bars.js`. It returns the table **plus any portfolio member not in it** (belt and braces), ordered by `added_at`.
- **Filling it**: `init()` runs `insert or ignore into universe select distinct symbol from portfolio_tickers` on every cold start (idempotent, no marker — that is how the 271 arrived), and **`writePortfolios()` runs the same insert before it deletes the memberships**, so no portfolio write can drop a stock.
- **Routes**: deleting a portfolio and taking a ticker out of one never purge. `DELETE /api/tickers/:symbol` (the × in the All view) is the only way out: `removeFromUniverse()` deletes the universe row and every membership in one batch, then `purgeSymbol()`. `POST /api/universe {symbol, portfolio?}` adds a stock with an optional portfolio (the console's Add a ticker, "No portfolio — screener only" by default); adding to a portfolio still adds to the universe. Every portfolio answer (`portfolioAnswer()`) carries `{portfolios, universe}`, and the screener's `applyPortfolioMap(map, universe)` keeps a row for every universe stock.
- **Snapshot memberships are stamped on the way out** (`membershipOf` per row, `snap.portfolios` from the table) — a deleted portfolio would otherwise linger on every row until the next refresh.
- Verified against the real server on an in-memory node:sqlite in place of Turso (27 checks: delete a portfolio keeps stocks and data, remove-from-portfolio keeps, remove-from-screener purges and removes memberships and is not brought back by a later write, the add route's 409/404/400s, snapshot stamping) and the screener's reconciliation (5 checks).

## Dropping a symbol
**A ticker removed from the screener (the All view's ×) takes its data with it** — `bars`, `fundamentals_history`, `profiles`, `names` and headlines, via `purgeSymbol()`. *Before 2026-09-15 this happened when a ticker left its last portfolio; see the universe table above.* `purge-orphans.js` sweeps up anything left, comparing against the universe.

- **Two of those come back, one does not.** Bars cost a single API credit to re-pull at any depth and momentum is recomputed from bars, but **`fundamentals_history` cannot be rebuilt** — the API only ever returns today's numbers, so a deleted row is gone and re-adding the ticker starts that series from zero. Every surface says so: the confirm dialog, the dry run, and the comment on `purgeSymbol()`.
- **The decision is made from the universe before and after the edit, not from the route.** A symbol still in another portfolio is untouched — verified: removing AAPL from one of two lists purged nothing, deleting the last portfolio holding SPY removed all 9,739 of its rows.
- **`snapshot` is deliberately not in `SYMBOL_TABLES`.** It is one JSON row rewritten wholesale on the next refresh, so it heals itself.
- **A failed purge never fails the edit.** Losing the portfolio change because the cleanup broke would be worse than leaving rows behind, and the sweep exists to collect them.
- **The UI confirms only when data will actually be destroyed.** `lastHome()` asks whether this removal leaves the symbol in no portfolio, so removing it from one of several still passes without a dialog — that really is trivially undone. Deleting a portfolio names the symbols that will lose their history. Both then report the row count through `setStatus()`, because a silent ten-thousand-row delete is not something to discover later.
- Swept on introduction: **SPY 9,739 rows, XLF 5,009, BTC 1** — the database now holds data for exactly the 83 symbols in the portfolios.

## Editing portfolios does not pull
**Adding or removing a ticker, and creating, renaming or deleting a portfolio, no longer refresh from the API.** Every one of them used to call `refresh()`, which sets `?refresh=1` and recomputes the entire universe from Twelve Data — roughly **560 credits and a ten-second wait** to reflect a change the server had already made and had already told the client about. Refresh is a button; it stays one.

- **`applyPortfolioMap(map)` reconciles the table from the response.** All five endpoints answer with the fresh `name -> symbols` map, and membership, badges, tab counts, the portfolio picker and the purge warning all derive from it. Only the price and fundamental columns need a pull.
- **A symbol with no row yet gets a placeholder**, reusing the `s.error` branch the row renderer already has: `No data yet — click ↻ Refresh to pull it.` It is in the portfolio, so it belongs in the table and in the count — silently not appearing looks like the add failed. `applyLens()` already skips error rows, so nothing downstream needed changing.
- **A symbol left in no portfolio loses its row**, because the server has just purged its stored data.
- **`activeTab` falls back to `All`** when the portfolio it names stops existing. `renamePortfolio()` sets the new name *before* reconciling, so a rename keeps you where you were.
- **The add route returns the company name it resolves** (1 credit, cached, and it was already being fetched). That lookup is now the only thing that touches the symbol before the next Refresh, so a name coming back empty is the earliest hint of a typo and the page says so. Without it a mistyped ticker would sit as a placeholder until the next refresh turned it into an error row.
- **The as-of date still pulls**, both setting and clearing it — a forward-returns view is computed live by definition.
- Verified against the extracted function with eight cases: a new ticker, a ticker already in the universe added to a second portfolio, removal from one of two holders, removal of the last holder, rename, delete with one orphan and one survivor, a null map, and a placeholder being replaced by real data.

## Saved column layout
Which column groups a user has collapsed is stored per account in the `prefs` table (`user_key`, JSON `data`), read by `GET /api/prefs` and written by `PUT /api/prefs`.

- **Server-side, not `localStorage`.** The choice belongs to the person, so it follows them to another browser or machine rather than belonging to a device and being shared by whoever sits at it.
- **Keyed on the signed-in email**, falling back to `'admin'` for the legacy password cookie and for open mode, where there is no user row — the same convention `chat_usage` uses.
- **`fwd` is never persisted.** It is derived state: `refresh()` sets it from whether an as-of date is set, so saving it would only store a value the next load overwrites.
- **Prefs are awaited before the first render**, in the boot chain `checkAuth().then(loadPrefs).then(refresh)`. Applying them afterwards would paint the default layout and then visibly rearrange it.
- **Writes are debounced 600ms** — the columns menu stays open while toggling, so changes arrive in bursts — and are suppressed until the load lands, or the defaults would be written back over the values being fetched.
- **The server stores only what it understands.** `PUT` rebuilds the `collapsed` map from scratch against `/^[a-z]{1,16}$/`, so the endpoint cannot be used as free per-user storage and `__proto__` cannot get in.

## Member portfolios
**A member's own lists — named FILTERS over the shared snapshot, never new data (2026-09-14).** The load-bearing invariant, enforced server-side: `PUT /api/my/portfolios` validates every symbol against the live universe and silently drops the rest, so a member can never add a stock to the system. Everything heavy (bars, profiles, momentum, refreshes, the snapshot, chat) is untouched — a personal portfolio costs nothing.

- **Storage: `user_portfolios`** (`user_key`, `name`, `position`, `symbols` as a JSON array per row) — deliberately NOT in `prefs`: three pages debounce-write prefs under the hand-back rule, and a page bug there must not be able to wipe someone's portfolios. Keyed on email (else `'admin'`, the prefs convention); `deleteUser()` purges them; `purgeSymbol()` sweeps a dropped ticker out of every member list and reads filter to the universe anyway (belt and braces). Caps: 10 portfolios, 40-char names, whole-collection replace rebuilt from scratch on write (the prefs-PUT rule).
- **Routes**: `GET`/`PUT /api/my/portfolios`, both `requireMember` — guests 403 and have no key. The PUT logs the **diff** to the activity log (`myportfolio` kind: `create:Name +2`, `Name +NVDA -MU`, `delete:Name`), capped at 10 rows per save, never the blob.
- **UI (screener)**: personal tabs live under the **`my:` prefix** in `activeTab` so names can never collide with shared portfolios (`tabExists()` guards the three fallback sites). The portfolio picker grows a personal section — green tint, each row with rename/delete as `.pick-row` siblings (a button cannot nest in a button) — plus "New personal portfolio". Membership is toggled from the **star on each row** (`.mypf` in the symbol cell, hidden for guests), which opens the `#myPop` fixed popover: tick portfolios in and out, menu stays open while toggling (the Rules-menu pattern), saves debounced 600ms and **adopts the server's cleaned answer** so the UI cannot drift from what was stored. Personal memberships render as **dashed badges** (`.badge.mine`) beside the shared solid ones. The ⋯ menu's Rename/Delete stay shared-only (`isMyTab` guard); loaded in the boot chain beside prefs, before the first render.
- **Admin review**: `/api/users` attaches each account's portfolios (name + symbols, shown as a summary cell with the full lists in the tooltip on `/users`), plus `otherPortfolios` for keys with no account row (the legacy `'admin'` key), so nothing escapes review. The activity log carries the edit stream.
- Verified on introduction: junk/duplicate/lowercase symbols cleaned, the 10-portfolio cap, guest 403s, diff rows in the activity log, the review shape, and the purge sweep (2 rows edited, symbol gone, other symbols kept).

## The refresh budget — pacing and rotation
**Two changes on 2026-09-14 took the nightly job from "breaks at 530 symbols" to "1,000 in 24 minutes", on the same Pro plan.** Both were prompted by costing an upgrade and finding it was not worth buying: 987 credits/minute is a slider inside Pro at $149 — no `/market_cap`, no forward estimates, no analyst data, all of which need Ultra at $999 — and it would have bought **nine minutes on an unattended midnight job**, while removing not one required fix. 1,001 price credits do not fit a 987 ceiling either.

**Prices are paced across rounds.** They cost 1 credit a symbol and the whole universe was pulled in one round, so above **529 symbols** the round asked for more than the minute allows and was refused — and a Refresh All could never get past its first round. A price round now takes the next slice that fits, anything outside it is read from the archive so the snapshot stays complete, and only the round reaching the last symbol stamps `prices_at`. `refresh_state.priced` is how a stateless function knows where it got to; `notePriceRound()` is the one place that advances it, so the cron route and `/api/stocks` cannot drift. Only freshly-priced symbols go to `persistBars` — re-persisting archive-sourced bars is a no-op upsert of the overlap window for every unpriced symbol, every round.

**Fundamentals rotate instead of being re-pulled nightly.** `PROFILE_TTL_MS` is now `FUND_ROTATION_DAYS` (env, default **7**) rather than 24 hours, and the cron's `?start=1` expires the **oldest** `ceil(N/7)` profiles (`expireOldestProfiles`) instead of all of them. The justification is measured and already in this file: the twelve tracked fundamental fields move on **1-5% of nights**, and when they move it is a step, not a wobble. A symbol with no profile at all is never waiting its turn — `ensureProfiles` treats a missing one as stale. `?full=1` on the cron, and the console's **Refresh all**, still sweep everything.

| universe | nightly before | nightly now | credits before → now |
|---|---|---|---|
| 270 (today) | 39 rounds, 40 min | **6 rounds, 6 min** | 21,909 → **3,396** |
| 500 | 73 rounds, 75 min | 12 rounds, 12 min | 40,573 → 6,272 |
| 1,000 | *impossible* | **23 rounds, 24 min** | 81,145 → 12,463 |

- **`MAX_ROUNDS=60` and `timeout-minutes: 45` did not need raising** — 23 rounds and 24 minutes at 1,000 symbols sit inside both. They would bind on a full sweep, which the nightly job no longer runs.
- **A plain Refresh above ~609 symbols prices what fits and leaves the rest on their archived close.** Honest but partial; Refresh All is what walks the whole list.
- Verified by simulating whole runs against the real `liveRefreshOpts()` at nine universe sizes from 93 to 1,500: **no round anywhere exceeds 610 credits and every profile is still pulled**. The load-bearing half of that is the bottom — 93 and 270 reproduce today's 14 and 39 rounds exactly, because below 529 the slice is the whole universe and the new path is never taken. The rotation SQL is unit-checked against a throwaway in-memory SQLite: oldest-first, the limit respected, never-fetched rows not counted against it.
- **What none of this fixes**, and what will bind next: the assistant's ~700-stock context wall, and `readBarsFor`'s 650-day window across the universe (3.5s at 85 symbols, so ~41s at 1,000) on every round.

## Fast refresh — the same sweep without the per-round rebuild
**A Refresh all re-pulls every profile AND rebuilds the whole table on every round** — a 650-day window of bars for the entire universe, rescored, advice re-run, snapshot rewritten — to fold in the seven profiles that round fetched. Measured 2026-09-16 at 271 stocks: **40 rounds, 72.4 minutes, 21,837 credits**, of which 62s a round is the deliberate wait and **~46s is that rebuild**. **Fast refresh (2026-09-16) keeps the sweep and drops the repetition**: its rounds fetch profiles and nothing else, and the table is rebuilt **once, at the end**.

- **Why it matters at 1,000 stocks**, which the owner is heading for: the round count is fixed by the API (80 credits a profile against 610 a minute = 7 a minute = 143 rounds ≈ 2.4 hours, which nothing can beat), but the per-round work is not. Estimated at 1,000: **Refresh all 5-7 hours and ~63M rows read; Fast refresh ~2.5 hours and under 1M**. A five-hour run driven from a browser tab does not finish — two runs were already `abandoned` at 134/271 on 2026-09-16.
- **`GET /api/refresh-profiles`** is the round: `ensureProfiles(universe, 7)`, `noteRefreshProgress`, `recordRound`, and a stopped-run check — no bars read, nothing scored, no snapshot written. It **deliberately does not close the run** when coverage completes; the rebuild does.
- **The rebuild is the ordinary round.** `RefreshLoop.run('fast', …)` calls `/api/refresh-profiles` until every stock has a profile, then **one** `/api/stocks?refresh=1&run=…`, which is where scores, advice, the snapshot, `fundamentals_history`, the report and the email all come from — the same ending a Refresh all has, so none of that is duplicated. The hook fires `onWait('rebuild')` so the page can say what is happening.
- **Stopping loses nothing**: the profiles fetched so far are stored, and the next refresh of any kind picks them up. A stopped or stalled run does NOT rebuild.
- **The trade-off, and it is the reason Refresh all stays**: the screener shows the previous numbers until the final round. Refresh all remains the manual "everything, visibly" sweep; Fast refresh is the one to use as the universe grows.
- **`Fill missing` is a different axis and they multiply**: it narrows WHICH stocks get pulled (fewer rounds), this narrows what a round COSTS. Measured the same night: Fill missing 20 rounds / 137 profiles / 31.2 min ≈ 93s a round against Refresh all's 108s — halving the rounds did not make a round cheaper.
- Verified: the loop against a fake server (12 checks — the fast start, profile-only rounds, exactly one rebuild and it is last, the run named on it, progress from the round counts, a stop ending it without rebuilding, a refusal retried, a stall giving up without rebuilding) and the round endpoint against the real server on an in-memory database with the provider stubbed (11 checks — the flag's mode, the run's kind, seven profiles a round really fetched, the round recorded, progress written, NO snapshot until the end, the run still open after full coverage, a stopped run refusing).

## Fill missing — the incremental refresh
**`Fill missing` on the admin console's Data section re-pulls only the stocks whose company data is missing (2026-09-15).** Same round loop, banner and report as Refresh all; the difference is which profiles are expired at the start.

- **What counts as missing — `profileGaps()`**, in order: no stored profile (`none`, a new ticker); a stored profile **lacking a key** the app now writes (`fields`); a pull refused or never finished (`failed`, fetched_at 0). **Old is not missing** — that is the rotation's job and Refresh all's.
- **Absent, not null, is the test, and it is load-bearing.** `emptyProfile()` is the literal `fetchProfile()` starts from, and `PROFILE_FIELDS = Object.keys(emptyProfile())`, so every pull since a field was added carries its key even when the provider has no value. A fund pulled since 2026-09-14 has `industry: null` and is left alone; a naive "industry is blank" test would re-pull SMH, XLF, KRE and CYBR on every run and buy the same null. Measured before building: 115 profiles lacked the `industry` key (Cisco and Netflix among them — pulled the morning of the 14th, before the field shipped), 4 had it null. Adding a field to `emptyProfile()` is therefore all it takes to make the next Fill missing backfill it.
- **Prices**: none by default — `POST /api/refresh-all?mode=missing` stamps `prices_at` at once and every round is an archive round (7 profiles, 561 credits). The exception is a stock with **no bars at all**: `liveRefreshOpts()` gives a `mode: 'missing'` run one price round of exactly those symbols, then `notePriceRound` stamps `prices_at`.
- **State**: `refresh_state.mode` (`'missing'` or null). **`beginRefresh()` now also resets `prices_at` and `priced`** — it upserts onto a possibly stale row, and before this a row left by an abandoned run could tell the next run its prices were already pulled.
- **`GET /api/refresh-missing`** (admin) is the costed preview and changes nothing: the console shows it under the button ("115 of 271 stocks — 115 pulled before a field existed (industry). About 17 rounds, ~17 min, ~9,217 credits") and repeats it in a confirm; with nothing missing the button says so and does not navigate. The POST answers `{nothing: true}` in that case and the screener says so instead of starting.
- **The nightly job fills gaps first**: on a rotation night the cron `?start=1` expires the gaps before `expireOldestProfiles()`, which only picks among fetched_at > 0, so gaps are not counted against the rotation. A new ticker or a new field fills the next night.
- **The report** is titled *Fill missing* and adds "Still missing" (a fresh `profileGaps()` after the run) and "No industry from the provider (expected for funds)".
- **Credits are a pacing unit, not a budget**: Pro has no daily cap (`/api_usage` returns only `plan_limit: 610` per minute — a Basic key also returns a daily limit).
- Verified: the preview on live data (115 gaps, 17 rounds); a vm simulation of the real `profileGaps` / `missingPlan` / `liveRefreshOpts` / `notePriceRound` over an in-memory store (17 checks — only the 115 gaps re-pulled, 17 rounds, max 561 credits/round, a new ticker priced alone in round one, a normal Refresh all and a plain Refresh unchanged, null industry not a gap); headless page runs of the console preview + confirm, the screener's `?do=refreshmissing` run, and the nothing-missing path.

## Intraday price refreshes
**Prices refresh every 30 minutes through the US session, 9:40 AM to 3:40 PM New York time, on trading days (2026-09-15).** An external scheduler (**cron-job.org**, owner's choice over GitHub Actions, whose schedule can run 5-30 minutes late or skip under load) calls `GET /api/cron/intraday` with `Authorization: Bearer $CRON_SECRET`, set in New York time at minutes 10 and 40 of hours 9-15, Monday-Friday. **The server decides whether to act**, so the schedule can be broad and daylight saving needs no thought:

1. **Weekday, 9:38 AM-4:00 PM New York** (`nyClock`, two minutes of scheduler slack). Outside it the call returns without logging — the 9:10 call lands there every day by design.
2. **Nothing else running** — a Refresh all, Fill missing or nightly run in `refresh_state`; two jobs in one minute breach the 610 credits. Logged as a `skipped` run.
3. **NYSE open** by Twelve Data's `market_state` (`XNYS.is_market_open`, **1 credit**, verified on this plan 2026-09-15) — it knows holidays and early closes, so there is no holiday list to maintain. Logged as `skipped` when closed. An unreadable answer proceeds (a holiday run costs 272 credits and harms nothing).
4. One plain price refresh through the same `computeStocks` → `finishLiveRefresh` path as the Refresh button: snapshot rewritten, bars upserted (today's provisional bar, healed by the overlap), 12 stocks' headlines topped up, **no email**, logged as an `intraday` run with measured credits (~273 a run, ~3,550 a trading day; no daily cap on Pro).

- `?dry=1` runs every check and reports the decision without refreshing or logging. Measured: 13 runs on a summer day and on the Monday after the clocks change; boundaries at 9:37/9:38 and 16:00.
- **Open screener tabs follow the data**: every five minutes a visible tab calls `/api/status`, which now carries `updatedAt` from the snapshot's own column (`snapshotUpdatedAt()` — the ~1MB payload is not read), and reloads in place when it is newer. Sort, filters, tab and scroll survive (page state the reload does not touch); it waits while pulling, in a forward-returns view, and while the ladder panel or the star popover is open. Verified headlessly: 8 checks.
- **Advice is intraday too**: mid-session the Advice columns and the Changed chip read "so far today" against a provisional bar. The status line shows the update time.
- `/refreshes` shows the kind as *Intraday prices*, `skipped` runs with their reason, and a line in the Last nightly tile: today's intraday runs, the last one, failures and skips.
- **Setting it up is on cron-job.org, not in this repo** — nothing runs until that job exists.

## Refresh runs — the history page
**`/refreshes` (admin only, linked from the console's Data section) tracks every refresh run, manual or scheduled (2026-09-15).** `refresh_state` is the live flag and is deleted when a run ends; `refresh_runs` (one row per run) and `refresh_rounds` (one per round) are what is left afterwards. Kinds: `refresh` (plain price Refresh, a run of one round), `all`, `missing` (Fill missing), `nightly`, `nightly-full`, `nasdaq` (the three exchange pulls as one run). As-of views are lookups, not refreshes, and are not recorded.

- **Linking a stateless round to its run**: `refresh_state.run_id`. The browser loop passes `&run=<id>` on every `/api/stocks?refresh=1`, the workflow passes `runId` in its body, the NASDAQ page passes it between its three calls. A round lands on `running.runId`, else the id it was given — so a round whose 4-minute flag aged out still lands on the run that started it.
- **Credits are MEASURED**: `fetchJson` adds each response's `Api-Credits-Request` header to an `AsyncLocalStorage` meter (`creditMeter`) that `metered()` opens around each round's `computeStocks`; `fetchProfile` counts pulls and refusals into the same meter. Outside a metered round the store is undefined and nothing is counted.
- **Outcomes**: `complete` (finishLiveRefresh's covered branch), `incomplete` (the client's or the job's DELETE — gave up), `stopped`, `failed` (a plain Refresh or NASDAQ pull that errored), `abandoned` (still `running` with no update for `RUN_ABANDON_MS`, 6 min — swept lazily by every read, so no job has to), `running`. **`finishRun` only closes a run still running**, so the client's give-up arriving after a Stop cannot overwrite it.
- **Stop** (`POST /api/refresh-runs/stop`): clears the flag, marks the run `stopped`, sends the report. The loop driving it is told on its next round — `/api/stocks?refresh=1&run=<stopped id>` answers `{stopped:true}` and computes nothing (measured 0.4s, no round written), the cron route answers `{done:true, stopped:true}` and the workflow exits 0. **Without the run id the loop would have carried on as plain refreshes**, which is why every caller passes it.
- **Reports are stored on the run** (`report_html`, whether or not mail went out — `sendRefreshReport` now builds for a tracked run even without `MAIL_READY`), with failed and still-missing symbol lists; the page shows the report in a sandboxed iframe.
- **The missed-night alarm is a Vercel cron, not GitHub** (`vercel.json`, `GET /api/cron/watchdog` at 23:30 UTC — 7:30 PM EDT / 6:30 PM EST; Hobby crons fire somewhere in the hour). The failure it exists for is GitHub's scheduler not firing, and a watchdog on the same scheduler would miss the same night. It mails only for `missed`, `failed`, `abandoned`, `incomplete` or `stopped` on today's New York date; `running`, `pending` and `untracked` stay quiet. Vercel sends `Authorization: Bearer $CRON_SECRET`, which `isCron` already checks. `?dry=1` returns the verdict without mailing.
- **`nightVerdicts()`** judges each New York day by the newest nightly-kind run started that day (any trigger — a workflow_dispatch counts): ok / running / missed / pending (today before 5 PM) / untracked (before the first recorded run) / the run's own status.
- **The workflow** now sends `trigger` (`manual` for workflow_dispatch), the Actions `runUrl` (stored as the run's link, GitHub URLs only), and the `runId` from its start call on every round; it exits 0 on `stopped`.
- **Retention**: `pruneRuns()` rides every finishLiveRefresh — 90 days of runs, 30 of rounds. **Tracking never fails a refresh** (`trackSafe`), the bars rule.
- **The page**: the live run (progress against the run's own targets, rounds, ETA at 7 profiles a round, credits so far, last round vs 610, GitHub link, Stop), a health strip (last nightly, 14 New York nights, company-data age buckets + the Fill missing count + prices-through), trends (nightly run time over 30 runs coloured by outcome, credits per day stacked by kind), filterable history (type / trigger / outcome), and a run dialog (round chart with the 610 line, refused rounds in red, the coverage curve, a round table, the stored report). Polls every 10s while a run is live, 60s otherwise.
- Verified: db functions against the real tables with test rows removed afterwards (13 checks — round numbering, aggregates through an error round, Stop not overwritten, abandonment sweep, report storage); a local route walk (anon/guest 302 and 403, admin 200, 404 for an unknown run, the stopped short-circuit, watchdog 401 without the secret); the workflow's jq payloads; headless page runs over synthetic history (live card, Stop, 14 nights, trends, filters, dialog with rounds and report).

## Refresh state
**One Refresh All pulls prices ONCE (2026-09-13).** Its first round fetches prices live (chunked, 1 credit/symbol), archives them via `persistBars`, and stamps `prices_at` on `refresh_state`; every later round sees the stamp and rebuilds all rows **from the archive** (`readBarsFullFor`, zero price credits, SPY alone re-fetched live for 1 credit since the benchmark is deliberately never archived — the orphan sweep would collect it). An archive round therefore affords **7 profiles** (7×80 = 560 of the 610/min, `PROFILE_CAP_ARCHIVE_ROUND`), while any pass that also pulls prices sizes its profile batch down via `liveRefreshOpts()` so prices + profiles fit one minute — at 93 symbols that is 6, at 300 it is 3. Rounds run 62s apart; the browser budget is `ceil(total/7)+6` and the workflow's `MAX_ROUNDS` is 60 (~400-symbol headroom). At 300 symbols a full run is ~44 rounds ≈ 46 minutes. A Refresh All re-pulls only that round budget of profiles per call, 62s apart, so it runs for several minutes. The `refresh_state` table (at most one row; absent = idle) lets every instance know — a process variable cannot work, since Vercel shares nothing between them.

- `POST /api/refresh-all` expires the cache and calls `beginRefresh()`; `DELETE /api/refresh-all` ends it when the client's loop finishes or gives up.
- Each `?refresh=1` round calls `noteRefreshProgress()`, which **only updates a refresh that is already running** — that is what stops an ordinary price Refresh (seconds long) from raising the banner. It closes the flag itself once every row has a profile.
- `readRefreshState()` returns `null` once `updated_at` is older than `REFRESH_STALE_MS` (4 min), so an admin closing the tab mid-backfill can't pin the notice up forever.
- **The browser loop's round budget scales with the universe**: `ceil(total / 6) + 4`, where `total` comes from the `POST /api/refresh-all` response. It was a flat 15, which at 84 symbols left one round of slack — so a single refused round ended a run early and it stopped at 78/84. Refused rounds now have their own allowance (`REFUSALS_ALLOWED`) instead of spending the budget of a round that would have fetched something.
- **`DELETE /api/refresh-all` reports too**, like the cron job's equivalent. That is the give-up path — the loop ran out of rounds, or the tab was closed — and a run that stopped short has to say so. It discarded `endRefresh()`'s return before, so an incomplete run went silent, which is indistinguishable from success.
- **`endRefresh()` returns the run it cleared, or `null`.** Both the natural finish and the client's `DELETE` land there, and only the caller whose `delete` actually removed a row gets the state back — that is what stops one run sending two report emails. Anything hung off completion must go through its return value, not just call it.
- `GET /api/status` is a cheap poll for viewers — every open page hits it every 30s while a refresh runs, so it deliberately does not return the snapshot. When the flag clears the page pulls the finished data on its own.

## Charts and the stock page
Charts are hand-rolled inline SVG in `rowcard.js` — no library, no build step. `chartSVG(closes)` returns `{ svg, log }`.

- **The y-axis goes logarithmic above a 4× range.** MU ran from $1.69 to $932, so on a linear axis nineteen of twenty years flatten onto the floor and only the last month is visible. The caption says "log scale" when it happens, because an unmarked log axis misleads. A single year rarely trips it, so the default view stays linear.
- **The chart window is 253 bars, not 252.** A change is measured across the *gaps* between bars, so matching `pctChange(values, ONE_YEAR)` — bar 0 against bar 252 — needs one bar more than there are intervals. One short and the headline disagreed with the card's own 1Y row by 27 points on a name that gapped on earnings a year ago.
- **`/api/history` reads the archive only** — no API call, so it costs nothing and works for members. It returns `dates`, `closes` and `volumes`; ~1.9 KB for a year of closes. Fetched per symbol on hover and cached by `symbol|days`, so the card never waits on the network to appear. **The `days` floor is 2**, because the stock page's shortest range is a trading week — it was 20, which silently returned 20 bars for a 1W request.
- **No area fill — the price is a stroked line only.** Keep `fill` off `.ln`: the stroke and fill rules are deliberately *not* grouped, because setting `fill` on the line path closes it back to its start and paints a filled wedge. The area path masked that for a while.
- **`chartSVG(closes, { volumes, ticks })` draws volume bars and price gridlines**; without them it is a plain line. It returns `{ svg, log, ticks }`, each tick carrying `top` as a fraction of the viewBox so the caller can place an HTML label at the same height.
- **The price axis replaced the floating high/low captions** on the stock page — with real gridlines the two said the same thing twice. Ticks are evenly spaced along whichever scale is in use and rounded to significant figures; even spacing beats round numbers, because on a log axis round values land unevenly and the gridlines look accidental. The hover card passes none — at 104px there is no room — so the two surfaces share one function without the card becoming cramped. Bars are scaled to the largest in view, not an absolute, so a quiet stretch still shows its own shape, and each is green or red by whether that session closed above the one before it.
- **Axis labels are HTML, not SVG text.** The chart stretches with `preserveAspectRatio="none"`, which would distort any glyph inside it. The same reason the price captions are absolutely-positioned spans, anchored by percentage to the bands they describe rather than to the box.
- **Stock page ranges are 1W/2W/1M/3M/6M/1Y/5Y/Max** in trading days (5/10/21/63/126/253/1260/5200).
- **The symbol in the masthead is the stock picker.** Clicking it opens a filtered list of the whole universe, alphabetically, matching on symbol *or* company name. It is styled as the heading it replaced — only the caret says it opens — so the page gained navigation without gaining a control or a row.
  - **The list comes from `universe` on `/api/stock`**, `[{symbol, name}]`. That route already holds the whole snapshot in memory to work out the rank, so the field costs a `map` and ~3 KB rather than a query or a second request.
  - **Selecting navigates (`location.href`) rather than swapping in place**: back and forward keep working, and the per-symbol history fetch dominates the timing anyway.
  - Arrow keys move, Enter goes, Escape and an outside click close. On open the current symbol is the active row and is scrolled to **`center`**; arrowing uses `nearest`, so the list moves as little as possible.
- **Employees, website and the company description come from `/profile`, which `fetchProfile()` already calls for the sector** — the call costs the same whether one field is read or five, so these were free and were simply being discarded.
  - **They are deliberately absent from the snapshot**, and therefore from `/api/stocks` and from the chatbot. Eighty-odd descriptions is ~70 KB on every screener load and ~20k tokens of prose that answers none of the numeric questions the bot is asked. `/api/stock` reads them from the profile row instead, via `readProfile(symbol)` — a single-row read added for exactly this, since `readProfiles()` would pull 84 blobs to render one page.
  - **`buildSections(s, { extra: { info: [...] } })` appends rows to a named group.** Opt-in, and only `stock.html` passes it: these values are not in the snapshot, so putting them in `FIELD_SPEC` would print an em-dash in the hover card on every other page. A row with `href` renders as a link (`.rc-link`).
  - **A row with no value is omitted, not shown empty.** A profile last pulled before these fields existed simply has fewer rows, which reads better than a column of dashes — they fill in as each profile refreshes.
- **The sector/portfolio/rank chips sit inside `.head`, not on a row of their own.** A separate band cost 26px plus 30px of margins and pushed the chart 265px down the page; inline they cost nothing above ~900px wide and simply wrap below it.
- **Indicators stack as panes below the price, in the order RSI then volume** — RSI is 0–100 and price is in currency, so it cannot share the price axis. Turning a pane on **grows the chart** (viewBox 213 → 270, and the CSS height 320 → 406px in the same proportion) rather than squeezing the price panel: the svg stretches with `preserveAspectRatio="none"`, so a taller viewBox alone would just squash every band.
- **`chartSVG` returns `panes`**, each with `top`/`bottom` as fractions of the viewBox, so the caller positions HTML labels against real geometry. The volume label moves down on its own when RSI appears — an earlier hardcoded percentage had to be fixed once already.
- **Those fractions are of the viewBox, and the svg does not fill the box** — 18px at the bottom is reserved for the date axis. Every pane label therefore goes through `atY()`, which converts with `calc(f * (100% - 18px))`. A raw `top: %` lands things progressively low and pushed the oversold label clean out of its pane.
- **The chart has a hover crosshair** — a dotted line, a dot on the price, a date pill on the axis, and a readout of price, day change, volume and RSI. All of it is HTML overlaid on the svg, never drawn inside it: `preserveAspectRatio="none"` would stretch the dot into an ellipse and distort any text. `panes.price.at(v)` exists so the dot can sit on the line without the caller knowing whether the scale is log or linear. The static change caption and MA legend fade while hovering, so the top-left says one thing at a time.
- **`priceTicks()` returns low-to-high**, so in the DOM the *first* axis label is the bottom of the scale and the *last* is the top — the reverse of reading order. The end-clamping rules were briefly backwards and pushed the top label 6px above the chart, where `overflow: hidden` ate it.
- **The RSI band is taller than the volume band** (58 vs 44 viewBox units) because it carries threshold lines with words above and below them, where volume only needs bars. It also paints its own `.pane-bg`, a shade off the chart's ground, so the indicator reads as a separate instrument. 70 is red and 30 is green — the colours mean overbought and oversold; 50 stays neutral because it is only an anchor for the eye.
- **`RowCard.rsiSeries()` uses Wilder smoothing**, matching `rsi()` in server.js. Wilder is an exponential average and converges slowly, so it is computed over the padded window and sliced. Cross-checked against the screener's own RSI column for six symbols: **gap 0.000**, at both 300- and 453-bar warm-ups.
- **The momentum line has its own token, `--mom` (`#22d3ee`).** It shared `--accent-2` with RSI, which was tolerable while it was opt-in and is not now that the two adjacent panes both draw on load. Verified against real geometry rather than by eye: the 70 label lands at 30% of the pane, 30 at 70%, and the line's top maps to 87.8 against MU's measured high of 87.6.
- **The hover readout carries Mom. beside price, RSI and volume**, ordered to match the panes top to bottom so it reads down the chart.
- **A symbol with no momentum history disables the toggle rather than opening an empty pane** — SKHY and SPCX have too few bars to score (`MIN_BARS` 274). The `has-mom` class is set from what was actually drawn, not from the toggle, so the chart keeps the shorter height.
- **The momentum pane's axis is a fixed 0-100**, with reference lines at 70/50/30 — the whole point of an absolute score is that 68 means 68, so auto-scaling to the visible range would throw away the only thing the number has over a percentile. A consistently strong name therefore draws a flat high line, which is the correct picture.
- MACD is the obvious next pane and is already computed per row (`macdLine`, `macdSignal`, `macdHist`); adding it is a pane entry rather than a rewrite.
- **Moving averages are a list (`MAS` in stock.html), 50-day and 200-day.** Adding another is one entry there plus a colour in `app.css`.
  - **The page fetches `range.days + MA_PAD`**, padded by the *longest* average rather than by whichever are switched on, so toggling redraws from the cache instead of refetching. Without the run-up a line would start 200 sessions late and leave a gap at the left edge; with it, even the 1M view draws both averages as unbroken paths.
  - **Overlays are folded into the y-scale.** An average sits above a falling price and below a rising one, so scaling to the price alone clips it. A `null` breaks the path rather than joining across the gap.
  - `RowCard.sma()` is exported for it. Cross-checked against the screener's own `vs50ma` and `vs200ma` for six symbols: exact agreement, gap 0.000 on both.
- **`.rc-*` classes are unscoped in `app.css`**; only the floating container is tied to `#rowcard`. The hover card and `/stock/<SYMBOL>` render the same sections from the same `FIELD_SPEC` via `RowCard.buildSections()` — a third copy of the field list is exactly what the row card existed to prevent.
- **`GROUP_COLORS` / `GROUP_LABELS` now live in `rowcard.js`** and are exported. `index.html` and `analysis.html` keep their own copies because they also colour the table's group banners and the columns menu, so **all three must stay in step** — renaming the `rank` group's label to `Scores` in one of them left the stock page's card still saying `RANK`.
- The name cell links to `/stock/<SYMBOL>` on **both** the screener and the analysis page; the symbol cell still links out to Google Finance. All of them open in a new tab (`target="_blank" rel="noopener"`), so a click never loses your place in the list. `.namelink` lives in `app.css` because two pages use it; only the frozen-column truncation (`td.frz1 .namelink`) stays in index.html. `.namelink` inherits its colour and only underlines on hover — 69 rows of blue underlines would wreck the table.

## Bar archive
The `bars` table keeps one row per symbol per trading day (`open/high/low/close/volume`, keyed on `(symbol, d)`). **Currently 333,301 rows across 93 symbols, 2006-05-25 → today.**

**It costs no API credits.** Every refresh already fetches ~300 daily bars per symbol and discards them; `persistBars()` writes them instead. Twelve Data charges **1 credit per symbol regardless of `outputsize`** — measured, `Api-Credits-Request: 1` for 5000 bars — which is why the deep backfill was affordable in the first place.

- **The backfill is PACED, and it had to be (2026-09-15).** One symbol is one credit and the plan allows 610 a minute; the unpaced script fetched at ~15 symbols a second (900/min) and only survived the 190-symbol run because the whole job was 190 credits in 13 seconds. `makePacer(limit, windowMs)` is a sliding-window meter shared by the workers — `take()` resolves only when one more credit keeps the last 60s under `--rate` (default **580**) — and a refusal (`/credit|rate limit|429|too many/i`) waits 62s and retries up to 3 times rather than writing the symbol off. The receipt prints symbols/min and how long was spent waiting. Verified: the pacer on a fake clock (8 checks — no 60s window over the limit across 1,000 symbols, a 190-symbol run never delayed, the window sliding, the refusal pattern) and a live 3-symbol dry run at `--rate 2` that waited exactly 60s.
- **Measured throughput, 2026-09-15**: 190 symbols at depth 5000 took **3 min 45 s** and wrote **755,991 bars** — fetching was 13s of that, so writes are ~95% of the wall clock. Extrapolating to a 1,000-symbol universe: ~2.9M more rows, ~15-20 minutes, ~730 credits. **What actually costs at that size is profiles, not bars**: 730 new stocks is ~58,000 credits and ~1h45m of Fill missing.
- **Never run a shallow `--depth` over symbols that already have deep history** — each symbol is replaced wholesale, so `--depth 1300` would truncate twenty years to five. Use `--only` with the shallow ones (2026-09-15: 190 of 271 had under 1,300 bars).
- **`backfill-bars.js` is a local script, not a route.** 5000 bars is ~580 KB per symbol and ~40 MB across the universe: fine locally, far past what a serverless function should hold. Same one-off pattern as `migrate-to-turso.js`, dry-run by default.
- **Writes are incremental, not wholesale.** `persistBars()` upserts only bars newer than the stored `max(d)` plus a `BAR_OVERLAP` of 5. Steady state is a handful of rows per symbol per refresh, not 300.
- **The overlap is not decoration.** Twelve Data returns a bar for *today* while the market is open, with the current price as its close, so a mid-session refresh stores a provisional value. Re-upserting the recent window replaces it with the settled close. Measured drift on a real pull: 0.003–0.007%.
- **Splits are detected, not ignored.** A split re-prices all of history, so a stored bar `SPLIT_PROBE_BARS` (60) back would silently disagree with the fetched one and leave a phantom cliff in any chart. `persistBars()` compares that one bar per symbol — one query for the whole universe, since US symbols share trading days — and rewrites the symbol in full when it differs by more than `SPLIT_TOLERANCE` (0.5%). Re-running `backfill-bars.js --only SYM` is the manual repair.
- **An as-of pull must never write.** `computeStocks(asOf)` fetches a truncated range; persisting from it would corrupt the archive. Guarded by `if (!asOf)`, the same rule the snapshot uses.
- **A failed archive write never fails the refresh** — it is caught and logged. The screener is the product; the archive is a by-product.
- `open` is stored although nothing reads it yet. `high`/`low` drive the 52-week range and `volume` the volume trend, so an archive without them could draw a chart but not reproduce the screener — which is the point of keeping it.

## The nightly job
`.github/workflows/nightly-refresh.yml` runs a Refresh all at **4:15 PM in New York** — fifteen minutes after the close (changed 2026-09-15; it was 00:00 UTC, which drifted between 8PM and 7PM Eastern).

**A local time needs two crons and a guard, because GitHub cron is UTC and has no timezone option.** Both UTC times are registered — **20:15 while EDT is in force and 21:15 while EST is** — and the first step of the job converts `now` to `America/New_York` and stops unless the hour is 16. Exactly one of the pair can be right on any given day. **The guard asks the tz database rather than hardcoding changeover dates**, because those move; the US has shifted them before. Verified by replaying 400 days through the real tz data: exactly one run fires per day, with clean handover on 2026-03-08 (21:15Z → 20:15Z) and 2026-11-01 (back again). A `workflow_dispatch` run is never gated.

**4:15 PM is close to the close, and that is a deliberate trade.** Twelve Data returns a bar for *today* while the market is open whose close is the current price, so a run this soon after the bell can store a value that is not yet the settled close. It self-heals: `persistBars()` re-upserts a `BAR_OVERLAP` window of 5 sessions, so the next day's run overwrites it with the final figure (measured drift on a real pull: 0.003-0.007%). What it costs is that the evening's snapshot may carry a provisional close. Moving to 4:30 or 5:00 PM is one digit in both cron lines if that ever matters.

**A runner, not a Vercel cron, because a full pass takes ~13 minutes** and no serverless function stays alive that long. The workflow drives exactly the loop the browser drives.

- **`POST /api/cron/refresh`** is the job's only endpoint, authenticated by `Authorization: Bearer $CRON_SECRET`. Deliberately **not** admin: the secret satisfies that one route, so a leak cannot delete a portfolio. Unset leaves the route closed, not open. `?start=1` expires the cache and raises the flag; every later call runs one round. `DELETE` on the same path ends a run early and reports what it got.
- **The 62-second gap between rounds is not conservatism — it is measured.** Two rounds inside one minute returned *"1128 API credits were used, with the current limit being 610"*. A round costs roughly **560 credits** — and the decomposition was re-measured off `Api-Credits-Request` on 2026-09-13: **profiles are 85% of it, not the prices.** Exact per-call costs: `time_series` **1 credit per symbol** (batched or single), `/statistics` **50**, `/profile` **10**, `/earnings` (8 quarters) **20** — a cold profile is **80 credits/symbol**, so 6 per round = 480, plus ~1/symbol for the batch. The plan ceiling of 610/minute allows at most **7 cold profiles a minute**; prices for even a 300-symbol universe would cost only ~300 credits. Don't lower `GAP` without measuring again.
- The job stops after 3 rounds without progress, same as the browser loop, and cleans up the flag on its way out.
- Repository secrets required: `APP_URL`, `CRON_SECRET`.

### The refresh report
**Both kinds of refresh email the owner** — a Refresh all and an ordinary price Refresh — **whoever started it**, the job or the button. The Refresh all path hangs off `endRefresh()` rather than the cron route, because both ways of finishing one converge there.

- **Which report goes out is decided by whether a run was live a moment earlier, not by what the `delete` returned.** `endRefresh()` runs either way so a flag left stale by an abandoned run gets cleared, but a *stalled* Refresh all still reports nothing, and the click that happens to complete the universe is reported as the ordinary Refresh it was.
- **An ordinary Refresh has no `refresh_state` row**, so `/api/stocks` passes its own `startedAt` and actor into `finishLiveRefresh()` as `ctx`. No `ctx`, no plain report — which is what keeps the nightly job's dozen rounds quiet.
- **The plain report says `Fundamentals: not re-pulled — prices only`.** A price Refresh reuses cached profiles, so presenting the fundamentals as fresh would make the mail actively misleading. It is judged on failed symbols alone; coverage is a Refresh all's measure, and "no profile yet" is omitted entirely.
- **A plain price Refresh no longer mails (2026-09-15)** — manual or scheduled. Intraday refreshes made it thirteen emails a trading day, burying the Refresh all report that matters. `sendRefreshReport(state, 'plain')` still builds the report and stores it on the run for `/refreshes`; only `kind !== 'plain'` resolves a recipient. Refresh all, Fill missing and the nightly job mail as before.

- **Receipt**: run time first and largest, then the start and finish clock times, actor, `n/total` coverage, failed symbols, symbols still without a profile, prices-as-of, fundamentals rows recorded, bar-archive size and through-date. The duration leads because it is the number that says whether the night went normally — a Refresh all that finishes in seconds did not really run.
- **Clock times are rendered in `REPORT_TZ`, defaulting to `America/New_York`** — the runner is UTC and the reader is not. `fmtClock()` uses explicit `month`/`day`/`hour` components rather than `dateStyle`/`timeStyle`, because ECMA-402 refuses to combine those with `timeZoneName` and the throw lands in the fallback, which would quietly print UTC instead of saying so.
- **The digest is gone, and "The run" replaced it (2026-09-16, owner's call).** The day's movers and the advice changes both restated a standing the screener shows live, and neither said anything about the job — which is what this mail is for. In their place, the figures from the Refresh runs page: **rounds** (and how many were refused), **profiles** pulled and refused, **credits** with a per-round average against the 610/min ceiling, whether **prices** were pulled live or read from the archive, **round time** (average and longest), **round spacing** (against the 62s floor), the **trigger**, a link to that run's page and, for a nightly, the Actions job log. `runFacts()` reads the run's own row and its rounds. Removing the advice section also took the per-symbol news read off the report path — it no longer touches the `news` table.
- **The final round is now recorded BEFORE the tail runs.** `finishLiveRefresh` is what closes the run and builds the report, so a round recorded after it was missing from the very summary it completed; the counts it needs are derived from the same rows a moment earlier.
- **A Fast refresh mails as "Fast refresh"** — subject and heading both, from `state.mode`, rather than borrowing Refresh all's name.
- Verified end to end with the provider and the mail API stubbed: a Refresh all and a Fast refresh each driven to completion, then the delivered body checked — no movers, no advice changes, the run section present with rounds/credits/profiles/timing, the run link, the receipt still leading with the duration, and each job named correctly in its own subject and heading (18 checks).
- *Removed:* the day's five best and worst movers. **The screens and the highest-rated list were removed in Sep 2026** — both restated a *standing* rather than reporting what happened, both are one click away on `/analysis` and the screener where they are current rather than frozen at whenever the refresh finished, and together they were most of the email. `Screens.run()` over the whole universe went with them, so the report costs less to build too. `REPORT_SCREEN_NAMES`, `rating()` and `symLink()` were deleted as dead code; `Screens` is still imported for `cleanWeights` and `PAST_PERIODS`.
- **Fundamentals that moved** — *Refresh all only*, since only a Refresh all re-pulls the profile cache; a plain Refresh would be comparing a number with itself. Each symbol's row in `fundamentals_history` is compared against **the previous recorded set**, which is not "yesterday": a row exists only for days a Refresh all actually ran, so after a quiet stretch the comparison reaches further back. The email prints that date, because a change "since yesterday" that is really since last Thursday is a lie told by omission. `readFundamentalsPair(day)` fetches both sets in one round trip — measured at **48ms** against Turso.
  - **Six fields are deliberately excluded**: price, market cap, forward P/E, PEG, FCF yield and net-cash %. Measured across the recorded history, each moves on **79–100% of consecutive nights with a median of about 2%**, because each is divided by, or is, the price. They restate the day's price move, which the movers list already gives. The twelve fields that *are* tracked move on **1–5% of nights**, and when they move it is a **step, not a wobble** — AVGO's revenue sat at 75.46B for five recorded days, went to 89.10B, and stayed. The email says which fields are left out and why, so their absence is not read as an omission.
  - **Two derived signals were tried and rejected.** Forward EPS revision, backed out as `relΔ(price) − relΔ(forward P/E)`, and share-count change as `relΔ(market cap) − relΔ(price)`. Both look clever and both measure the wrong thing: SNOW threw a "+20.9% EPS revision" on a day its forward P/E was **byte-identical**, and the share-count signal oscillated −20.9% then +22.1% on consecutive days. The profile blob and the price are not sampled at the same instant, so the subtraction reads that staleness rather than the company.
  - **Absolutes are compared relatively, percentages in points.** A margin going from 0.1% to 0.3% is "+200%" and means nothing; it is +0.2pt. Thresholds are **5%** for an absolute (with a 1M floor, below which a percentage is silly) and **1 point** for a percentage — sized against the recorded history to yield roughly seven field-moves a night, clustered into one or two companies.
  - **A sign change is worded, not percentaged.** Net income going −29M → 59M is not "+302%", it is a company that started making money, and it leads the section. `netIncomeTtm`, `fcfTtm` and `netCash` each carry their own pair of phrases ("turned profitable" / "swung to a loss", "moved to net cash" / "moved to net debt").
  - **A point move's rank is capped** (`FUND_PTS_SCORE_CAP`), because a percentage-point change is not on the same scale as a relative one and the growth rates swing wildly — AVGO's earnings growth moved **128.6 points** on the night its net income rose 31%, and uncapped the derived figure outranked the report it came from.
  - **A symbol with no earlier row is counted, not compared** — it is new to the archive, not a company that changed overnight. The night the 33 faders were added, the email would have said "33 recorded for the first time, so nothing to compare." (They have since been pruned back to 14 — see the universe section.)
  - Values are printed **currency-neutral** (`89.10B`, not `$89.10B`): these are in the company's reporting currency, and Samsung's revenue is not dollars.
- A run that *stalls* sends nothing — nobody calls `endRefresh()` and the flag ages out on its own. The cron job covers that case, since it is the thing still awake.
- `REPORT_TO` overrides the destination; unset it falls back to the owner account's email, so it works before anything is configured.
- **Fundamentals rows are dated by `marketDay(rows)`** — the freshest bar in the universe, not the server clock. This mattered more at the old 8PM Eastern slot, where the server's UTC date was already tomorrow and dating by `new Date()` filed every automated run a day ahead of its own bars. At 4:15 PM Eastern the UTC date still agrees, but the rule stands: the bars decide the date, not the clock.

## Chatbot
`/chat` answers questions about the screener data. Open to **any signed-in user** (`requireAuth`), with the allowance set by role.

**The whole dataset goes in the system prompt — there is no retrieval layer, and adding one would make it worse.** Measured against the live API, not estimated: the system prompt was **~17,500 tokens** for 69 stocks, roughly 250 per stock — so at the current 93 it is around **23,000**, and worth re-measuring off `usage` rather than scaling this number if it ever matters. (A bytes/4 estimate said 7,400 — it underestimates dense numeric CSV by well over 2x, so trust `usage` in a real response, not arithmetic on the payload.) That still fits a 200k context about eleven times over and stays viable past 700 stocks, well beyond the 119-ticker ceiling the price API imposes. Embeddings are the wrong tool twice: the data is a numeric table where "margin above 15%" is an exact filter rather than a similarity, and retrieval could only drop rows the answer needs.

- **`cache_control` sits on the data block, which caches everything before it too** — so the rules block rides along and the whole system prompt is cached. A real call showed `cache_read_input_tokens: 17470` against `input_tokens: 31`: after the first question, all you pay for is the question and the answer.
- **`max_tokens` must cover the model's reasoning, not just the reply.** At 1200 a ranking question spent the entire budget thinking and came back with a single `thinking` block and no text at all — a silent empty answer. It is 6000 now. The reasoning is worth paying for: working through 69 rows is exactly where a model miscounts. `stop_reason === 'max_tokens'` is reported to the user as "ask something narrower" rather than a generic failure.
- **`CHAT_FIELDS` is the glossary and the column list at once.** Adding a column to the bot means adding one entry there — nothing else. Fields absent from older snapshots simply come through blank.
- **Derived values are computed server-side on purpose** (`fcfYield`, `netCashPct`, all three margins, `range52Pos`). Asking a model to divide across 69 rows is where it goes wrong, so the prompt tells it to use the column rather than re-derive it.
- **Four behavioural rules live in `chatRules()`**, and they are the actual product: name the specific gap and redirect rather than refusing bare; general finance and company knowledge is fine but flagged as background; no buy/sell/hold verdicts (declined for the same reason as any unsupported claim, *not* as a standing disclaimer); "will it go up" is unknowable rather than missing. The prompt lists what the data does **not** contain — a general "say you don't know" instruction does not stop confabulation, an explicit inventory does.
- **Quota lives in `chat_usage`** (one row per user per UTC day), because an in-memory counter cannot work when consecutive requests need not share a process. It is charged only after the config and validation checks, so a malformed request cannot burn it, and the 429 is returned before any API call — a member over their limit costs nothing.
- **Two ceilings:** `CHAT_DAILY_LIMIT` (60) for the owner, `CHAT_DAILY_LIMIT_MEMBER` (3) for everyone else. The owner pays for the key and does the prompt tuning; members get enough to be useful. **Keyed on the account**, so a shared login shares the allowance rather than multiplying it. With the current roster that caps daily exposure at 60 + 3 per member.
- **Upstream errors are never echoed to the client** — the response body can restate the request, and the API key travels in the same headers.
- Not streaming yet. Responses are a few seconds behind a "thinking" indicator; streaming is the obvious next step but could not be verified against Vercel from here.
- `private/chat.html` carries a ~60-line markdown renderer for the reply (tables, lists, headings, bold/italic/code). **It escapes HTML before applying any markdown**, so nothing a model returns can inject markup.

**Gated pages are no longer served raw** — see **Serving and the gate** above. The short version: they left `public/`, because nothing in `public/` can be protected.

## Does any of this predict anything? — the research log
**Read this before designing another backtest.** Several framings have been tested against the full archive and all of them came back flat. The point of writing them down is so the next session tries something new rather than rediscovering the same negatives.

**The momentum half of this log moved to [docs/momentum-delta.md](docs/momentum-delta.md) on 2026-09-15**, when momentum over time was retired from the product — the delta, the stored history, the signal study and the lab's slope indicator. Five framings on the momentum score and its delta are recorded there, every one flat, the strongest reading **r = −0.003** against the next fortnight. Read it before proposing anything momentum-shaped; do not re-run those tests from memory.

**The owner's constraint: a maximum holding period of one to two months.** That is the hardest horizon there is — short-term reversal has faded and momentum has not started — and it rules out most of what the literature offers. Anything proposed should be judged against it.

### What has been tested, and what came back

| test | result |
|---|---|
| **range position → next month**, three definitions (60d z-score, 252d and 120d high-low band), cross-sectional | **Flat, and mildly the wrong way.** Bottom band −0.00% to +0.53%, top band +0.23% to +0.27%; largest \|t\| anywhere 1.2 in one thin cell. The *top* of the range does slightly better than the bottom |
| **the two falling-knife filters** on the bottom band — idiosyncratic vs market-wide fall, and "has it turned" | Neither helps; both point slightly the wrong way. Stacking trend + idiosyncratic + turned leaves **under 500 observations in nineteen years**, ~26 a year — unusable even if it worked |
| **RSI**, 40 settings, cross-sectional at one month | **Best top-decile t: 1.76** on the 116-symbol universe, **2.73** on the 93-symbol one — see the universe section; the pool changed, not the indicator. |
| | Pre-2020 t 0.1, post-2020 t 2.2. Pre-2020 t 0.1, post-2020 t 2.2. On a single stock the deciles are visibly non-flat while the correlation reads 0.044 — which is the lesson, not a finding |
| **risk-adjusted velocity**, 144 parameter settings, cross-sectional at one month | **Best top-decile t anywhere on the grid: 1.73.** With 144 tests that is noise. Pre-2020 t 0.2–0.6, post-2020 t 1.6–2.1 — the same split as everything else |
| **a 50-day-only trend rule**, raw and with 2/3-close persistence filters, vs the 200-day rule, 90 symbols | **Worse than the 200D at every cost level, in both eras.** Median 158 round trips vs 62 (persistence trims to 68 but does not rescue returns); beats the 200D on 30/90 stocks at zero cost, 19/90 at 20 bps; identical drawdown medians, so it buys nothing on risk either. The faster re-boarding is real (PTON: −12% vs the 200D's −62%) and swamped by churn everywhere else (NVDA: +4,446% vs +47,355%). Measured 2026-09-12; do not build a 50D mode without a new reason |
| **ranking WITHIN an advice tier by room to exit** (2026-09-17; 307,965 stock-days, 42,285 in the technical Strong Buy tier, 3,463 days where 4+ competed, ranked within the day against the universe's equal-weight return) | **The raw distance orders the downside BACKWARDS.** The roomiest half of a tier sits 25.6% above its 200-day on 31.7% volatility against 13.8% and 24.3% — "more room" is "more extended" — and its tail is fatter in 6 of 6 era/horizon cells (3M p10 −18.5% vs −15.7%). Divided by the stock's own volatility it inverts and behaves: thinner p10 AND p25 in 6 of 6 cells, both eras, by 0.9–3.3 points. **Neither version picks winners** — every mean-excess difference is noise (|t| ≤ 1.6 raw, ≤ 0.6 adjusted) and the raw one's sign flips post-2020. Shipped as the **Cushion** column and the advice-sort tiebreak, labelled as tail size |
| **volume confirmation on breakouts** (2026-09-14; three definitions × vol/20d-avg at 1.25/1.5/2.0×, benchmark-relative 1M/2M excess) | **The first result whose sign survives the 2020 split.** Fresh 3M-high crosses on ≥1.5× volume beat quiet ones in BOTH eras (pre-2020 +0.36%/1M t 2.56, +0.56%/2M t 2.52; post +0.88% t 2.43, +1.39% t 2.54; pooled t 3.4–3.6 on 32k events); 52W-high crosses agree, strongest pre-2020 (t 3.2–3.5). Event clustering means the honest t is nearer 2–2.5. **The 200D upcross gets nothing from volume in either era** — the engine's trend gate has no volume story. Effect is real-looking but small (+½–1% per event over 1–2 months) and not monotone (2× is weaker than 1.5× — blowoff days). Suggestive; fit for a descriptive marker, not a gate |

### The conclusions

- **The momentum delta was not predictive in any framing**, and in September 2026 it was removed from the product along with everything built on it — see [docs/momentum-delta.md](docs/momentum-delta.md). The live score stays, as a description of where a stock stands rather than a claim about where it is going.
- **The range hypothesis does not hold here.** "Stocks trade in ranges, buy the lower side, avoid the falling knife" was tested with three range definitions and both knife filters. Every cell sits inside noise, and the faint direction runs *toward* momentum rather than reversion. The one cell that looked interesting — bottom band, trend intact, not falling worse than the market — reads **−0.03% before 2020 against +1.59% after, t 0.6**: the same regime split as everything else.
- **The trend filter (above/below the 200-day) is the only thing that keeps reappearing**, positive in four of five range bands and negative below the line in four of five. It is never significant (|t| ≤ 0.8) and `from_high` looked this promising once too, so treat it as a lead and not a finding.
- **A sixth framing came back flat on return, and one came back inverted.** Ranking inside an advice tier by room to exit looks like a safety measure and is an extension-and-volatility measure; the roomiest names had the worst tails. Volatility-adjusting it fixes the ordering of the tail and changes nothing about the mean. The lesson is the `vol trend` one again: **a risk field that is not normalised by the stock's own volatility is measuring size, not risk.**
- **Nothing yet clears a bar worth acting on**, least of all at a one-to-two-month horizon. The nearest miss is volume-confirmed momentum breakouts (above), the only line whose sign held through the 2020 split — worth showing as a fact beside the Entry state, not worth gating a rule on.
- **The screener is descriptive rather than predictive**, and that is a legitimate thing for it to be: a consistent way to see where every stock in the universe stands and how each got there.

### The method, which matters more than any single result

Four ways to make a weak relationship look strong, each of which has caught something real here:

- **Hold out a period before looking.** `from_high` looked like the one factor with a consistent sign across the whole archive until it was re-run on the widened universe, where it fell from t −2.2 to −1.4 and lost half its magnitude. It was a universe artefact.
- **Discount for overlapping windows.** Daily observations of a 10-day forward return carry about one observation's worth of evidence per ten. A level is far more autocorrelated than a delta, so its effective sample is smaller still — a per-stock test of the level has almost no power, whatever its n says.
- **Rank within the day, not across the pool.** Both legs then live through the same fortnight, so the market subtracts out and the baseline becomes zero by construction. This is the only framing that has shown anything at all.
- **Give a hit rate its base rate.** A stock that rises most fortnights hands a high hit rate to a signal that knows nothing. Only the lift is worth reading. Splitting a 0-100 score at zero once reported a lift of exactly `+0.0 pts` for every horizon — an artefact that looked like a finding.

### What would actually change the answer

Five hypotheses have now come back flat, which starts to say something about the search space rather than the hypotheses. The binding constraints, in order:

- **The universe.** 93 large, heavily-covered, correlated names is the most arbitraged corner of the market, and at a one-month horizon most of their variance is common. Mean reversion in particular works better in higher-volatility, less-followed stocks. Changing this would do more than any signal.
- **The data.** Everything tested comes from daily bars. Earnings revisions, short interest, options positioning and intraday behaviour are all absent, and they are where the remaining short-horizon effects live.
- **The horizon.** One to two months is the gap between reversal and momentum. Nothing much lives there.

### Untested, and worth doing

- **Post-earnings announcement drift** is the best fit for a one-to-two-month hold that exists — right timescale, durable literature, and there is already a "Drifting after a beat" screen built on the idea. **Recording began 2026-09-13**: `next_earnings_date`, `next_earnings_estimated`, `last_earnings_date` and `last_surprise` now ride every Refresh-all row of `fundamentals_history` (via `FUND_EXTRAS` in db.js — written beside `FUND_FIELDS`, deliberately outside the email's numeric moved-fields pipeline). The study becomes answerable a couple of quarters after that date; nothing before it is recoverable.
- **Benchmark-relative returns everywhere** — subtract the universe's equal-weight return that day. It turns "did it go up" into "did it beat its peers", which is the only version that survives the remaining bias.
- **Longer horizons, 6m and 12m.** The model is built around 12-1 momentum and has never been tested at the horizon it was designed for. Two more `LEAD`s in the view.
- **A per-factor sign.** `cleanWeights()` clamps weights to `0…MAX_WEIGHT` as integers, so a factor can be zeroed but not inverted. Expressing a reversion model needs a sign. Build that as a second model, not as a preset — a momentum model with its momentum inverted is a different thing wearing the same name.

### Where the analysis actually runs

**On the owner's machine, in throwaway scripts, leaving no trace.** None of the numbers above can be reproduced from the product; there is no button for any of it. If a result starts mattering, precompute it nightly into its own table the way `fundamentals_history` is — a page that recomputes a universe-wide statistic per request would be the 3.8s cross-sectional query on every load.

**The wall is a duration, not a size.** An early note here said "~36 MB in one round trip" and that was wrong — measured properly:

| query | result |
|---|---|
| `readBarsFor`-shaped, 650 days x universe — **the biggest the app issues** | **50,036 rows, 2.0s** |
| one symbol's full momentum history | 4,734 rows, 0.2s |
| the snapshot `/api/stocks` serves | 0.34 MB |
| all bars, `close` only | 441,483 rows, **82.6s — ok** |
| all bars, every column | 441,483 rows, **158.1s — ok** |
| both of those in parallel | **fails at 215.8s**, having read 36.2 MB |
| `ntile()` deciles aggregated server-side, returning 10 rows | **ok, but 159.7s** |

So a single query has carried 441k full rows fine, and the failure came after **83 MB**. The boundary sits between **158s and 216s** — a response timeout of roughly three minutes somewhere in the stack. Parallel queries fail *sooner in bytes* only because they share wall-clock, which is what made it look like a size cap.

- **Production is nowhere near it.** The largest query any route issues is 50k rows in 2.0s — about eighty times inside the wall. This constrains ad-hoc analysis, nothing else.
- **Pushing the work into SQL is not the escape hatch it looks like.** The `ntile()` aggregation returns ten rows and still took 159.7s: for whole-archive work Turso is compute-bound too, and that query is itself close to timing out.
- **For repeated analysis, use the local copy — `analysis-db.js`.** `node:sqlite` ships with Node 24, so it adds no dependency. **Full build 48.6s for 852,706 rows; incremental sync 4.7s.** Verified faithful on creation: every table's count matched and 726 spot-checked values differed by exactly zero.
  - **The gain is not marginal.** The `ntile()` decile query takes **0.53s locally against 159.7s on Turso — 300x**, and the whole momentum table loads into memory in 0.39s against 41s chunked over the network. Research that was minutes-per-iteration is now instant, and costs no rows against the Turso account.
  - **It deliberately omits `users`, `sessions` and `password_resets`** — password hashes and live session tokens have no business in an unencrypted file on a laptop — and `visitors`, `prefs`, `chat_usage` as personal and useless here. **It is not a backup.**
  - The schema is copied from `sqlite_master` rather than restated, so a column added upstream arrives without this file knowing about it. Dated tables re-pull `OVERLAP_DAYS` (10) on top of what is local, because the archive rewrites recent bars — a provisional close for a session still in progress is not final. A split rewrites a symbol's whole history, so **`--full` after a split**, the same repair path `backfill-bars.js --only` is for.

## The strategy backtest
`/strategy` is the first thing here that simulates a rule rather than measuring an association: signal, position, rebalance, equity curve. Reached from **Strategy** in the screener's bar. **The word "backtest" was freed for it** — what used to carry that name is now the forward-returns view.

**Why a separate page from the lab.** The lab is per-symbol and asks whether an indicator is associated with what happens next. A portfolio is not about a symbol, and its outputs — equity curve, Sharpe, drawdown, turnover, correlation to holding — fit none of the lab's panels. Its controls are different too: sizing, rebalancing, filters, costs.

- **The rule is Moskowitz–Ooi–Pedersen time-series momentum**: long what rose over the lookback, short what fell, each position sized `targetVol ÷ its own realised vol`, rebalanced monthly, the last month of the lookback skipped. Stock selection is a dropdown over **All plus every portfolio**, so a rule can be run on Mag 7 or on the Faded 33.
- **The day is earned before the rebalance, and that was a bug first.** Rebalancing first decided a weight from today's close and then earned today's move with it. The sizing was the worse half — `vol[i]` contains the very return being earned, so a rule facing a gap sized itself down for a move it could not have seen. Measured on a synthetic series with a 48% gap landing on a rebalance day: **the wrong order booked 5.98%, the right one books 39.98%.** Note the direction — here the lookahead *understated* the return, which is why "does it look too good" is not a sufficient test for one.
- **Monthly returns, turnover and average exposure are shipped, not finished statistics.** That is what makes cost, target volatility and the cash rate sliders rather than assumptions: cost is charged against real turnover, vol scaling is linear in the returns, and the cash credit follows exposure — so all three apply after the fact. At a monthly rebalance across a hundred names, cost is often the difference between a positive and a negative result. Monthly rather than daily is what keeps the payload manageable; it costs drawdown resolution, and the page says so.
- **One file per universe, plus an index (`strategy-index.json`).** The grid is 192 variants and every universe's copy of it is ~13 MB — a multi-megabyte download to read one entry in a dropdown. The page fetches the index, then the chosen universe, and caches each. The largest is ~950 KB raw, **~230 KB over the wire**, and only one is ever fetched. Size tracks the 192 variants x 225 months, not the number of symbols, so pruning the universe barely moves it.
- **The grid covers six dimensions**: lookback (21/63/126/252), direction, the 200-day filter, **leverage cap (1× / 3×)**, **rebalance (daily / weekly / monthly)** and **volatility window (20 / 60)**. The last three were added after a piece of published long-only research specified 1×, daily and 20 — three things the page could not express. `skip` (21) and the stored `targetVol` (10) stay fixed.
- **Idle cash earns, and that is a slider.** A long-only rule behind a 200-day filter sits in cash for much of its life; crediting it nothing understates it by whatever bills paid. The rate is credited on `1 − exposure` (capped at 1, so a levered book earns nothing on cash) and buy-and-hold gets none, being always fully invested. An **Invested** card reports average gross exposure, because that single number explains a modest CAGR better than anything else on the page.
- **The cash rate is also subtracted as the risk-free rate in Sharpe, and skipping that half was a bug.** Crediting 4% on a book sitting 87% in cash and *not* subtracting it reported **Sharpe 2.79** — the T-bill's ratio, borrowed. Sharpe is excess of cash by definition. Both the strategy and the benchmark take the same rate, or the comparison is rigged. The invariant that proves it: for a fully-uninvested book the Sharpe is **−0.09 with or without the credit**, since adding a constant and then measuring excess over it changes nothing. A `s > 1e-12` guard returns `null` rather than a ratio for a constant series, which otherwise printed **3.45 for a T-bill** out of floating-point residue.
- **"Match buy-and-hold" exists because a comparison at different exposures is meaningless.** The stored runs sit at ~29% gross exposure, so their raw CAGR looks terrible against a fully-invested benchmark while their drawdown looks wonderful. Neither number says anything until the volatilities match.
- **A gap is carried forward for at most `MAX_CARRY` (10) sessions; a longer hole, and a symbol not yet listed, stay null.** Without *any* carry a single missing print reads as a 100% loss and back again. Without the **bound**, GPS — which has a **1,373-session hole between 2019-11 and 2025-03** — was held flat at $16.78 for five and a half years: realised volatility of exactly zero, so `targetVol ÷ vol` produced a weight of **3,227,535×** and the leverage cap silently *became* the position; it then earned nothing for the whole stretch and booked the entire re-listing as one **+33% day**. A hole is absent data, not a quiet stock. 10 is chosen to clear a foreign listing's holiday calendar and nothing more — 005930 (Samsung) legitimately misses up to 6 consecutive US sessions over Chuseok, and is the only other symbol in the archive with an internal gap over one.
- **`MIN_VOL` (1% annualised) floors the divisor** as a second guard, so the failure is impossible rather than merely unlikely. After both fixes the largest raw weight anywhere is **7.83× on VRT**, a genuinely quiet stretch, which is what the cap is for.
- **The leverage cap barely binds in this universe, and the control says so.** At a 10% target these stocks are volatile enough that `target ÷ vol` lands under 1× on **99.2% of stock-days**, so 1× and 3× agree to within 0.1 points of CAGR. It would bite in a calmer universe, or at a higher target — but note that target volatility is applied afterwards as a multiplier, so **raising it does not re-apply the cap**. The cap belongs to the 10% the grid was run at, and the hint says that too.
- **The first result on this project that does not vanish before 2020.** At matched volatility, 5 bps, nothing earned on cash, **on the 116-symbol universe**: CAGR **+16.7% against hold's +18.6%**, Sharpe **0.97 against 0.95**, max drawdown **−28.1% against −43.4%** — and in **2008–2019 alone, Sharpe 0.95 against hold's 0.84**. Re-measured on the 93-symbol universe it reads **+18.8% against +21.8%**, Sharpe **1.09 against 1.03**, max drawdown **−22.1% against −44.1%**. It earns that by sidestepping 2008, which is visible in the curve and is mechanically what a trend rule does. It is a *risk* result, not a return result, and it remains hostage to a universe with no bankruptcies in it.
- **The published long-only formulation beats holding, once the SMA bug above is fixed.** Set as specified — 252-day lookback, mandatory 200-day filter, 1× cap, daily, 20-day vol — at matched volatility and 5 bps: CAGR **+14.8%** against hold's +18.6%, Sharpe **1.00 against 0.95**, max drawdown **−18.2% against −43.4%**. Credit 4% on the cash it holds and subtract the same 4% from both Sharpes and it is **0.80 against 0.75**, ahead in *both* halves (0.63 vs 0.61 pre-2020, 1.04 vs 0.96 after). **This is the corrected reading**: the first pass reported 0.66 against 0.75 and "does not beat holding", which was `smaSeries` excluding 49 symbols rather than a property of the rule.
  - **All of the above is on the 116-symbol universe. On the 93-symbol one the "both halves" claim no longer holds**: Sharpe 0.86 against hold's 0.84 over the whole period, but **0.66 against 0.69 pre-2020** — it loses the half that matters. Same code, same settings, 19 fewer laggards. Treat the pre-2020 half as the test and this as a reminder of how little separates the two answers. The `Set the published long-only rule` link on the page puts all five knobs there in one click.
- **Daily rebalancing is only optimal below about 3 bps.** At matched volatility the spec rule turns over **199% of capital a month** against 55% monthly. Measured crossovers: daily leads weekly to **2.77 bps**, weekly leads monthly to **15.26 bps**, monthly above that. Research quoting a daily-rebalanced vol-scaled rule at zero cost is quoting the most flattering version of the most expensive setting — which is why cost and cash yield are both on screen, since they pull opposite ways and omitting both is not neutral.
- **`smaSeries` returned all nulls for any series with a leading null, and that silently broke every `+200D` result.** A symbol that listed after the window's start has leading nulls; the function bailed at the first one and returned an empty series, which then failed the `sma[i] == null` guard on every session — so **49 of 116 symbols held no position at all in any 200-day-filter variant**, while still counting in the divisor. The filter was not being selective, it was disqualifying nearly half the universe. Fixed to restart the window on a hole rather than end the series; **113 of 116 now get a usable average** (the other three are too short). Every `+200D` number published before 2026-09-10 was wrong, including the first read on the long-only spec, and the correction *reversed* its conclusion.

## The single-stock strategy
`/single` runs the same trend rule on **one stock at a time** — not which stock to own, but whether to own the one you have picked, and how much of it. Reached from **Single** in the screener's bar, beside Strategy.

**It is an exposure control, not a stock picker**, and that changes what counts as success. The benchmark is holding that same stock, and the honest expectation is that it loses on return and wins on drawdown, because a rule that sits in cash part of the time must give up upside to avoid downside.

- **Nothing is precomputed — the page ships the prices and simulates in the browser.** That is the opposite of `/strategy`, and the reason is a correctness one: **the position cap is a nonlinearity.** `/strategy` stores runs at a 10% target and scales afterwards, which is exact for everything except the cap, so raising the target there does *not* re-apply it. The cap binds on 0.8% of stock-days at a 10% target and **10.8% at 15%**, so the published rule's 1× cap at 15% is a setting that page cannot honestly express. Recomputing from prices makes every slider exact.
- **The engine is `strategy.js`, unchanged.** One symbol is a universe of one. Two options were added for this page and both default off, so the stored portfolio grid keeps its meaning: `exitOnSignal` (rebalance the day the signal flips, not only on the schedule) and `opts.ppy` (periods per year — 252 here, 12 for the monthly series `/strategy` ships). A single stock is small enough to measure daily, so its drawdown is the real intra-period low rather than a month-end understatement.
- **`single-closes.json` is ~700 KB gzipped at 93 symbols (888 KB at 116), and the precision was measured rather than assumed.** The archive holds split-adjusted floats, so essentially every symbol carries more than two decimals. Rounding to cents corrupts a daily return by up to **5.45 percentage points** — NVDA's 2007 closes are $0.16 after every split since, where a cent is 3% of the price. 4dp costs 750 KB and is wrong by at most 0.007 points of CAGR; the shipped 1e6 scale costs 888 KB and is exact. Deltas rather than prices, because consecutive closes are close together and small integers compress.
- **Both legs start where the rule first becomes evaluable.** Comparing a strategy's post-warm-up years against a benchmark's whole history is how a backtest flatters itself for free.
- **"Match buy-and-hold" solves for the target volatility rather than scaling a result**, by bisection, because the cap makes the relationship non-linear. It is load-bearing: at the defaults the rule averages **29% invested** against a fully-invested benchmark, so the CAGR column is not a fair fight and the page says so. **At a 1× cap the rule often cannot reach the stock's volatility at all** — it saturates, which the note explains rather than leaving the link looking broken. Matched at 3×: AAPL **25.1% against 24.2%** (Sharpe 0.76 vs 0.74), MU **45.7% against 30.1%** (0.89 vs 0.68).
- **Sharpe is the fair column and CAGR is not.** Sharpe is return over volatility, so it barely moves when the rule runs at a fraction of the exposure — the exposure cancels, including the cash credit and its matching risk-free subtraction. The verdict line says which to read.
- **The sweep is the point of the page.** The identical rule at the identical settings across the whole universe, because one stock rebalanced monthly over eighteen years is perhaps twenty independent decisions, and the tickers this gets tested on are the ones that went up. **~90 simulations in ~180ms**, so it follows a slider drag on a 140ms debounce rather than being a build step. `runOne(symbol, full)` skips slicing the chart-sized arrays the sweep would throw away.
- **What it says at the published settings: beats holding on Sharpe in 22 of 87 stocks (25%)**, cuts the worst drawdown in all 87 (which is arithmetic, not skill — anything holding cash does that), median time invested 28%. It was 33 of 110 (30%) on the 116-symbol universe. On the **Faded 33** the median CAGR gap turns *positive* (+1.7%), which is the one hint of something: a trend rule earns its keep on stocks that fall, and those are exactly the names a survivor-only universe under-represents.
- **A hole longer than `MAX_CARRY` (10) sessions is left blank**, the same bound `/strategy` uses and for the same reason recorded there.

## The indicator lab
`/lab/<SYMBOL>` builds a short-horizon indicator, draws it under the price, and reports what it does — for that stock live, and for the whole universe from a precomputed grid. Reached from **Indicator lab** beside Signal study on the stock page.

**Three indicators so far.** `skip` appears in both and is 12-1's trick at a short horizon: the recent days are where reversal lives, so leaving them out asks for continuation without buying the bounce.

- **Risk-adjusted velocity** — a short-horizon return divided by the stock's own volatility, filling the hole in the momentum score, which risk-adjusts at 3, 6 and 12 months and nothing below. Knobs: lookback, skip, vol window, smoothing.
- **Momentum score slope** — the least-squares slope of the score, in points per session. Knobs: fit window, skip, smoothing. **No volatility normaliser, and that is not an oversight**: the score has been on a fixed absolute scale since September, so 0.4 points a session means the same thing for every stock — which it would not while the score was a percentile. A direct payoff from that change.
  - **`compute(series, params, id)` takes the whole bundle** `{ closes, score, dates }` rather than closes, because the two indicators read different data with different start dates. Threading two signatures through three surfaces would have been worse than one bundle everywhere.
- **RSI** — Wilder's plain reading, not the sub-score the model derives from it. Knobs: period, skip, smoothing. Verified against `Momentum.rsiSeriesAt` at **gap 0.00e+0**, and against the screener's own RSI column (61.1603 both).

- **`indicators.js` is shared three ways** — the page, the server and the offline grid — for the same reason `momentum.js` and `screens.js` are. A second implementation of "vol-normalised 10-day return" would have drifted inside a week. Verified against `momentum.js` on creation: `windowReturn` agrees to **0.00e+0** and `realisedVolSeries` to **1.14e-13** once the parameterisations are lined up (momentum.js takes two endpoints, this takes length-and-skip, so its 252→21 is lookback 231 skip 21).
- **Series are oldest-first here**, the reverse of `momentum.js`, because charts and rolling windows both read forward. Flip once at the boundary rather than reasoning about a reversed index in every loop.
- **The rolling volatility requires its full window, which deliberately differs from `momentum.js`.** That function scores one date from whatever history it has; this one draws a line, and a line whose early points used 20 returns and whose later ones used 126 is not one measurement along its length.
- **The forward horizon is selectable — 1 week, 2 weeks, 1 month, 2 months, 3 months — and nothing is hardcoded to 21.** `HORIZONS` lives in `indicators.js` because that is the module all three surfaces load, and a page offering a horizon the grid never computed would snap silently to the wrong row.
  - **No forward-return array is shipped.** The browser has the closes, so `forwardReturn()` derives it for whichever horizon is chosen. Sending one array per horizon would have been five copies of something the page can work out, and it is the only way an arbitrary horizon could work at all.
  - **The overlap discount follows the horizon**, which is the point of showing it: MU's 4,638 usable sessions are **939 independent observations at a week and 73 at three months**. A three-month test looks like it has thousands of points and has seventy-three.
  - The grid covers **every indicator × every horizon** — 1,160 rows, five horizons × 232 settings — and snaps to the nearest computed horizon the same way the parameters snap, saying so when it does.
- **The browser computes the indicator, not the server.** A round trip per slider drag would make the control feel like a query. `/api/lab` ships bars and forward returns; the page recomputes the indicator and every statistic through the same modules the server uses. Redraws are debounced 90ms while the number beside the slider updates immediately.
- **The decile panel is the one that matters for a non-monotonic indicator**, and it is why RSI was worth adding. A correlation measures a *straight line*, so a relationship where both ends pay and the middle does not cancels itself out and reports zero — which is exactly the shape RSI is supposed to have, and why the model's own `rsiScore()` is a curve rather than a slope. Live demonstration on MU: **correlation 0.0440, which reads as nothing, while decile 9 returns +7.49% against a +3.46% overall mean.** Bars are measured against the overall mean rather than zero, because zero only tells you the stock went up. Buckets come from the lab's own bucketing.
- **The scatter colours the dots by era**, blue before 2020 and cyan after, with a fit line for each and the two correlations in the legend. The regime split every result on this project has shown is the thing to look for, and a reader should see two populations rather than consult a card. Clicking a legend chip hides that era; hiding both is refused, since an empty chart is not a view of anything.
- **A full recompute redraws both charts in 28ms** for MU's 4,680 sessions, comfortably inside the 90ms debounce. Dot radius steps down above 1,200 and 2,500 points, `dotNodes` is captured once per render, and the middle-99% axis with strays pinned in amber exists because: a handful of extreme fortnights would otherwise press every other point into a band a few pixels tall.
- **The page is a machine for finding things that are not there, and is built to say so.** Four sliders over one stock is thousands of combinations and some will look excellent by chance. So the **universe-wide result and the pre-2020 / post-2020 split sit on screen beside whatever is being tuned**, and the caption names the best |t| anywhere on the grid so a promising-looking cell can be compared against it.
- **`lab-grid.js` calls `compute()` rather than composing the indicator itself.** The first version inlined the composition so it could cache volatility across parameter sets — a second implementation of the indicator, which is the exact thing `indicators.js` exists to prevent, and untenable the moment there were two. The caching bought seconds in a script that runs offline. One grid per indicator now, over that indicator's own knobs.
- **`lab-grid.json` is committed derived data.** 192 settings across two indicators, 60 KB, rebuilt by `lab-grid.js` in 28.1s against the local copy. Shipping it static means the page costs the server nothing; the page prints the build date so a stale grid is visible rather than silent. Re-run it after a backfill.
- **Log price axis above a 4x range**, the same rule the price chart follows — MU ran from $70 to $1,033 over three years and a linear axis flattened all but the last few months onto the floor.
- **What the grid says so far: nothing, for any of the three.** RSI's best top-decile t is **2.73** across 40 settings (it was 1.76 before the universe was pruned — the pool got easier, the indicator did not get better) — and note that is the *cross-sectional* view, where the per-stock decile shape above largely washes out.
- **What the grid says: nothing, for either of the first two.** Velocity's best top-decile t over the full period is **2.76** across 144 settings and the slope's is **0.87** across 48 — **1.73 and 0.80 respectively before the universe was pruned.** A number that moves that far because 19 laggards left is a number about the sample. With that many tests anything under about 3 is noise, and the split is the familiar one — the slope reads pre-2020 t −1.3, post-2020 t +2.0. **A better-estimated version of a flat thing is still flat**, which is the useful answer: the delta's problem was never its estimator.
- **`num()` returned 0 for null**, because `Number(null)` is 0 and finite. It never showed while the only input was closes, which are never null — the moment the score arrived, with 273 blank sessions before a symbol is scoreable, slopes were being fitted from a fabricated zero up to the first real reading. Caught by a count that was impossible: 4,988 slope values from 4,734 scores. **Reject the empties before coercing, not after**, and check counts against what the input can support.

## The help page
`/help` explains the app to the people using it, so it is open to **any signed-in user** rather than admin-only. Reached from `Help` in the ⋯ menu, beside Contact, and from a link in the Weights menu itself — which is where the question actually arises.

- **Sixteen numbered sections behind a contents grid**: what the app is, reading the table, how scoring works, the eight momentum factors, presets and Custom, the Quality score, screens, the stock page, the assistant, data freshness, the account, the Advice column, **tiles, news, the phone page and cards to share**. Each ends with a link back to the contents, and `scroll-margin-top` clears the sticky island on an anchor jump.
- **It is the manual, and there is deliberately only one (2026-09-17).** The owner asked for a user manual; `/help` already was one — signed-in rather than admin, reached from `Help` in the ⋯ menu — so the four features written after it were added to it rather than to a second page. A separate guide beside it would have drifted the way every other duplicated thing here has. The gaps were tiles (the switch, per-row, the shared range, and that Tile setup is site-wide), news (the column's two kinds of blank, the panel, the 12-hour ticker, Check now), the phone page (the four tabs, views, the chart toggle, and that a reload is what fetches new data), and cards (Download PNG at true size, and saved posts remembering controls rather than a picture). Three older sections were missing their back-to-contents link and got one. **No new links were added anywhere — the owner's call.** Verified in a browser: 16 contents entries resolving to 16 sections in agreeing order, every section reachable back, anchor jumps clearing the sticky island, no sideways scroll at 390px, no text under 12px.
- Written for someone with a hand on a control, not someone studying the model — every factor says what raising or lowering it changes, not only what it is. Owner-only features carry an `.adm` badge rather than being hidden, so members can see what the buttons they lack actually do.
- **It restates numbers that live in code** — the default weights, the 65/35 blend, the preset table. If those change, this page has to change with them; there is deliberately no attempt to generate it from `screens.js`, because the prose around each number matters more than the number.
- The caution about fitting a weighting to today's list lives at the bottom of the presets section, next to the thing that invites it.

## Momentum weight lens
**The editor lives on `/admin`; `/` and `/analysis` read it.** `/analysis` still carries its own Default · Trend · Steady selector above the screens; the screener shows `#lensChip` when the weighting is not Default and nothing otherwise. Each page keeps its own choice; the nightly report, the assistant and the stored snapshot always stay on Default, so the shared score remains one comparable measurement.

- **On the screener the lens reaches further, so it is marked loudly**: it changes Momentum and Overall, which means it also changes the sort. `body.lensed` tints those columns (`.lens-col`) and the picker in accent. Quality is deliberately left plain — it is the one score a momentum weighting does not change. Marking was a per-cell bottom rule at first, which drew a line between every pair of rows and read as a ladder rather than a column.
- **The CSV names the weighting in the column headers it alters** — `Overall (Trend)`, `Momentum (Trend)` — and in the filename. A comment row would have changed the shape every parser expects; a header rename travels with the file and leaves the column order alone, which the export is documented to keep stable.
- **`applyWeights()` restates the weights inside `momentumBreakdown` too**, or the factor tooltip would explain a score using percentages that did not produce it.
- **`renderWeightMenu()` carries its own escaper.** The only `esc` in `index.html` is scoped inside the CSV export; reaching for it there throws and takes `render()` down with it — this has now caused two bugs, so check the scope before using that name on that page.

It re-scores momentum **in the browser** — the screener, the nightly report, the assistant and the stored snapshot all stay on Default, so the shared score remains one comparable measurement.

- **It costs nothing to compute.** `momentumBreakdown` already ships every factor's sub-score to the client, so re-weighting is arithmetic on data that is already there: no refetch, no endpoint, no API credits. `applyWeights()` returns a shallow copy using the same field names, so screens and cell renderers need no idea a lens is in play.
- **`momentumBreakdownPrev` ships too**, or the change column would re-weight today and not the fortnight-ago comparison and quietly mix two schemes.
- **Presets key on a stable `key` per factor, not the caption.** Labels are prose and may be reworded; `key` is the contract.
- **Weights need not total 100** — `scoreWithWeights()` renormalises over whichever factors have data, mirroring `scoreFactors()`.
- **The axis is continuation versus caution**, not adjectives. Trend leans short-horizon, strengthens trend regime and removes the reversal brake entirely; Steady leans on 12-1 and consistency and keeps it.
- **The bar prints how far the weighting actually moves the list** — measured live *on the 82-symbol universe*: Trend reordered 69 of 82 names, median 4 places, largest 20; Steady 75 of 82. The universe is 116 now, so those figures are stale as absolutes; the bar computes them live and is not. A preset that reordered three names would be a dial that does nothing, and this says so rather than hiding it.
- **`Custom` adds sliders, edited in one place only** — the **Scoring** section of `/admin` since 2026-09-14, the screener's Weights menu before that. `/analysis` offers Custom as a fourth choice and reads the saved set, but never edits it: two editors would double the work and drift.
  - **`cleanWeights()` in `screens.js` is used by both the browser and the server.** Only the eight known factor keys survive, as integers in `0…MAX_WEIGHT`, and an all-zero map is rejected because it leaves nothing to score with. That is what keeps `PUT /api/prefs` from becoming free per-user storage — verified: `evil`, `__proto__` and a negative are all dropped.
  - **The menu is built on open, not on every render.** A rebuild mid-drag replaces the slider under the cursor and kills the gesture, so `render()` redraws only the trigger.
  - **Table redraws are debounced 120ms** while the numbers beside each slider update immediately. A full re-score and rebuild is ~80ms, far too slow to run on every step of a drag.
  - **Each slider shows its renormalised share** (`25 → 23%`) because weights need not total 100, so a bare number means nothing on its own.
  - **Any page that writes prefs must hand back the parts it does not edit.** `/analysis` sending `collapsed: {}` silently reset every collapsed column group — it now returns what it was given.
- **The saved choice is a preset id plus, for Custom only, the map behind it.** A bad id falls back to Default.

## The Advice column
**Displayed as "Advice" since 2026-09-12** (renamed from "Action" at the owner's instruction; earlier candidates Verdict/Stance/Call were passed over). **Every internal identifier keeps the old name** — group id `act`, `grp-act` classes, `s.action`/`actionFlag`/`actionTrend` fields, `ActionRules`, the `action` prefs id — the Scores/`rank` precedent, so saved prefs, code and tests stay stable while the words changed. The CSV headers are now `Advice` / `Advice Reason`; Help §12 opens with a one-line disclaimer ("a mechanical reading of the table, not a recommendation tailored to you"), which was the price of the friendlier word.

**A change since the previous trading session is marked** — a chevron before the Advice word (the momentum-arrow language, other side), tooltip and hover card naming yesterday's verdict. **The amber bar on the row's Symbol cell went on 2026-09-16** (owner: "I don't need that anymore as we have the changed filter now on top") — the Changed chip filters to exactly those rows and the chevron still marks which verdict moved, so the bar was a third way of saying the same thing. `adviceMoved()` stays; only the `chgRow` class and its rule are gone, and both refresh emails carry an "Advice changes" section between the movers and the fundamentals (Balanced rules, biggest tier jumps first, "none — every verdict held" when quiet). Mechanics: `computeStocks()` stores `prevTech` on each row (the engine's technical inputs one bar back — fundamentals are day-cached steps and stand for both days), `scoreActionInto()` re-evaluates it as `advicePrev`, and the client's `scoreActionRows()` does the same under the active profile so the marker follows the picker. **The indicator is empty until the first refresh after deploy stores prevTech.** Dry-run measured 12 changes across 93 rows on a normal day.

An **Advice** column group (formerly Action Model) of its own (id `act`, `#5eead4`, mirrored in rowcard.js and analysis.html like every group): `Type · Trend · Entry · Fund. · Guards · Action`, plus a per-cell tooltip on Action naming **the one rule that fired**. The four state columns are the intermediate computations made visible — each condition family collapsed to ONE word (`states()` in action.js), priority-ordered like the rules, so the derivation reads left to right: Trend answers *may*, Entry answers *now*, Fundamentals answer *conviction*, Guards answer *wait*. Guards is blank on ~95% of rows by design — a quiet column is one that means something when it speaks. Sorting the state columns ranks by exported severity ladders (`TREND_ORDER` etc.), not the alphabet. Colour only on danger words (Breakdown, Downtrend, Extended, Weak); the Action stays the one loud cell. This is the second build; the first (a points model) was reverted, and the owner's constraints for the redo are load-bearing: **no custom composites by default** (Momentum, Quality — `use_momentum` / `use_quality` restore the original gates), no MA Cross, loss-avoidance first.

- **First-match-wins rule lists, not scoring.** Each type has an ordered list; the Sell/Avoid/Hold rules sit above every Buy rule **in the source**, and config can move thresholds but never reorder the lists. That is the loss-avoidance guarantee, and it is why every verdict has exactly one nameable reason.
- **Blanks satisfy no comparison in either direction** — and that is why the "No trend data" rule exists: without it a young listing with a blank 200-day fell through every trend rule to a blind Buy with Risk. Second closed hole: Weak fundamentals block Buy with Risk above the 200D. Both fixes are toggleable (`fixes.*`) and the off state reproduces the source brief exactly.
- **Early is scored on different rules, not different thresholds** — growth, gross margin, burn, short interest; never P/E or earnings growth. ETFs (detected by tag or by having no Quality *and* no forward P/E) get the technical rules alone — which is also what makes the backtest honest: an all-technical row classifies as ETF, so `action-backtest.js` replays the real engine, no fork.
- **Backtest verdict, stated plainly** (307,875 stock-days since 2008, split at 2020): the tiers order **downside** correctly in both eras — 3M p10 pre-2020 runs −12.3% (Strong Buy) → −24.6% (Sell Immediately) — but **medians are flat across tiers** and Sell even bounces hardest. It manages risk; it does not pick winners. Fundamentals rules cannot be replayed (history began 2026-08-30) and the report says so.
- **All five advice columns are permanent since 2026-09-15 — the Rules picker is gone (owner's call).** Choosing which columns show is the job of personal column views (planned), not a picker per group; until then the Advice group collapses as a whole from the Columns menu. `applyAdviceCols()` survives only as the per-render hook for the filter summary, the `body.hide-av-*` rules and `extraProfiles` are deleted, and the screener no longer writes `prefs.advices` (the PUT whitelist still accepts it, harmlessly). *What follows describes the picker as it was:* **The advice is COLUMNS now, one per profile, and the Rules picker is a multi-select (owner, 2026-09-13).** The single Advice column is gone; in its place up to five advice columns inside the Advice group — **Balanced always first and always shown** (it appears in the menu as a locked always-on row), the other four toggled by the `Rules` picker (`n/5` trigger, menu stays open while toggling, choice saved in prefs as `advices`, an allowlisted id array). Each column carries the full kit: colours, the firing rule as tooltip, **its own change chevron against that profile's own yesterday**, its own sort (`av:<name>` sort keys), and **clicking a cell opens the ladder panel under that column's profile** (`panelProfile`; state cells open under Balanced, since the four state columns are pinned to Balanced). Columns hide/show by body class with the group banner's colspan recounted (`applyAdviceCols()` from `applyGroups()`) — headers are never rebuilt. Non-Balanced columns carry the teal tint (`td.avx`). `scoreActionRows()` runs ten engine passes (five profiles × today+yesterday, ~10ms) into `adviceBy`, stamping the Balanced results onto the classic `s.action*` fields for server parity — the CSV, hover card, emails and chatbot all still read Balanced and are untouched (the CSV's profile-suffix machinery was removed; the owner plans to retire the export). This absorbed and replaced the temporary TMP-5COLS inspection columns.
- **Clicking an Action cell opens the ladder panel** — the rule list as rungs (vetoes / caps / setups), the fired rung lit, rungs below it dimmed "not checked", every condition a mini-gauge (passing zone shaded, dot at the stock's value), establishment score as a 7-segment bar, and a model-level reference behind "How is this computed?" (three tabs, one per type). It is drawn from **`ActionRules.explain()` / `ladder()` in action.js, not from UI-side prose**: explain() emits the rungs from the same defs and rule order evaluate() walks and sets `mismatch` if the first all-conditions-met rung differs from evaluate()'s verdict — action-test.js sweeps 18,000 random row×profile evaluations asserting it never fires, and the live snapshot was checked row for row (93/93 agree). The panel recomputes client-side per click (`/action.js` is already loaded for severity sorting); no server changes. **Every other cell in the group opens a panel of its own** (`data-fam` on the cells → `openFamilyPanel`): Trend / Entry / Fundamentals draw that family's own priority ladder from `explain().families` — built by `buildFamilies()` in the same order `states()` collapses by, from the same `condKit()` builders the Action rungs use, with `familyMismatch` set if the lit rung ever differs from the column's word (in the same 18,000-evaluation sweep, and 93/93 live). Type shows the establishment points; Guards evaluates both checks with nothing dimmed, because guards stack rather than first-match.
- **The stock page carries a trend ribbon** — a strip under the chart painting the Trend column's word per session over the drawn range, replayed client-side through `ActionRules.trendAt()` (the engine's own `definitions()` + `states()` on a minimal row, exported for exactly this). Trend is the one family that is 100% bar-derived, so the ribbon is one uniform series over the whole archive with no fundamentals gap; breakdown thresholds follow the stock's **current** type, since the classification is not replayable — "today's rules, today's type, replayed", the momentum-recompute semantics. The averages come from the same `RowCard.sma` the MA overlays draw, so a colour change lands exactly where the lines cross; the crosshair readout gains a Trend row; `No data` stays unpainted. Verified: 400-random-readings property test (`trendAt` equals the column word), a 12-check headless replay over a synthetic rise-and-collapse, and today's word replayed from the archive against the scored snapshot — **92/92 exact**. **The dashed line trades ONE rule — the green states — and nothing else, at the owner's instruction (2026-09-13).** A Rules control briefly let the line trade a profile's verdict replay (`ActionRules.actionAt()`, tier ≥ BwR enters / Hold keeps / ≤ Avoid exits); the owner removed it after DELL made the case vivid — the verdict machine exited January's Breakdown and then sat flagged "Extended — wait for a pullback" for 96 straight sessions through a +250% recovery, a flat line under a solid green ribbon — and, decisively, **a full advice history is not honestly reconstructible, so the line may only claim what the bars can prove**. The verdict replay SURVIVES as an explainer only: the crosshair's Model row and the why card run it against Balanced. What the removed modes measured stays recorded: the five profiles replay nearly alike on technicals (their aggression is fundamentals-side, unreplayable); the real spread was trend-hold vs verdict replay (DELL +356% held vs +19.6% verdict-replayed). **"Why do these differ?"** in the ribbon legend opens `#whyBox` — `compareRuns()` diffs the two position series day by day, ranks the disagreement stretches by what the price did during each, quotes the model's own flag for every one, and `whyHtml()` templates the verdict sentence from the two measured buckets (entry friction vs held-through) — prose from numbers, never numbers from prose; the crosshair readout names the blocking flag on divergent days; the headline is test-pinned to equal both equity lines exactly. **The TREND toggle off sweeps everything trend**: line, ribbon, legend, why card (replay spreads measured while the modes existed: PTON −59% vs −5%, MU +11,485% vs +28,544%, NVDA the other way: +51,190% vs +15,775%). **Beside the price sits the trend-hold line** (TREND toggle, dashed because hypothetical): equity from holding only Strong uptrend / Above 200D, in cash otherwise, anchored to the price where the ribbon starts so flat shelves are the cash stretches and the gap between the lines is the verdict. **The word at day t earns the t→t+1 move** — the signal acts at the close it appears on, pinned by a headless test, the look-ahead class the strategy backtest caught once already. Pure price, zero costs, nothing earned in cash; the caption reports `Trend-held ±X% vs ±Y% hold · N round trips`, the trip count being the tell for what costs would take (30–80 trips on a Max view) — `/single` is the full instrument. Measured on the archive: PTON −59% vs −92% hold, ZM +140% vs +9%, KSS −70% vs −71%, NVDA +51,190% vs +28,522% — sidesteps the collapses, and at zero cost even keeps up with the monsters, which the trip count says not to over-read.
- **Cushion, and why the raw distance is not a safety ranking (2026-09-17).** The owner asked the right question — five stocks all read Strong Buy, which one — and the measurement answered it the other way round. `actionCushion` is `actionRisk.drop ÷ (realisedVol ÷ √12)`: the distance to the exit in the stock's **own monthly volatility**, stamped beside `actionRisk` in `scoreActionInto`. It is a column in the Advice group, it colours amber under 1σ and green at 2σ, and **sorting by any advice column breaks ties on it, thickest first whichever way the tier is sorted** — because it reads tail size, not direction. The research-log row above has the numbers; the short version is that raw room ranks the tail backwards and the σ figure ranks it correctly, and neither ranks return. Every surface says so: the column's tooltip, the ladder panel, the hover card's comment and Help §12. **"Risk to exit" was renamed "Distance to the exit"** everywhere for the same reason — it read as a safety score.
- **Risk to exit (2026-09-14)** — the honest half of the infographic's risk/reward: `exitDistance(r, cfg)` in action.js simulates a one-day decline in 0.5% steps over the ALL-TECHNICAL rule list (the same list the backtest replays) until the verdict reaches Avoid or worse, returning `{drop, action, flag}` or null past −60%. MAs, RSI, volume and fundamentals are held at today's values — a gauge of where the rules stand, not a stop order, and the panel says so. Server: `scoreActionInto` stamps `actionRisk` (Balanced) on every row on the way out; client: the ladder panel computes it under the OPEN profile via the same export. **drop 0 under a verdict still above Avoid is not a contradiction** — the gauge is technical-only, so it means fundamentals are doing the holding, and both surfaces word it exactly that way ("technicals at Avoid — fundamentals hold the verdict"). Tested: 6 checks incl. a 500-row random property (the reported drop always exits, one step less never does); 120 engine checks total.
- **The breakout volume marker (2026-09-14)** — the volume study's verdict surfaced as a FACT, never a rule (no advice rule reads volume; the study found the 200D upcross gets nothing from it). `computeStocks` stamps `volX` (today's volume / prior 20-day average) and `fresh3mHigh` (first close above the prior 60-session high — the study's E60 event, re-arming only after a slip back under); both empty until the first refresh after deploy, the advicePrev pattern. On a fresh-high day the Entry cell gains a small dot — amber at ≥1.5× (the confirmed kind whose sign survived the 2020 split), faint below (the quiet kind with no measured edge) — with the study's one sentence as the tooltip; the Entry family panel carries the same line labeled "Context — not a rule input", and the hover card a Breakout row. Help §12 documents both.
- `whipsaw.neutral_band_pct` is implemented (a band around the 200D counts as neither side); `persistence_days` is deliberately absent rather than a dead knob — it needs day-over-day state.
- **The chatbot reads the Advice columns now (2026-09-12), with attribution required** — the bot may report "the Balanced rules read this as Buy: <flag>" but never present it as its own recommendation; the open decision is closed. **Momentum and Overall went the other way the same day**: the owner pulled every momentum-family field from `CHAT_FIELDS` ("momentum is not working the way I thought"), and Overall went with it because it is 65% momentum — Quality stays as the one momentum-free composite. The rules' does-not-contain inventory names the withholding explicitly and forbids improvising momentum verdicts from the raw returns the bot still has. Advice under Balanced never reads the momentum composite (`use_momentum` off), so nothing is smuggled back. **The bot also gets a `trendTimeline` per row** — the trend state's last ~12 months as dated runs ("2026-01-07 Downtrend → 2026-01-20 Breakdown → …"), computed by `Action.trendTimeline()` during the refresh from the same 650-day archive window `pastMomentum()` already reads (zero extra queries), stamped into the snapshot. Bar-derived only, so it is exact where a full advice history would be fiction — **with one dated boundary**: from 2026-08-30 (fundamentals recording) and fully from 2026-09-13 (earnings-date columns), a full advice REPLAY is a pure function of stored base data, recomputable under current rules on demand. The owner chose replay over storage ("why should I store it"); nothing before 2026-08-30 is recoverable by any means.

## News
Per-stock headlines — **headline / source / url / timestamp only, never bodies** — from a swappable provider in `news.js` (the momentum.js shape: parsing and selection pure, orchestration in server.js). Default provider is **Google News RSS, keyless**, queried by company *name* ("<name>" stock) because Google is not ticker-tagged — expect occasional neighbour-stock noise in a feed; `FINNHUB_API_KEY` switches to Finnhub's ticker-tagged `/company-news` (free tier: 60 calls/min, personal use), and `NEWS_PROVIDER=off` kills the feature (routes serve empty, pages show quiet states).

- **Storage**: `news` (id = url-hash, per-symbol, pruned to 21 days / 25 items) + `news_state` (per-symbol fetch clock, separate so an empty feed still counts as fetched). Both in `SYMBOL_TABLES`, so a dropped ticker takes its headlines along. Writes are one wholesale batch per symbol.
- **Coverage without a schedule**: `topUpNews()` rides every refresh (each cron round included), topping up the `NEWS_TOPUP_PER_REFRESH` (12) stalest symbols, never-fetched first — the nightly job's ~20 rounds cycle the whole universe in one night. Started early in the round and **awaited before the response** — it was fire-and-forget until 2026-09-15, and on Vercel a function is frozen the moment it responds: batches were suspended mid-fetch, then every 6-second abort timer fired on the next wake, so two batches logged 54s and 279s with all 24 fetches "aborted due to timeout" for refreshes that took 19s. **Nothing may run after a response on this platform.** The batch still never rejects, so a failed news write never fails a refresh (the bars rule). `GET /api/news?symbol=` is stored-or-fetch with a 6-hour TTL so a visited stock page stays fresh regardless; on a provider failure it serves the stored set, stale beating nothing.
- **Frontends (deliberately only two)**: a News card on `/stock/<SYMBOL>` between the chart and the fields (ten headlines, escaped, http(s)-vetted links out, `target=_blank rel=noopener`), and one `News` row in the hover card's Info group — injected onto the rows by `loadLatestNews()` after the table paints (`GET /api/news/latest`, stored-only, one query — the sparklines rule), never a table column. The analysis page shows nothing because its rows never get `newsLatest`; `CHAT_FIELDS` still excludes news.
- Verified: 18 checks in the news suite (RSS entities/CDATA, publisher-suffix strip, dedupe caps, stalest picker, renderer escaping incl. a `javascript:` url refused), plus a live boot fetching real MU headlines through the whole path.

### The News column (screener)
**A News column in the Info group, and a panel behind it (2026-09-16).** Until now the only per-stock way in was hovering a row or opening its page; this makes the whole thing one click from the table.

- **The cell is the age of the latest stored headline** (`2h`, `4d`) with a **count badge** when the stock has more than one from the last week, brightened when the latest is under a day old. **Blank means two different things and the cell separates them**: an em-dash is "checked, nothing stored" with the tooltip saying when it was last checked, a faint dot is "not looked at yet" — headlines are topped up a dozen stocks per refresh round, so the second case is common and must not read as "no news". The latest headline is the cell's tooltip.
- **Sorting is on the timestamp, not the age**, because a new column starts descending and descending has to mean freshest first. Stocks with nothing stored sort last.
- **`GET /api/news/latest` now returns `{items, counts, checked}`** — the latest per symbol as before, a 7-day count per symbol (`newsCountsSince`, one bounded group-by over the pruned table: 5,849 rows on 2026-09-16, allowlisted in the query-plan test with that number) and `news_state`'s fetch clock. All three are guest-filtered. The screener already loaded this after the table painted for the hover card, so the column costs one extra small query, not a new request.
- **Clicking a cell opens `#newsPop`**: up to ten headlines with source and age, each opening the publisher in a new tab, plus a link to the stock page. It reads `GET /api/news?symbol=`, which is stored-or-fetch on a six-hour rule — so **a stock nobody has looked at gets its headlines on the first click**, at no API cost (the provider is free and keyless). Escape, an outside click or a resize closes it.
- **`?force=1` checks now** rather than waiting out the six-hour rule — the stock page's **Check now** button, beside a line saying when that stock was last checked. The provider is free and keyless, so a forced check costs no API credits; the server keeps a **one-minute floor per symbol** so a held-down button cannot hammer anyone, and a refused force answers normally with `forced: false` rather than an error. The route now returns `checkedAt` so the page can say "checked just now". Verified with the provider stubbed: 12 checks (no call inside the TTL, a call when forced, the headlines stored, the clock moving, the second force inside a minute refused, the button's states).
- Verified in a browser over four seeded stocks (fresh / old / checked-but-empty / never-checked): 15 checks — the endpoint's three parts, each cell's shape and tooltip, the panel's list with sources and links, the stock-page link, Escape, the "nothing stored" wording, and the sort order.

### The news ticker (screener)
**A slow crawl of headlines published in the last 12 hours, under the table (2026-09-15, owner's spec: slow, hideable, 12h then stale).** **It follows the table** — the owner first chose the whole universe, then changed it the same day: the strip shows only stocks `visibleStocks()` leaves on screen (portfolio, sector, industry, advice, Changed, column filters), the label reads "last 12h · stocks in view" while narrowed, and a view with no recent news says so in the strip rather than hiding it (hiding would resize the table on every filter change; the strip hides only when nothing in the universe is recent). **`render()`'s empty-table branch returns early, so it calls `paintTicker()` itself** — missed on the first cut and caught by the test. `GET /api/news/recent` reads stored rows only (`readRecentNews`, published_at ≥ now − 12h — ISO strings, so the comparison is lexical), keeps at most 2 per stock and one row per URL, universe-filtered and guest-filtered, and sends **every stock's share** (147 items / 70 KB at the time) — a universe-wide top 60 would leave most filters empty; the page takes the first 60 of what is in view. Item: `SYM ▲2.3% · headline · source · 2h`; the symbol opens the stock page, the headline the article (http/https only), both in a new tab.

- **`#ticker` is `flex: none` inside the page column**, so showing it takes ~44px from the table rather than growing the page; every visibility change calls `paintWindow(true)` because the windowed table sizes itself from `.table-wrap`'s height and no resize event fires.
- **Two copies of the items crawl by exactly one copy's width** (`translateX(-50%)`), so the loop has no seam; `--tk-dur` is set from the measured width at **60px/s**. Hover, keyboard focus and the pause button stop it (`animation-play-state`) — pausing moving content is a WCAG requirement, not a nicety. **Reduced motion gets no crawl**: one headline at a time, swapped every 6s with an opacity fade.
- **Rebuilt only when its items or the prices change** (`tickerKey` = urls + `lastUpdated`): `paintTicker()` runs at the end of every `render()`, and rebuilding the track restarts the crawl, so a sort or a filter must not. Re-read every 10 minutes, and the 12-hour window is enforced client-side too, so an item ages out between reads.
- **On by default; hiding is stored** (`prefs.tickerOff`, boolean-only in the PUT whitelist). The × on the strip hides it; `News ticker` at the top of the ⋯ menu toggles it back. Hidden when the window holds nothing.
- **Fixed first, because a ticker would have advertised it: news was searched by the quoted LEGAL name**, which matches mostly old filings that fall outside the 21-day window — 36 of 271 stocks held no headlines. `newsNamesFor()` now searches the display name (override, else `deriveShortName`), measured on all 36: legal-name queries found anything for 28 and stored nothing that survived the window; short names found 22-25 results for all 36, and at least as many for the control group. A stock holding no headlines whose fetch is past the TTL is now picked ahead of the rotation in `topUpNews`. **The short-name rule gained whole `American Depositary Shares` / `Sponsored ADR` / `N.V.` / `S.A.` tails** — 8 of 271 display names changed, every one a fix (AstraZeneca PLC American → AstraZeneca, Toyota Motor Corporation Sponsored → Toyota Motor, Adyen N.V → Adyen, NXP, STMicroelectronics, Spotify Technology, Ericsson, Nano Dimension); 14 rule cases pass.
- **Cadence caveat**: headlines are only as fresh as their last fetch — 12 stocks per refresh round plus stock-page visits. A 12-hour window therefore shows mostly the stocks fetched recently; the News runs page's coverage tile says how many that is.
- Verified: the filtered strip headlessly (11 checks — a sector shows only its stocks and all of them, a sort keeps the crawl, an empty table and a newsless view keep the strip with its message, one stock shows only its headlines, clearing returns to everything); the endpoint on live data (≤2 each, none older than 12h, no duplicate URLs; a guest saw only preview symbols); TM and AZN fetched through the fixed search (25 new headlines each, from nothing); a headless run on the real page over those headlines — 15 checks normal, 10 under `--force-prefers-reduced-motion`.

### The news log (`/news-runs`)
**Every batch of headline fetches is logged (2026-09-15)** — `news_runs` (one per batch) and `news_run_items` (one per stock in it), read by `/news-runs` (admin, a `News runs` row in the console's Data section). Two triggers: `refresh` (a refresh round's top-up, linked to the refresh run it rode on via `refresh_run_id`, whose kind the page shows — "Nightly round", "Fill missing round") and `page` (a stock page fetching headlines older than `NEWS_TTL_MS`).

- **`runNewsBatch(picks, meta)`** is the one path both triggers take: it logs each stock's outcome as it lands (fetched, **new** — not already held — and stored after the write) and closes the batch `complete`, `partial` or `failed`. **`writeNews()` now returns `{added, stored}`**, reading the symbol's prior ids and its final count in the same batch as the write, so the log costs no extra round trip.
- **`topUpNews` is called after `finishLiveRefresh` reads the run state**, so the batch knows its refresh run; it was the first line of the function before.
- **`abandoned`** = still `running` three minutes after starting (`NEWS_RUN_ABANDON_MS`). The top-up is fire-and-forget, and on serverless an instance can be frozen once the response has gone — this is the status that would show it happening. Swept lazily by every read.
- **The page**: coverage (last fetch under a day / under a week / older / never, the stalest stock, rounds for a full pass at 12 a round, provider), headlines stored (total, stocks with at least one, **stocks fetched but holding nothing**), the last 7 days (batches, fetches, failure rate, new headlines, batches that never finished), fetches per day for 14 days with new headlines as a line, filterable history, and a per-stock dialog linking to the batch's refresh run (`/refreshes?run=<id>` opens that run).
- **Retention**: 30 days of batches, 14 of per-stock rows, pruned after each batch.
- **First real reading, 2026-09-15: 36 of 271 stocks have been fetched and hold no headlines** (MS, RBRK, SKHY, BABA, CMCSA among them). The Google query is the quoted legal name + "stock", and a quoted "SK hynix Inc. American Depositary Receipt" matches nothing — the short name is the likely fix.
- Verified end to end locally: a stale stock page (EBAY, 13h) logged one batch — 25 fetched, 4 new, 25 stored, 668ms — and a second open inside the TTL logged nothing; route walk (anon/guest 302 and 403, admin 200, unknown batch 404); headless page over synthetic history.

## Logos — built and removed the same day
**There are no company logos anywhere (owner's call, 2026-09-14): the provider's marks aged badly and sat on white boxes, and the whole pipeline came out the day it went in** — the `logos` table (dropped from Turso), the fetch/override/serving routes, the admin button, its receipt email, the LogoFX transparency module, and every `<img>`. Tickers render as **typographic monograms** (`.lg` on the promo cards) or plain text; the stock masthead is back to the symbol alone. What the episode measured, kept for the next time logos come up: Twelve Data `/logo` costs 1 credit/symbol and its image URL is keyless (~7KB 128px JPEG) but the marks lag rebrands by years; Wikimedia thumbnails moved to `thumb.wikimedia.org`, exist only at pre-rendered sizes, and require a User-Agent; ~11 of 93 symbols (ETFs mostly) had no mark at all. If logos ever return, the per-symbol URL override was the workable shape and the bulk provider was the problem.

## The basket page
**`/portfolio/<name>` — one portfolio aggregated, for shared portfolios and a member's own lists (`my:<Name>` in the URL), reached from a chart-arrow beside every row in the portfolio picker (2026-09-14).** A screener view, deliberately not a tracker: no positions exist anywhere in this product, and the owner ruled out P&L tracking explicitly. First cut is three cards; more modules (composition, movers, earnings, news) are the planned second pass.

- **The curve** (`GET /api/basket?name=&days=`, requireMember): equal dollars at the window start, held — `mean(close/start)` per session over the union-of-dates axis, symbols with no history at the window start sit out and are counted in the caption. **The benchmark is the whole universe on the same convention**, not SPY: the benchmark is deliberately never archived, and "did my list beat my own screener" is the better question anyway. One `readBarsFor(all, since)` covers both curves (basket ⊆ universe); measured 1.27s at 1Y × 93 symbols, cached per range client-side. Ranges 1M/3M/6M/1Y; the chart is a hand-rolled dual-line SVG (no `preserveAspectRatio="none"`, so SVG text labels are safe here).
- **Stance**: trend-state stacked bar + counts, % above the 200D, Balanced advice rollup, and today's advice changes within the list.
- **Returns next to the whole screen**: mean of each stock's own horizon return (1W→1Y), list vs all vs difference — an equal-weight reading, the caption says so. Verified live: Mag 7 +11.0% 1Y against the universe's +60.4%, both curves anchored at exactly 1.0.
- Personal lists resolve against the CALLER's own `user_portfolios` via `prefsKey`, so one member can never read another's; guests are refused by requireMember and the page redirects them.

## Cards, and the module behind them
**`private/cards.js` owns every card: its markup, its wording AND its styles** (2026-09-14). Two surfaces draw from it — `/promo` (owner) and `/cards` (members) — and a second copy of "Top gainers this week" would have drifted inside a week, the same reason `rowcard.js`, `screens.js` and `action.js` exist. The contract is one call: `Cards.build(id, ctx)` with `{stocks, myLists, size, opts, getBasket}` returning the card's inner HTML. **The module reads no DOM and issues no requests** — the host hands it control values by control id and a `getBasket(days)` that returns the archive payload or null while it loads, which is what makes all sixteen card variants testable off-page in a `vm`. Styles ride along in a template literal injected by `Cards.injectStyle()`, so a grammar added to one page cannot be styled only in the other.

**A card is dated by its DATA, not by the reader's clock (2026-09-16).** `dateStr()` stamped `new Date()`, so a card built on a Saturday — or after a refresh had failed — printed today's date over Thursday's prices, and that card goes on Instagram. It is the freshest `latestDate` across the rows now, derived in `build()`, parsed at **local noon** because `new Date('2026-09-16')` is UTC midnight and renders as the 15th west of Greenwich.

**The card prints the refresh TIME beside that date** (owner: "why can't you show the same on the visual itself") — `Sep 16, 2026 · 4:37 PM ET` in the header pill, from `ctx.updatedAt`, which the studio, `/cards` and the phone's post endpoint all now pass. **Always America/New_York, never the builder's own zone**: the card is a baked image, so whoever renders it would otherwise bake their clock into something other people read, and the market it describes keeps one timezone. A host that does not supply the time gets the date alone rather than an invented one.

**Both studio pages say when the prices were pulled** (`#freshLine`, owner's request — "this will give me confidence that it is latest data"): the refresh clock, how long ago, and the day the cards will carry, from the `updatedAt` that `/api/stocks` already returned and both pages were discarding. It goes **amber past 26 hours**, because the one thing worth noticing before posting is that the numbers are yesterday's. The phone's post sheet carries the same fact as "data as of …". Verified on fresh and week-old snapshots: the wording, the flag, and the card stamping Sep 9 rather than today.

**Cards label by NAME, not by ticker** (2026-09-14, owner's instruction). `nameOf(row)` is `shortName || name || symbol` and `symOf(sym)` looks a bare ticker up through it; every card's rows, tiles, tracks, rankings, verdict roll-calls, scatter labels and the Intro live-data slides run through them. A feed reads a company faster than a four-letter code, and the screener carries a Short name column now, so the string exists on every row. The columns were restyled with it — the mono face gives way to the sans one, widths roughly double, and every one truncates with an ellipsis rather than wrapping a post into two lines.

**The Movers time window has calendar windows beside the rolling ones (2026-09-15)**: *This week* (`wtd`) runs from the last close before the current week's Monday, *This month* (`mtd`) from the last close before the 1st — "current" being the week and month of the freshest bar, so a Saturday post shows the week just ended. *Week* and *Month* stay **rolling** (5 and 21 sessions — the last 7 and ~30 days) and are labelled so in the dropdown; the card text for Week changed from "this week" to "past week", since that phrase now belongs to week-to-date. **The snapshot carries no calendar anchors**, so `GET /api/period-anchors` (member, cached 10 minutes) reads each symbol's last close before each boundary (`closesBefore`, ~80ms a boundary) and the studio stamps `wtdPct` / `mtdPct` = price ÷ anchor on its rows; a row without an anchor is simply not ranked. **An anchor more than 7 days older than its boundary is dropped** — a symbol with a gap in its bars (AVB, no bars after 2026-08-24, at the time) would otherwise show a move spanning weeks under a "this week" label. Only `/promo` offers the two windows; `cards.js` supports them wherever a host stamps the fields. Verified: the endpoint against an independent query (NVDA −2.25% week, −3.36% month), and headlessly on real prices (10 checks — both windows rank their own leaders, Week still ranks the rolling 7 days, compare column, side by side).

**The Movers card can carry the verdict** (2026-09-14, `movAdv`): off by default, else a rule set from `ADV_PROFILES`, scored in the browser through `advScored()` — the same cached engine pass the Advice cards use, so it costs nothing beyond the first profile. **The word rides UNDER the name, not in a column of its own**: "Sell Immediately" would need ~170px, and with the comparison column on there is not that much left; stacked it costs no width and about 2px of row height. **The rule set is named in the KICKER, not in a paragraph** — the first cut put it in an `s-sub` and the five extra lines of prose were most of what pushed the card over its own masthead; the attribution still has to be there, so it moved somewhere free. A row the engine cannot score simply has no second line. **The side-by-side layout is excluded** — it already runs two rankings in half the width apiece — and the promo control hides itself when Direction is "Both, side by side". `/cards` passes no `movAdv`, so the member card is unchanged; exposing it there is one option element.

**A card is a fixed artboard and cannot scroll, so a list too tall does not clip — it sits on top of the masthead**, because `.s-body` centres its content. The ranked list therefore picks its rhythm from the row count, the artboard and whether a verdict line is on (`ROOM` / `RHYTHM` in `tplMovers`): default, then `tight`, then `tighter`, each shrinking the bar, the name and the number **together**, since a short bar beside a full-size number reads as a mistake rather than as a denser card. Past the tightest rhythm the list is **trimmed** rather than overflowed — the same per-shape cap the side-by-side card takes — and because the rows are sorted by the move it is always the smallest that go, with the title saying "Top gainers" rather than a count so nothing on the card becomes untrue. Square at Top 15 with a comparison column lands there (11 rows); it overflowed before the verdict feature existed too. **Verified by a harness that measures the artboard independently of the builder's own estimates** — 72 combinations of shape × count × advice × compare × direction, 0 overflowing, tightest fit 46px of slack.

**Side by side on a Story fills the frame (2026-09-15)** — it used to render the post layout, dense past ten rows (names cut at 92px), bunched at the top of a 1920px artboard. On `sz-story` each row is two lines (name and move above, the bar full width beneath), the rows sit in `.mlist` and spread with `space-evenly`, and **the shorter column is padded with invisible `.mpad` rows** so the nth gainer stays level with the nth loser. Six rows or fewer get `.few` (40px type, 30px bars), because five rows spread through a story read as lines floating in a frame. Post and square are unchanged. Verified by measuring the real page, 3 shapes × 3 scopes × Top 5/10/15 × two windows: no overflow anywhere, columns level everywhere, the list uses 94-98% of the story height, at most 2 names in 30 truncated on a story.

**Two places deliberately keep the ticker.** The **Chart** card: its legend chips and end tags sit in inches of space beside a drawn line, where only a short code fits — the owner excluded it by name. And the Intro **mock** slide, which draws a fake screener row beside its star: that column really does show the symbol, so naming it would make the illustration lie about the product. The fundamentals scatter compromises instead — it names its eight outlier dots, anchors a label back towards the middle when its dot sits in the right half, and clips past 22 characters, because a name is many times wider than a ticker and would otherwise run off the artboard.

**`/cards` is the member room** (signed-in, not guest; a row at the top of the Research picker): the three data cards — Movers, Chart, Advice — one shape (the 4:5 post), controls in a bar above rather than a studio panel, and **Save image**, the same data-URI exporter. Keeping the export is deliberate: every card carries the wordmark, the domain and the disclaimer, so a member sharing one is distributing the brand with its rules attached. Personal lists appear in every scope picker, resolved against the caller's own rows. **`/promo` stays the owner's superset** — it keeps the other two artboard sizes, the crop guide, and the two templates that are the brand's own voice (Intro carousels, Announcement). Guests get neither page.

## The public side: a landing page and a blog
**Three pages a stranger can read (2026-09-16, owner's request — "a home page like tastytrade.com, and a blog open to all visitors").** Until now the whole site was a door: anonymous `/` redirected to a password form that never said what the site was.

- **`/` serves `private/landing.html` when signed out**, the screener when signed in — the one-line change in the root route. Structure follows the reference site (nav → hero with two CTAs → stat strip → what it is → an illustrated table row → the research-honesty section → data/freshness → FAQ → closing CTA → footer with the not-advice disclaimer), in this app's own voice and palette. **The illustrated row is labelled as an illustration** with made-up names; the two real numbers come from `GET /api/public-stats` (universe count + snapshot timestamp, cached 10 min, deliberately cheap — no row counts).
- **`/blog` and `/blog/:slug` are PUBLIC and RENDERED ON THE SERVER.** The pages are templates in `private/` with `%PLACEHOLDERS%` filled by the route, so a post's words are in the HTML: a blog that needs JavaScript to show its text is one that search engines and bad connections cannot read. Each post page carries its own `<title>`, description, `og:` tags and canonical link. `/robots.txt` and `/sitemap.xml` list the published posts.
- **Posts live in `posts` (slug primary key, `status` draft|published)** and are written at **`/posts`** (admin) — title, address, summary, markdown body, Save draft / Publish / Preview / Delete. Publishing needs no deploy. `published_at` is stamped on the FIRST publish and never moves on an edit, so the index does not reshuffle when a typo is fixed; a rename carries the post to the new slug rather than leaving two. The editor's preview renders through the SERVER's markdown, because a second renderer in the page would drift from what readers see.
- **`renderMarkdown()` escapes first, then formats** — the chat renderer's rule, and it matters more here: `<script>` in a draft comes out as text, and a `javascript:` link keeps its words and loses its href. Supported: headings (`#` becomes an `h2`, since the page owns the `h1`), lists, quotes, fenced code, bold/italic/inline code, `---`, and http(s) or internal links.
- **Drafts are invisible to everyone but the owner** — enforced in the route (`publishedOnly`), not in the page: `/blog/<draft>` is a 404 and the public list and sitemap omit it.
- The login page gained **Read the blog** and **What is Tickr Lab?** links, and the landing page's Try it button uses `/login?guest=1`, which the login page acts on — the guest door stays in one place rather than being POSTed from a public page.
- **The admin API routes had to move below `requireAdmin`'s definition** — registered beside the public ones they threw `Cannot access 'requireAdmin' before initialization` at boot, exactly as the `/api/db-stats` note warns. `node --check` passes that; only running it catches it.
- Verified: 34 checks against the real server with a password set, so the public pages are tested as a stranger sees them (publish/draft/rename/delete, the publish date surviving an edit and a rename, the escaping and link rules, anonymous access to `/`, `/blog`, a post, the sitemap and robots, 404s for a draft and an unknown slug, 403s for the admin API, the editor's redirect) plus 20 browser checks at 1440px and 400px (no sideways scroll on any page, headings present, the live stock count, no console errors).

## The phone page
**`/m` — a separate page for browsing screens on a phone, signed-in including guests (2026-09-16).** The screener is a 80-column table on a page that deliberately does not scroll, and it was never going to become responsive; the owner's call was a second page rather than a rewrite. **It has no settings of its own** — the only control is which *view* a row shows, and the views themselves are configured on a desktop at **`/mobile-setup`** (admin, a Mobile setup row in the console's Screener section).

- **The server runs the screen, not the phone.** `/api/stocks` is ~1.3 MB; trimmed to what a row card can render it is still 569 KB, which is not a payload for a phone on mobile data. So `GET /api/m/screen?id=&view=` filters, sorts and **formats** server-side and returns ~20 rows — **0.4 KB in the test, a few KB on the real universe**. `/api/m/screens` returns every screen with its live count, `/api/m/stock` one stock's grouped fields plus its stored headlines.
- **That is only possible because the filter grammar left the page.** `private/filters.js` (commit 9389322) holds `filterValue` / `compileFilter` / `screenRows` — extracted from index.html so the server can evaluate a screen exactly as the browser does. `rowcard.js` became Node-loadable at the same time (`})(typeof window !== 'undefined' ? window : globalThis)`), so `mobileRow()` formats through `RowCard.fieldValues()` — **the same text the hover card and the tiles show**, which is the point: a number cannot read differently on a phone.
- **`readScreens()` returns `group`, not `grp`.** The first cut of `/api/m/screens` read `sc.grp` and every heading on the phone came back `undefined` — caught by the test, invisible to a syntax check.
- **The setup**: up to **6 views of at most 5 fields** each, in `app_meta` under `mobile_config`, cached 60s per instance. `cleanMobileConfig()` rebuilds from scratch against `RowCard.fieldCatalogue()` — unknown field keys dropped, nameless or empty views refused, ids slugified and deduped — the prefs-PUT rule. **An empty list falls back to the three built-in views** (Move / Verdict / Value), so deleting the last one cannot leave a phone with nothing to show.
- **The chosen view lives in `localStorage` (`m.view`)**, which is right for it: it is a per-device convenience, not an account preference, and a guest has no prefs key at all.
- **Charts, and the toggle beside View (2026-09-16, owner's request — "it looks nicer").** On by default, remembered per device (`m.chart`). **The toggle controls the PAYLOAD as much as the display**: `chart=1` is what makes the server draw anything, so with charts off the rows fall back to the compact line they were and nothing is sent. The stock sheet's chart follows the same switch.
- **Guests are filtered server-side on every route**, the CDN incident's lesson: `mobileRows()` narrows to `GUEST_SYMBOLS` before any screen is evaluated, so the counts are over the preview too, and `/api/m/stock` 403s a symbol outside it.
- **Discovery**: a `Phone version` row in the screener's ⋯ menu, and `#phoneBar` — a one-line hint shown on the screener below 720px with a dismissal kept in `localStorage`. Showing or hiding it calls `paintWindow(true)`, since the windowed table sizes itself from its container and no resize event fires (the news ticker's rule).
- Verified: 56 checks against the real server on an in-memory database — the API and its validation, the phone at 390×844 (screens list with counts, a screen's rows, the stock sheet with its news, the view switcher surviving a reload, the three tabs, no sideways scroll, every tap target ≥36px), the charts (absent unless asked for, the thinning, the compressed cost, the line's colour and the area's, the toggle off/on and surviving a reload, the sheet's taller one), the desktop editor saving a rename and a field, the phone picking it up, and the screener's own table still aligned; plus 21 role checks with `ADMIN_PASSWORD` set — anon 302/401, guest limited to its two preview symbols and charted only for those, 403 on the setup, member 302 on the setup page and 403 on its PUT, owner 200 on both.

## Saved posts — the studio on a phone
**A studio card, named and kept, opened on a phone and saved to the camera roll (2026-09-16, owner's idea).** The workflow it exists for: set a card up on the desktop once, then on any day open it on a phone and post it to Instagram without touching a computer.

- **A post stores the CONTROLS, not a picture** — `{id, name, tpl, size, opts}`, the studio's control values by control id. Opening it tomorrow draws tomorrow's data, which is the whole point.
- **Site-wide and admin-curated** (owner's call), in `app_meta` under `promo_presets`, the Tile setup and Mobile setup pattern — these are the brand's posts, not a per-account scratchpad. `GET /api/promo-posts` is **member-only**, `PUT` admin. `cleanPosts()` rebuilds from scratch: template checked against `Cards.ids`, artboard against the three real sizes, up to `POSTS_MAX` 12. **Control ids are shape-checked, not listed** — the studio owns that list and restating it here would drift the first time a control was added; the card builder ignores an id it does not know, exactly as `cleanViews` leaves the column list to the screener.
- **NOT `/api/posts` — the blog owns that path**, registered higher up, and Express takes the first match: the first cut answered the blog's empty list and the studio showed "None yet" with no error anywhere. Renamed to `/api/promo-posts`. Nothing but running it finds this.
- **The phone lists posts by NAME and builds one only when it is opened** (owner's call: "I need to see the Preset name and when it click on it it should open the visualization"). `GET /api/m/posts` is the names — **319 bytes for three** — and `GET /api/m/post?id=` builds that one card from the snapshot as it stands, with `Cards.STYLE`. So the tab costs nothing, the archive read a chart card needs happens on the tap, and **opening a post is what makes it current**. A thumbnail shelf was built first and thrown away.
- **The phone would otherwise need the 1.3MB snapshot and the card module to draw its own** — the same reason `/api/m/screen` formats rows here. `cards.js` got the Node-loadable tail for it (`global.Cards`, and only `injectStyle()` ever touches the DOM).
- **Nothing is cached on the phone but three display choices** (`m.view`, `m.chart`, `m.range`), and there is no service worker — so a reload always hits the network and always brings the latest snapshot. That is the owner's stated rule for this page: **no poller, no restore-on-return; a refresh is how you get new data, and it must actually get it.** Pinned by a test that rewrites the snapshot and re-opens a post.
- **`basketPayload()` was lifted out of `/api/basket`** so the posts endpoint runs the same curve rules rather than a second copy. The studio always reads `name=All` and lets the card narrow it, so one basket per window covers every scope.
- **The video export is gone (2026-09-16, owner's call: "remove the export video option everywhere").** It rendered the animation frame by frame into a MediaRecorder; the Motion controls stay as a preview, and a screen recorder is how that becomes a file now. `Cards.freezeAt()` went with it — it existed only to photograph one instant of the animation — while `motion`, `unfreeze` and the number-counting helpers it shared stay in use.
- **Each saved post carries Replace and Delete as labelled buttons.** The delete was a bare `×` beside the word Replace and read as punctuation rather than as the other choice.
- **Clicking a post reloads the whole panel, and `render()` alone is not enough.** The template and size chips are painted by `paintControls()`; without it the card redrew while the chips stayed lit on whatever was chosen before, which reads as nothing having happened. `openPost` also sets every `*Sector` control first, calls `syncIndustries(true)`, then sets the rest — an industry only exists as an option once its sector is set, and depending on the order the keys happen to be stored in is not a plan. Pinned by a test that drives the real UI, saves a filtered card, moves template / shape / every control away, clicks the post and diffs all 43 controls plus both chips plus the card's own markup — and it was checked against the broken version first, where the two chip assertions fail.
- **`private/cardshot.js` is the rasteriser, and there were THREE copies of it** — promo.html, cards.html, and the phone would have been the third. Extracted: computed styles inlined onto a clone (a serialized SVG sees none of the page's stylesheets), the Google fonts fetched once and embedded as data URIs (an external ref inside an SVG-as-image is dropped, and a face still pointing at the network *taints the canvas*), then foreignObject → canvas at the card's true pixel size. Verified straight after the move: a real 1080×1350 PNG, 538 KB in 249ms, and the studio's own Download button still downloading.
- **The artboard's own styles moved into `cards.js` as `.s-art`**, for the same reason — promo.html and cards.html each had a copy, and the phone would have made three. Each host still owns what is genuinely its own: the studio scales it to the window, `/cards` fixes one shape and adds a drop shadow.
- **The phone's fourth tab, Posts** (hidden from guests: the studio is member-only, and these carry portfolio names and whole-universe rankings the preview withholds). Thumbnails are **the real card scaled**, not a reflowed copy — a 1080px artboard with `transform: scale(measured width / 1080)`, so the picture tapped is the picture saved; the scale is measured after layout rather than computed, so rotating the phone re-fits it. **The frame must be `display:block`** — it is a `<span>` inside a `<button>`, and its height was silently ignored until it was.
- **Tapping a name opens the card itself**, scaled to the sheet, with the data's timestamp on it; **Save as an image** is the second step, because rasterising 1080×1920 on a phone takes a moment and is only wanted when the file is. A screenshot would be the phone's aspect ratio with a status bar in it; the PNG is the exact artboard. The card is built off-screen at 1080 (`#shot`), shot, and removed; the object URL is released on `sheetclose`.
- **Two bugs a screenshot caught that passing assertions did not.** The PNG came out **perfectly sized and completely blank**: `inlineStyles` copies the original's own placement onto the clone, so the off-screen `left:-20000px` went with it and the card rendered outside the canvas — `shoot()` now neutralises the clone's position (to `relative`, not `static`: the card is the containing block for its own aura). And `.sheet h2` beat `.s-title` on specificity, quietly resetting the card's 66px headline to 18px — every sheet rule that a card could also match is a **child** selector now. The tests check ink and computed type, not just dimensions.
- Verified: 29 checks — validation (unknown template, unknown artboard, nameless, junk control ids including `__proto__`, the cap), every post built server-side with its artboard and the chart card actually reading the archive, the studio listing / opening / saving (template, size and controls all restored), the phone's tab and thumbnails (real card, scaled, right shape, no sideways scroll), a 1080×1350 and a 1080×1920 PNG rendered on the phone as blobs, the off-screen stage cleaned up; plus the role walk (guest 403 on both post routes, member read-only, owner writes and every member sees it) and the studio export re-verified after the extraction.

## The admin console
**`/admin` (admin only, the one `Admin` link in the screener's bar) — everything that is not a description of the table (2026-09-14).** Six sections behind a jump bar: **Data** (both refreshes, Database, News runs, Refresh runs, Fill missing, add a ticker, forward returns), **Research** (Cards, Analysis, Strategy, Single, plus the three per-stock pages behind a symbol picker), **Scoring** (the momentum weighting), **Studio** (Promo, member Cards), **People & logs** (Users, Visitors, Activity), **Reference** (Help, Ask, Contact). The bar it replaced carried six pickers and eighteen buttons.

- **The console's refresh rows open `/refreshes` (2026-09-15)** — `/refreshes?do=refresh|refreshall|refreshmissing` — and that tab runs the refresh while its live card, rounds and history update beside it. **The round loop moved into `private/refresh-loop.js` (`RefreshLoop.run(mode, api, hooks)`, `RefreshLoop.runPlain(api)`)**, shared by the screener and `/refreshes`, so there is still exactly one copy of the pacing (62s), the budget (`ceil(work/7)+6`, Fill missing sized from its targets), the refusal allowance (8), the stagnation stop (3) and the closing DELETE; pages pass their own `api()` and draw progress from hooks. `/refreshes` strips `?do=` before acting, refuses to start while a run is already live, warns before the tab is closed mid-run, and says how the run ended (done / nothing to do / stopped / ended short). The screener still honours `?do=` for old links. Verified: 13 loop unit checks (every outcome, run-named rounds, DELETE always sent, budgets), headless runs of all three console actions plus the already-running guard on `/refreshes`, and the screener's Refresh all through the shared loop.
- **The console is a door, not a second machine.** *(Superseded for the refreshes by the entry above.)* Both refreshes are deep links — `/?do=refresh` and `/?do=refreshall` — because the round loop, its budget, the progress banner and the table being refreshed all live on the screener; a second copy of that loop here is exactly the drift `screens.js`, `action.js` and `cards.js` exist to prevent. The forward-returns view is the same: `/admin` picks the date and links to `/?asOf=YYYY-MM-DD`, and the screener still owns the mode, its banner and its column group. **The screener strips the instruction out of the address bar with `history.replaceState` before acting** — a reload that re-ran `do=refreshall` is ten minutes and several hundred API credits.
- **Add a ticker is implemented here**, because it is one API call with no table state behind it, and it reports the resolved company name — a name coming back empty is still the earliest hint of a typo.
- **The weighting EDITOR is here and nowhere else.** It left the screener's bar rather than being copied, so "edited in one place only" still holds; the screener and `/analysis` read the saved set and neither edits it. **The console must hand back every pref it does not touch** — a prefs PUT replaces the whole row, so it merges into what `GET /api/prefs` returned rather than sending the weighting alone. A screener tab left open still holds the old weighting and would write it back on its next pref change, so the save message says to reload it.
- **What the screener kept instead of the picker: `#lensChip`**, shown only while a non-default weighting is in force. The two score columns are tinted whenever the lens is on, and with the picker gone nothing else would say why.
- **"Disable the button" became a flag.** `setPulling()` toggles `body.pulling` (a progress cursor) and guards `refreshAll()` from starting twice; the buttons it used to grey out are on another page.
- **Members are not locked out of anything they had.** `/admin` is admin-only, so the research menu stays in their bar; what they lose is editing the weighting, which `/analysis` still lets them switch by preset.
- Verified on introduction by a route walk (admin 200 on `/admin`, anon redirect, `/admin.html` funnels to `/admin`) and by a check that every `$('id')` the page asks for exists in its own markup — a syntax check cannot see a `TypeError` from a control that was deleted out from under its listener.

## The database page
**`/database` (admin only, a `Database` row in the console's Data section) lists every table with its exact row and column count (2026-09-15).** `tableStats()` in db.js reads `sqlite_master`, then one **read batch** of `count(*) from pragma_table_info(?)` plus `count(*)` per table — measured 41-152ms for 21 tables and ~397k rows. Views would be listed with columns but not counted (counting a view runs its query); there are none today.

- **`GET /api/db-stats` caches the result five minutes per instance**, because every count reads every row and Turso meters rows read (~397k a count, nearly all `bars`); `?fresh=1` (the page's **Count again**) always counts.
- **Descriptions and areas are a static map in the page** (`TABLES`). A table added later still shows, under *Other*, with no description — add it to the map.
- **`/api/db-stats` must sit below `requireAdmin`'s definition.** The first cut registered it beside the page route near the top of server.js and the server failed to boot with a TDZ ReferenceError — `node --check` passes that; only the route walk caught it.
- Verified: local walk (anon/guest 302 and 403, admin 200, `/database.html` funnels), cached vs fresh responses, counts equal to an independent query.

## The NASDAQ reference list
**`/nasdaq` (admin only, linked from the console's Reference section) — every US listing NASDAQ publishes, in its own table, joined to NOTHING (2026-09-14).** It exists so a candidate ticker can be *found*; adding one to the screener is still a deliberate act, and the owner's instruction was explicit: do not mix this with the screener.

- **Why it exists at all.** Twelve Data will not sell this on the Pro plan at a sane price, measured rather than assumed: `/stocks` is free and lists 16,399 US common stocks but carries **no market cap**; `/profile` (10 credits) has sector, industry and employees but **no cap field either**; `/statistics` (50 credits) is the only Pro source of `market_capitalization` and **does not batch** — `symbol=AAPL,MSFT` returns 404 and still charges 50. Sweeping NYSE + NASDAQ (5,747 names) would be **~287,000 credits and about eight hours** at the 610/minute ceiling. There IS a `/market_cap` endpoint at **1 credit**, and it is **Ultra-only** — it answered for AAPL and 403'd for MSFT and NVDA, the free-sample-symbol trap this file already warns about, caught a second time.
- **The source**: `api.nasdaq.com/api/screener/stocks?tableonly=true&limit=25000&download=true&exchange=<EX>`, free, keyless. All 12 published fields are stored plus the exchange, which the API does not return and the *request* therefore supplies — three calls, NASDAQ 4,131 + NYSE 2,710 + AMEX 295 = **7,136 rows**, of which **4,344 clear $100M** and 398 carry no cap at all.
- **The User-Agent is load-bearing and the failure mode is nasty.** Given a bot-shaped agent the endpoint does not answer 403 — **it never replies at all**. Measured: an honest `TickrLab/1.0` agent hung until a 20s abort; a browser-shaped one answered 200 in 296ms. So `NASDAQ_TIMEOUT_MS` is not belt-and-braces, it is what stops a serverless function sitting on its hands until the platform kills it.
- **One exchange per request**, `POST /api/nasdaq/refresh {exchange}`, driven three times by the page — the Refresh-all shape. Measured server-side: AMEX 0.3s, NYSE 1.0s, NASDAQ 2.7s, so the split is comfortable rather than necessary. Each exchange is a wholesale replace, which is how a delisting leaves.
- **`{ ...row }` on a libSQL Row gives POSITIONAL keys.** The first read shipped `{0: 'AAPL', 1: 'NASDAQ', …}` and every field on the page read `undefined`; named access works, spreading does not. Accessors here build the object field by field.
- **`url` is stored but never shipped** — all 7,136 are exactly `/market-activity/stocks/<lowercase symbol>`, checked, so the page derives it and the payload drops 277 KB. What is left is ~2.0 MB, ~370 KB gzipped, fetched once for an admin page that then filters in the browser.
- **The page marks rows already in the screener** (`held`, from the universe in `/api/portfolios`, NASDAQ's `BRK/B` matched as `BRK.B`) and can filter them out, which is the actual workflow — hunting for what you do not have. **Only symbols flow the other way, and only when ticked** (see below); no NASDAQ value — cap, sector, price — is ever written into the screener.
- **Bulk add (2026-09-15, owner's choice of the bulk-import options).** A checkbox column (held rows and `^` preferred shares disabled), a header box that selects every MATCHING row in sort order (not just the drawn page), and a selection bar: count, the data-pull cost (~81 credits a stock, 7 a minute of Fill missing), an optional "Also add to <portfolio>", Add. `POST /api/universe/bulk {symbols, portfolio?}` (admin, ≤ `BULK_ADD_MAX` 500): `/` → `.`, `SYMBOL_RE`, dedupe; answers `{added, already, invalid}` plus the universe. **Names come from the listing** (`cleanListingName` strips "Common Stock", "Ordinary Shares", ADR tails), only where none is stored — **so adding costs no credits**; the data arrives with Fill missing (the done bar links `/refreshes?do=refreshmissing`) or the nightly gap fill. **Read the universe BEFORE the portfolio write** — `writePortfolios()` inserts members into the universe, and reading after it reported every new stock as "already there" with no names; caught by the test. A symbol the data provider does not know shows as an error row after the pull and stays a Fill missing "failed" gap until removed. Verified: API caps/404, and the page in headless Chrome against the real server on an in-memory database (23 checks — disabled rows, BRK/B matching, cost line, portfolio add, names trimmed and never overwriting, select-all capped and filter-respecting, held after add, no page errors). Its sector and industry columns are labelled *(NASDAQ)* because they are a third taxonomy agreeing with neither GICS nor the Twelve Data values on the screener.
- The ballast query the research log wants is one filter away: pre-2010 listing, $100M–$2B today — **276 candidates** at the time of writing.

## The promo studio
**Every scope in the studio has an Industry picker under its Sector picker (2026-09-15)** — `movIndustry`, `chtIndustry`, `spkIndustry`, `rngIndustry`, `advIndustry`, `fundIndustry`. `scopeOf()` in cards.js derives the industry key from the sector key (`xxSector` → `xxIndustry`), so a host without the control (the member `/cards` page) filters nothing and is unchanged; the kicker names the narrowest cut (the industry alone, since it implies its sector). `syncIndustries()` runs at the top of every studio render and refills an industry list only when its sector changed, offering just that sector's industries (`Cards.industries(rows, sector)`) and dropping a choice the new sector does not hold. Verified headlessly on the live snapshot: 19 checks (placement under Sector in all six, nesting, card rows only semiconductors, reset on sector change, a second template, a host with no control). **Open to every signed-in member since 2026-09-15, from a `Promo Studio` button in the screener's bar after Rules** (hidden for guests, whose `/api/basket` 403s; the route redirects them, and signed-out visitors go to `/login`). **The Announcement template stays the owner's** — `OWNER_ONLY_TPLS` in promo.html filters it from the template list unless `/api/me` says admin; it is free text with no data behind it, so there is nothing server-side to guard. The page now loads `wmark.js` like every member data page (the owner is skipped). The Promo row left the admin console's Studio section, which keeps the Member cards row. Verified: headless template lists per role (member 8 templates without Announcement, owner 9) and a route walk (anon → login, guest → `/`, owner 200). *Originally:* **`/promo` (admin only, ⋯ menu): fixed marketing-card templates rendered from tonight's data in the site's own theme, for manual screenshotting** — no social integration by design (2026-09-14). A real 1080px artboard (4:5 / square / 9:16) scaled to fit the window, so crops line up post after post; every card carries the same chrome (wordmark, date, `tickrlab.com · N stocks screened nightly · Mechanical readings — not investment advice`). Five templates, all client-side from `/api/stocks` (tickers as typographic monogram tiles — logos were removed): **Movers of the week** (top |1W| moves), **The rules changed their mind** (advice changes with the fired rule named — the differentiator card), **Breakout radar** (fresh 3M highs, amber ≥1.5× volume badge), **Where the market stands** (universe by trend state), and **Announcement** (kicker/headline/body free text in the house type). **The no-verdict rule extends to marketing**: cards report what the screen and the rules did, never "stocks to buy" — the owner signed off on that line explicitly. Empty states are written to still be postable ("every verdict held today"). Templates were VM-smoke-tested (four builders + two empty states) and the route walk verified (admin 200, anon/guest redirect, `/promo.html` funnels to `/promo`).

## Screens (the Yahoo-style screener)
**`/analysis` was folded into a Screens menu on the screener on 2026-09-15** (owner's decisions: picker beside View, starter screens only — no personal screens —, the full Yahoo-mapped table plus the seven /analysis screens, two new data fields, /analysis retired, no market-wide movers). `/analysis` now redirects to `/`; `analysis.html` is deleted. **Members lost the /analysis weighting selector with it** — the momentum lens is now set only from /admin, for the account doing the setting.

- **A screen is `{filters, sector?, industry?, advice?, changed?, sort, columns}`** — filter-row grammar for the filters, the bar pickers' state for the bound ones, and a column list applied like a view. Stored in `screens` (id, name, grp, position, description, def JSON), **seeded once** from `STARTER_SCREENS` in server.js (22 screens in six groups) behind the `app_meta` marker `screens_seeded`. `GET /api/screens` (any signed-in, guests included), `PUT /api/screens` (admin, whole-list replace through `cleanScreens()`: keys and columns shape-checked, values capped, sort dir forced to ±1).
- **Applying one** (`applyScreen`) saves the table's state the first time (`screenBase`: sort, filters, bound pickers, Changed, filter-row visibility), sets the screen's, opens the filter row when it shows anything, and **overrides the view through `activeView()`** — `activeViewId` and the saved pref are never touched. **Clear screen** restores `screenBase`. Picking a View while a screen is active keeps the filters and drops the columns (`screenColsOff`); **any difference from the definition reads "(edited)"** (`screenEdited`). Nothing is persisted — filters are not saved, and a screen is a set of filters.
- **Live counts** per screen in the menu, over `tabStocks()` (the portfolio in view), computed by `screenRows()` with the same `compileFilter` / `filterValue` the table uses.
- **Filter extensions the screens needed**: list filters accept either-of values (`Breakdown|Downtrend`, shown "Breakdown or Downtrend"); **screen-only keys** with no column — `fresh3mHigh` (Yes/No), `lastSurprise`, `daysSinceEarnings` — labelled by `SCREEN_ONLY_KEYS` in the filter strip.
- **Owner editing is from the table**: set it up, then **Save the table as a starter screen** (name, heading, one line), or the refresh icon beside a screen to replace it with the table as it is now, or delete. Everyone else sees a lock on the heading.
- **Six new columns**, all derived from bars already fetched (no credits) and filled from the next refresh: **Relative** gains `52W Lo days` / `52W Hi days` (`daysSince52wLow/High`, sessions since the extreme — `extremeAges()`) and `52W range` (`range52Pos`, already computed, never shown before); **Volume** gains `Rel vol` (`volX`), `Volume` and `$ Volume` (`volume`, `dollarVolume` = volume × close, trading currency). Row width is 64 cells (`PAD_SPAN`), the error row spans 59, the empty row 61.
- **Honest labels kept from the discussion**: Bullish/Bearish are our Trend/Entry rules, Strong Buys is the Balanced rule set, earnings growth is last quarter YoY (Yahoo's growth screens use forecasts, Ultra-plan only), PEG is unreliable for loss-makers.
- Verified: headlessly as member (34) and owner (36) against a stub — the 64-cell rows and aligned headers, 22 screens with counts matching independent counts, a screen's rows / sort / columns / filter-row inputs / chips, "edited", sector-bound, either-of, screen-only chip, Advice and Changed screens, a View dropping the columns, Clear restoring sort / filters / pickers / filter row, owner save and delete; the real server — seeding (22 in 6 groups), guest read-only, anon 401, `cleanScreens` on junk, the list restored; `extremeAges` unit-checked; views, filter row, scores and advice suites unchanged.
- **An incident the same day: every screen lost its `<`/`>` filters in production.** `SCREEN_FILTER_VAL_RE` excluded `<` and `>`, which the filter grammar is made of (`>=3`, `<20`), so `cleanScreens()` silently dropped 13 screens' filters — and the local test's "restore the list" PUT went through that sanitizer against the production database, so the stored screens kept only their sorts and columns (Day gainers sorted everything by Today and filtered nothing). The stub tests passed because they fed `STARTER_SCREENS` to the page directly and never round-tripped it through the server. Fixed by allowing any printable value (everything is escaped where shown), a **round-trip check that `cleanScreens(STARTER_SCREENS)` keeps every filter**, and the 22 definitions written back to production. Lessons: a test that writes to production must restore through a path that cannot transform the data, and a sanitizer needs a round-trip test on the real definitions.
- **Found and fixed on the way**: the member Research menu still linked to the deleted `/signal` page, and **`/lab` has been broken since 2026-09-15's momentum cleanup** — `private/signal-stats.js` was deleted though `lab.html` calls `SignalStats.summarise/buckets/mean`; the file is restored from the commit before its deletion.

## Analysis screens (retired — kept for the thresholds' history)
**The seven predicates live in `private/screens.js`, not in the page.** `analysis.html` loads it with a `<script>` tag and `server.js` `require`s it, so the nightly report and the page can never disagree about what "bouncing off the lows" means — the same reason `rowcard.js` exists. Only the *selection* is shared (which rows, in what order); the columns, the prose and the empty messages stay with whichever surface is drawing them. Verified equivalent against the live universe on extraction: all seven lists identical, order included.

`/analysis` is seven filtered views built from the **raw fields, not the composite scores** — the scores already drive the table's ranking, and a screen that just re-sorts them adds nothing. Every threshold below was set by running the candidate against the live universe: a screen returning 0 names is a dead box, and one returning 25 of 69 is not a signal. **The hit counts in the table were measured at 69 symbols and the universe is now 116 — expect every one of them to be larger, and the 33 `Faded` names to crowd the bounce and value screens in particular.** Re-tune the thresholds against a fresh snapshot before reading anything into them.

| screen | rule | hits when set |
|---|---|---|
| Bouncing off the lows | `range52Pos ≤ 30` (or `pctFromHigh ≤ −25`), `1M > 3%`, `2W > 0` | 5 |
| Just started moving | `2W > 5%` while `3M < 0` | 2 |
| Drifting after a beat | surprise 10–100%, reported ≤ 21d | 2 |
| Reporting in 14 days | unchanged | 4 |
| Cheap, growing, profitable | fwd P/E < 20, rev > 15%, margin > 15% | 13 |
| Business improving, price isn't | rev > 25%, `3M < −10%` | 13 |
| Overextended | RSI > 75 and > 12% above the 50-day | 2 |

- **`range52Pos`** (0 = on the 52-week low, 100 = on the high) and **`pctFromLow`** are derived in `computeStocks()` from bars already fetched, so they cost no credits. Snapshots written before they existed fall back to `pctFromHigh ≤ −25`, and the section prints a note saying so. **The `≤ 30` threshold is unverified against real range data** — it was tuned on the fallback — so expect to adjust that one number after the first refresh.
- **Earnings surprises above 100% are excluded** as feed artefacts. Two different mega-caps currently report *exactly* +214%, which is a data bug rather than a coincidence; without the cap they would top the drift screen.
- **`netCash` is meaningless for financials** — if a balance-sheet screen is ever added, exclude the Financial Services sector, or JPM and HOOD will lead it.
- `analystConsensus`, `targetUpside` and the forward estimates are **0/69** on the current plan, so nothing can be built on them.
- Two removed screens ("Good business, price not working" / "Price working, business not") keyed off Quality and Momentum ratings; "Business improving, price isn't" is the same idea on raw revenue growth. "Deepest drawdowns" went too — it is the bounce screen's population without the part that matters, whether the thing has turned.

## API patterns
- `GET /api/stocks` — serves snapshot to public; `?refresh=1` recomputes live (admin only)
- `GET /api/stocks?asOf=YYYY-MM-DD` — the forward-returns view (admin only)
- `POST /api/refresh-all` — expires the profile cache, forces re-pull (admin only); `DELETE` ends the refresh flag
- `GET /api/status` — `{ refreshing }` only; polled by every open page during a refresh
- `POST /api/cron/refresh` — the nightly job's one round (bearer `CRON_SECRET`, not admin); `?start=1` begins a run, `DELETE` ends one early
- All portfolio/ticker CRUD routes require admin

## Conventions
- All persistence goes through `db.js` — never reintroduce `fs` reads/writes for state; a serverless filesystem discards them
- No build step — vanilla JS, plain CSS. The design system lives in `public/app.css`; each page keeps only its own layout in one inline `<style>` block
- Admin-only UI elements use class `admin-only` — toggled by `applyAdminUI()` in index.html
- **The CSV export was removed on 2026-09-14** (the owner had planned to retire it; it was the one bulk-copy button in the UI and fell with the watermark work). `esc()` — the escaper scoped inside it that twice caused bugs — went with it; `we()` is now the page's only escaper. Older notes in this file about CSV headers naming the weighting/horizon describe the removed feature.
